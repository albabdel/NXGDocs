---
title: "NTP Server Configuration for GCXONE"
description: "Complete guide for configuring Network Time Protocol (NTP) servers in GCXONE to ensure accurate time synchronization across all devices"
tags:
  - role:installer
  - category:configuration
  - difficulty:beginner
  - platform:GCXONE
sidebar_position: 10
last_updated: 2025-12-04
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

export const DocHeader = () => {
  const cardStyle = {
    background: 'var(--ifm-background-surface-color)',
    border: '1px solid var(--ifm-color-emphasis-200)',
    borderRadius: '0.75rem',
    padding: '1.5rem',
    boxShadow: '0 1px 3px 0 rgba(0, 0, 0, 0.05)',
    transition: 'all 0.2s ease'
  };
  
  const badgeStyle = {
    display: 'inline-flex',
    alignItems: 'center',
    padding: '0.375rem 0.875rem',
    borderRadius: '9999px',
    fontSize: '0.75rem',
    fontWeight: '500',
    background: 'var(--ifm-color-primary-lightest)',
    color: 'var(--ifm-color-primary-darkest)'
  };

  return (
    <header style={{
      background: 'var(--ifm-background-color)',
      marginBottom: '2rem'
    }}>
      <h1 style={{
        fontSize: '2.25rem',
        fontWeight: '800',
        color: 'var(--ifm-heading-color)',
        marginBottom: '1.5rem',
        lineHeight: '1.2'
      }}>
        NTP Server Configuration for GCXONE
      </h1>
      
      <div style={cardStyle}>
        <p style={{
          fontSize: '1rem',
          color: 'var(--ifm-color-content)',
          lineHeight: '1.75',
          marginBottom: '1rem'
        }}>
          Network Time Protocol (NTP) configuration is a <strong>critical step</strong> in setting up your security infrastructure within the <strong>GCXONE</strong> ecosystem. <strong>Accurate time synchronization</strong> ensures that event logs, video playback, and alarm triggers are perfectly aligned across all devices and the cloud platform.
        </p>
        
        <div style={{
          background: 'var(--ifm-color-primary-lightest)',
          border: '1px solid var(--ifm-color-primary)',
          borderRadius: '0.5rem',
          padding: '1rem',
          marginTop: '1rem'
        }}>
          <p style={{
            fontSize: '0.875rem',
            color: 'var(--ifm-color-primary-darkest)',
            margin: 0,
            fontWeight: '600'
          }}>
            ⚠️ Important: Discrepancies in time can lead to misaligned event sequences, playback errors, or difficulties in forensic investigations.
          </p>
        </div>
      </div>
    </header>
  );
};

export const NTPServerAddresses = () => {
  const sectionStyle = {
    padding: '2rem 0',
    borderTop: '1px solid var(--ifm-color-emphasis-200)'
  };
  
  const headingStyle = {
    fontSize: '1.5rem',
    fontWeight: '700',
    color: 'var(--ifm-heading-color)',
    marginBottom: '1.5rem',
    lineHeight: '1.3'
  };
  
  const cardStyle = {
    background: 'var(--ifm-background-surface-color)',
    border: '1px solid var(--ifm-color-emphasis-200)',
    borderRadius: '0.75rem',
    padding: '1.5rem',
    boxShadow: '0 1px 3px 0 rgba(0, 0, 0, 0.05)',
    marginBottom: '1rem'
  };

  return (
    <section style={sectionStyle} aria-labelledby="ntp-servers-heading">
      <h2 id="ntp-servers-heading" style={headingStyle}>
        GCXONE NTP Server Addresses
      </h2>
      
      <p style={{
        fontSize: '1rem',
        color: 'var(--ifm-color-content)',
        lineHeight: '1.75',
        marginBottom: '1.5rem'
      }}>
        Depending on your network connection type, use the following server addresses:
      </p>
      
      <div style={cardStyle}>
        <h3 style={{
          fontSize: '1.125rem',
          fontWeight: '600',
          color: 'var(--ifm-heading-color)',
          marginBottom: '0.75rem'
        }}>
          For VPN Customers (Private DNS)
        </h3>
        <ul style={{
          margin: 0,
          paddingLeft: '1.5rem',
          color: 'var(--ifm-color-content)'
        }}>
          <li><code style={{
            background: 'var(--ifm-color-emphasis-100)',
            padding: '0.125rem 0.375rem',
            borderRadius: '0.25rem',
            fontSize: '0.875rem'
          }}>time1.nxgen.cloud</code></li>
          <li><code style={{
            background: 'var(--ifm-color-emphasis-100)',
            padding: '0.125rem 0.375rem',
            borderRadius: '0.25rem',
            fontSize: '0.875rem'
          }}>time2.nxgen.cloud</code></li>
        </ul>
      </div>
      
      <div style={cardStyle}>
        <h3 style={{
          fontSize: '1.125rem',
          fontWeight: '600',
          color: 'var(--ifm-heading-color)',
          marginBottom: '0.75rem'
        }}>
          For Non-VPN/Public Connections
        </h3>
        <ul style={{
          margin: 0,
          paddingLeft: '1.5rem',
          color: 'var(--ifm-color-content)'
        }}>
          <li><code style={{
            background: 'var(--ifm-color-emphasis-100)',
            padding: '0.125rem 0.375rem',
            borderRadius: '0.25rem',
            fontSize: '0.875rem'
          }}>timeext1.nxgen.cloud</code></li>
        </ul>
      </div>
    </section>
  );
};

export const DeviceConfiguration = () => {
  const sectionStyle = {
    padding: '2rem 0',
    borderTop: '1px solid var(--ifm-color-emphasis-200)'
  };
  
  const headingStyle = {
    fontSize: '1.5rem',
    fontWeight: '700',
    color: 'var(--ifm-heading-color)',
    marginBottom: '1.5rem',
    lineHeight: '1.3'
  };
  
  const cardStyle = {
    background: 'var(--ifm-background-surface-color)',
    border: '1px solid var(--ifm-color-emphasis-200)',
    borderRadius: '0.75rem',
    padding: '1.5rem',
    boxShadow: '0 1px 3px 0 rgba(0, 0, 0, 0.05)',
    marginBottom: '1.5rem',
    transition: 'all 0.2s ease'
  };
  
  const stepStyle = {
    display: 'flex',
    alignItems: 'flex-start',
    marginBottom: '1rem',
    padding: '0.75rem',
    background: 'var(--ifm-color-emphasis-100)',
    borderRadius: '0.5rem'
  };
  
  const stepNumberStyle = {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    width: '2rem',
    height: '2rem',
    borderRadius: '50%',
    background: 'var(--ifm-color-primary)',
    color: 'white',
    fontWeight: '700',
    fontSize: '0.875rem',
    marginRight: '1rem',
    flexShrink: 0
  };

  return (
    <section style={sectionStyle} aria-labelledby="device-config-heading">
      <h2 id="device-config-heading" style={headingStyle}>
        Device-Specific Configuration Steps
      </h2>
      
      <div style={cardStyle}>
        <h3 style={{
          fontSize: '1.25rem',
          fontWeight: '600',
          color: 'var(--ifm-heading-color)',
          marginBottom: '1rem'
        }}>
          1. Teltonika Routers
        </h3>
        <p style={{
          fontSize: '0.875rem',
          color: 'var(--ifm-color-content-secondary)',
          marginBottom: '1rem'
        }}>
          By default, these routers may block the DNS resolution required for NTP. Follow these steps to enable it:
        </p>
        
        <div style={stepStyle}>
          <div style={stepNumberStyle}>1</div>
          <div>
            <strong>Access the Web Interface:</strong> Log in to the router via its IP address.
          </div>
        </div>
        
        <div style={stepStyle}>
          <div style={stepNumberStyle}>2</div>
          <div>
            <strong>Adjust DNS Settings:</strong> Navigate to <strong>Network &gt; DNS</strong> and turn off <strong>"Rebind protection"</strong> to allow the router to resolve the NTP server address.
          </div>
        </div>
        
        <div style={stepStyle}>
          <div style={stepNumberStyle}>3</div>
          <div>
            <strong>Configure NTP:</strong> Go to <strong>Services &gt; NTP</strong>. Under the NTP server section, enter <code>time1.nxgen.cloud</code> and <code>time2.nxgen.cloud</code>.
          </div>
        </div>
        
        <div style={stepStyle}>
          <div style={stepNumberStyle}>4</div>
          <div>
            <strong>Set Interval:</strong> Specify a synchronization interval of <strong>3600 seconds (60 minutes)</strong>.
          </div>
        </div>
      </div>
      
      <div style={cardStyle}>
        <h3 style={{
          fontSize: '1.25rem',
          fontWeight: '600',
          color: 'var(--ifm-heading-color)',
          marginBottom: '1rem'
        }}>
          2. Hikvision NVR/IP Cameras
        </h3>
        
        <div style={stepStyle}>
          <div style={stepNumberStyle}>1</div>
          <div>
            <strong>Access the Interface:</strong> Open the device's web interface using its IP address.
          </div>
        </div>
        
        <div style={stepStyle}>
          <div style={stepNumberStyle}>2</div>
          <div>
            <strong>Navigate to Time Settings:</strong> Go to <strong>System Settings &gt; Time Settings</strong>.
          </div>
        </div>
        
        <div style={stepStyle}>
          <div style={stepNumberStyle}>3</div>
          <div>
            <strong>Configure NTP:</strong> Select the NTP tab, enable the service, and enter the server address: <code>time1.nxgen.cloud</code>.
          </div>
        </div>
        
        <div style={stepStyle}>
          <div style={stepNumberStyle}>4</div>
          <div>
            <strong>Set Interval:</strong> Set the synchronization interval to <strong>3600 seconds</strong>.
          </div>
        </div>
      </div>
      
      <div style={cardStyle}>
        <h3 style={{
          fontSize: '1.25rem',
          fontWeight: '600',
          color: 'var(--ifm-heading-color)',
          marginBottom: '1rem'
        }}>
          3. Dahua Devices
        </h3>
        
        <div style={stepStyle}>
          <div style={stepNumberStyle}>1</div>
          <div>
            <strong>Access the Interface:</strong> Launch the web browser and enter the device's IP address.
          </div>
        </div>
        
        <div style={stepStyle}>
          <div style={stepNumberStyle}>2</div>
          <div>
            <strong>Navigate to Date & Time:</strong> Look for <strong>Configuration</strong> or <strong>System</strong> settings and locate the <strong>"Date&Time"</strong> section.
          </div>
        </div>
        
        <div style={stepStyle}>
          <div style={stepNumberStyle}>3</div>
          <div>
            <strong>Configure NTP:</strong> Enable NTP and enter the server address: <code>time1.nxgen.cloud</code>.
          </div>
        </div>
        
        <div style={stepStyle}>
          <div style={stepNumberStyle}>4</div>
          <div>
            <strong>Set Interval:</strong> Use an interval of <strong>3600 seconds</strong> for hourly synchronization.
          </div>
        </div>
      </div>
    </section>
  );
};

export const BestPractices = () => {
  const sectionStyle = {
    padding: '2rem 0',
    borderTop: '1px solid var(--ifm-color-emphasis-200)'
  };
  
  const headingStyle = {
    fontSize: '1.5rem',
    fontWeight: '700',
    color: 'var(--ifm-heading-color)',
    marginBottom: '1.5rem',
    lineHeight: '1.3'
  };
  
  const cardStyle = {
    background: 'var(--ifm-background-surface-color)',
    border: '1px solid var(--ifm-color-emphasis-200)',
    borderRadius: '0.75rem',
    padding: '1.5rem',
    boxShadow: '0 1px 3px 0 rgba(0, 0, 0, 0.05)',
    marginBottom: '1rem'
  };
  
  const iconStyle = {
    width: '24px',
    height: '24px',
    marginRight: '0.75rem',
    flexShrink: 0
  };

  return (
    <section style={sectionStyle} aria-labelledby="best-practices-heading">
      <h2 id="best-practices-heading" style={headingStyle}>
        Critical Best Practices
      </h2>
      
      <div style={cardStyle}>
        <div style={{ display: 'flex', alignItems: 'flex-start', marginBottom: '1rem' }}>
          <span style={iconStyle}>⏰</span>
          <div>
            <h3 style={{
              fontSize: '1.125rem',
              fontWeight: '600',
              color: 'var(--ifm-heading-color)',
              marginBottom: '0.5rem'
            }}>
              Enable DST (Daylight Saving Time)
            </h3>
            <p style={{
              fontSize: '0.875rem',
              color: 'var(--ifm-color-content)',
              margin: 0,
              lineHeight: '1.6'
            }}>
              Always ensure that <strong>Daylight Saving Time (DST)</strong> is enabled on the device. Without this, your system will be one hour out of sync for half the year, leading to incorrect alarm timestamps.
            </p>
          </div>
        </div>
      </div>
      
      <div style={cardStyle}>
        <div style={{ display: 'flex', alignItems: 'flex-start', marginBottom: '1rem' }}>
          <span style={iconStyle}>🔥</span>
          <div>
            <h3 style={{
              fontSize: '1.125rem',
              fontWeight: '600',
              color: 'var(--ifm-heading-color)',
              marginBottom: '0.5rem'
            }}>
              Firewall Whitelisting
            </h3>
            <p style={{
              fontSize: '0.875rem',
              color: 'var(--ifm-color-content)',
              margin: 0,
              lineHeight: '1.6'
            }}>
              Ensure your network firewall allows traffic to and from the <strong>GCXONE</strong> NTP servers. Ports <strong>123 UDP</strong> is standard for NTP, though specific cloud integrations may use <strong>HTTPS/443</strong> for time sync.
            </p>
          </div>
        </div>
      </div>
      
      <div style={cardStyle}>
        <div style={{ display: 'flex', alignItems: 'flex-start' }}>
          <span style={iconStyle}>⏸️</span>
          <div>
            <h3 style={{
              fontSize: '1.125rem',
              fontWeight: '600',
              color: 'var(--ifm-heading-color)',
              marginBottom: '0.5rem'
            }}>
              Stop Recording Before Manual Changes
            </h3>
            <p style={{
              fontSize: '0.875rem',
              color: 'var(--ifm-color-content)',
              margin: 0,
              lineHeight: '1.6'
            }}>
              If you must modify the time manually, <strong>stop all recording operations first</strong> to prevent database corruption or errors in the video timeline.
            </p>
          </div>
        </div>
      </div>
    </section>
  );
};

export const Analogy = () => {
  const sectionStyle = {
    padding: '2rem 0',
    borderTop: '1px solid var(--ifm-color-emphasis-200)'
  };
  
  const cardStyle = {
    background: 'var(--ifm-color-primary-lightest)',
    border: '1px solid var(--ifm-color-primary)',
    borderRadius: '0.75rem',
    padding: '1.5rem',
    marginTop: '1rem'
  };

  return (
    <section style={sectionStyle} aria-labelledby="analogy-heading">
      <h2 id="analogy-heading" style={{
        fontSize: '1.5rem',
        fontWeight: '700',
        color: 'var(--ifm-heading-color)',
        marginBottom: '1rem',
        lineHeight: '1.3'
      }}>
        Understanding NTP Configuration
      </h2>
      
      <div style={cardStyle}>
        <p style={{
          fontSize: '1rem',
          color: 'var(--ifm-color-primary-darkest)',
          lineHeight: '1.75',
          margin: 0,
          fontStyle: 'italic'
        }}>
          Think of <strong>NTP Configuration</strong> as <strong>synchronizing all the watches</strong> of a specialized team before a mission. If one person's watch is even two minutes fast, they might arrive at a location before the "all-clear" signal is given, causing confusion and compromising the entire operation. <strong>NTP</strong> ensures that every "watch" in your security network (cameras, routers, and servers) ticks in perfect unison, so when an incident is reported, you know exactly when it happened down to the second.
        </p>
      </div>
    </section>
  );
};

export const GCXONETimeSync = () => {
  const sectionStyle = {
    padding: '2rem 0',
    borderTop: '1px solid var(--ifm-color-emphasis-200)'
  };
  
  const headingStyle = {
    fontSize: '1.5rem',
    fontWeight: '700',
    color: 'var(--ifm-heading-color)',
    marginBottom: '1.5rem',
    lineHeight: '1.3'
  };
  
  const cardStyle = {
    background: 'var(--ifm-background-surface-color)',
    border: '1px solid var(--ifm-color-emphasis-200)',
    borderRadius: '0.75rem',
    padding: '1.5rem',
    boxShadow: '0 1px 3px 0 rgba(0, 0, 0, 0.05)',
    marginBottom: '1rem'
  };
  
  const stepStyle = {
    display: 'flex',
    alignItems: 'flex-start',
    marginBottom: '1rem',
    padding: '0.75rem',
    background: 'var(--ifm-color-emphasis-100)',
    borderRadius: '0.5rem'
  };
  
  const stepNumberStyle = {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    width: '2rem',
    height: '2rem',
    borderRadius: '50%',
    background: 'var(--ifm-color-primary)',
    color: 'white',
    fontWeight: '700',
    fontSize: '0.875rem',
    marginRight: '1rem',
    flexShrink: 0
  };

  return (
    <section style={sectionStyle} aria-labelledby="gcxone-timesync-heading">
      <h2 id="gcxone-timesync-heading" style={headingStyle}>
        Configuring TimeSync in GCXONE
      </h2>
      
      <p style={{
        fontSize: '1rem',
        color: 'var(--ifm-color-content)',
        lineHeight: '1.75',
        marginBottom: '1.5rem'
      }}>
        Follow these steps to configure TimeSync for your devices in the GCXONE platform:
      </p>
      
      <div style={cardStyle}>
        <div style={stepStyle}>
          <div style={stepNumberStyle}>1</div>
          <div>
            <strong>Start from the Marketplace:</strong> Navigate to the Marketplace to activate TimeSync.
          </div>
        </div>
        
        <div style={stepStyle}>
          <div style={stepNumberStyle}>2</div>
          <div>
            <strong>Access Configuration:</strong> Move to the configuration section.
          </div>
        </div>
        
        <div style={stepStyle}>
          <div style={stepNumberStyle}>3</div>
          <div>
            <strong>Navigate to Devices:</strong> Go to the <strong>Devices</strong> tab.
          </div>
        </div>
        
        <div style={stepStyle}>
          <div style={stepNumberStyle}>4</div>
          <div>
            <strong>Edit Device:</strong> Click <strong>More</strong> to edit the device settings.
          </div>
        </div>
        
        <div style={stepStyle}>
          <div style={stepNumberStyle}>5</div>
          <div>
            <strong>Time Zone Sync:</strong> Tap <strong>Time Zone Sync</strong> to align your device's clock with the correct location.
          </div>
        </div>
        
        <div style={stepStyle}>
          <div style={stepNumberStyle}>6</div>
          <div>
            <strong>NTP Configuration:</strong> Access NTP settings to get the server information and complete the setup.
          </div>
        </div>
        
        <div style={stepStyle}>
          <div style={stepNumberStyle}>7</div>
          <div>
            <strong>Daylight Saving Time:</strong> Toggle <strong>Daylight Saving Time</strong> to automatically adjust for seasonal changes.
          </div>
        </div>
      </div>
    </section>
  );
};

<DocHeader />
<NTPServerAddresses />
<GCXONETimeSync />
<DeviceConfiguration />
<BestPractices />
<Analogy />

