---
title: "What is Evalink Talos?"
description: "Learn about Evalink Talos, the cloud-based alarm management platform that partners with GCXONE for comprehensive security solutions"
tags:
  - role:all
  - category:overview
  - difficulty:beginner
  - platform:GCXONE
sidebar_position: 2
last_updated: 2025-12-04
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import { useState } from 'react';

export const DocHeader = () => {
  const cardStyle = {
    background: 'var(--ifm-background-surface-color)',
    border: '1px solid var(--ifm-color-emphasis-200)',
    borderRadius: '0.75rem',
    padding: '1.5rem',
    boxShadow: '0 1px 3px 0 rgba(0, 0, 0, 0.05)',
    transition: 'all 0.2s ease'
  };
  
  const badgeStyle = {
    display: 'inline-flex',
    alignItems: 'center',
    padding: '0.375rem 0.875rem',
    borderRadius: '9999px',
    fontSize: '0.75rem',
    fontWeight: '500',
    background: 'var(--ifm-color-primary-lightest)',
    color: 'var(--ifm-color-primary-darkest)'
  };

  return (
    <header style={{
      background: 'var(--ifm-background-color)',
      marginBottom: '2rem'
    }}>
      <h1 style={{
        fontSize: '2.25rem',
        fontWeight: '800',
        color: 'var(--ifm-heading-color)',
        marginBottom: '1.5rem',
        lineHeight: '1.2'
      }}>
        What is Evalink Talos?
      </h1>
      
      <div style={cardStyle}>
      <h2 style={{
        fontSize: '1.25rem',
        fontWeight: '600',
        color: 'var(--ifm-heading-color)',
        marginBottom: '1rem'
      }}>
        What is Evalink Talos?
      </h2>
      
      <div style={badgeStyle}>
        <span style={{
          display: 'inline-block',
          width: '6px',
          height: '6px',
          borderRadius: '50%',
          background: 'var(--ifm-color-primary)',
          marginRight: '0.5rem'
        }} aria-hidden="true"></span>
        Now Integrated with GCXONE
      </div>
      
      <div style={{
        fontSize: '0.875rem',
        color: 'var(--ifm-color-content)',
        lineHeight: '1.75',
        marginBottom: '1.5rem'
      }}>
        <p style={{ marginBottom: '0.75rem' }}>
          Evalink Talos is a specialized, automated, cloud-based platform designed for monitoring stations to manage security alarms efficiently.
        </p>
        <p style={{ marginBottom: '0.75rem' }}>
          Built on <strong>Amazon Web Services (AWS)</strong> infrastructure across multiple global regions, Talos provides a highly reliable, scalable solution for alarm management.
        </p>
        <p>
          It serves as the <strong>central alarm management hub</strong> in the security ecosystem, working seamlessly with NXGEN GCXONE. While GCXONE focuses on video processing and AI, Talos specializes in workflows and dispatching.
        </p>
      </div>
      
      <div style={{
        fontSize: '0.875rem',
        color: 'var(--ifm-color-content)',
        borderTop: '1px solid var(--ifm-color-emphasis-200)',
        paddingTop: '1rem'
      }}>
        For comprehensive Evalink Talos documentation, visit the{' '}
        <a 
          href="https://documentation.evalink.io/" 
          target="_blank" 
          rel="noopener noreferrer" 
          style={{ 
            color: 'var(--ifm-color-primary)', 
            textDecoration: 'none',
            fontWeight: '500'
          }}
          onMouseEnter={(e) => e.target.style.textDecoration = 'underline'}
          onMouseLeave={(e) => e.target.style.textDecoration = 'none'}
        >
          Documentation Site
        </a>
        {' '}and{' '}
        <a 
          href="https://documentation.evalink.io/" 
          target="_blank" 
          rel="noopener noreferrer" 
          style={{ 
            color: 'var(--ifm-color-primary)', 
            textDecoration: 'none',
            fontWeight: '500'
          }}
          onMouseEnter={(e) => e.target.style.textDecoration = 'underline'}
          onMouseLeave={(e) => e.target.style.textDecoration = 'none'}
        >
          Evalink
        </a>.
      </div>
    </div>
  </header>
  );
};

export const DocWhyTalos = () => {
  const benefits = [
    {
      title: "Unified Interface",
      desc: "Single platform for all alarm management needs.",
      icon: "🖥️"
    },
    {
      title: "Workflow Automation",
      desc: "Automated processes reduce manual workload.",
      icon: "⚡"
    },
    {
      title: "Scalable Architecture",
      desc: "Cloud-based infrastructure grows with your business.",
      icon: "☁️"
    },
    {
      title: "Integration Ready",
      desc: "Seamless integration with GCXONE.",
      icon: "🔌"
    }
  ];

  const sectionStyle = {
    padding: '2rem 0',
    borderTop: '1px solid var(--ifm-color-emphasis-200)'
  };
  
  const headingStyle = {
    fontSize: '1.5rem',
    fontWeight: '700',
    color: 'var(--ifm-heading-color)',
    marginBottom: '1.5rem',
    lineHeight: '1.3'
  };
  
  const cardStyle = {
    background: 'var(--ifm-background-surface-color)',
    border: '1px solid var(--ifm-color-emphasis-200)',
    borderRadius: '0.75rem',
    padding: '1.5rem',
    boxShadow: '0 1px 3px 0 rgba(0, 0, 0, 0.05)',
    transition: 'all 0.2s ease'
  };
  
  const cardHoverStyle = {
    boxShadow: '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)'
  };

  return (
    <section style={sectionStyle} aria-labelledby="why-talos-heading">
      <h2 id="why-talos-heading" style={headingStyle}>
        Why Evalink Talos?
      </h2>
      <div style={{
        background: 'var(--ifm-color-emphasis-100)',
        border: '1px solid var(--ifm-color-emphasis-200)',
        borderRadius: '0.75rem',
        padding: '1.5rem',
        marginBottom: '1.5rem'
      }}>
        <h3 style={{
          fontSize: '1.125rem',
          fontWeight: '600',
          color: 'var(--ifm-heading-color)',
          marginBottom: '0.5rem'
        }}>
          More Than a Solution - A Companion
        </h3>
        <p style={{
          color: 'var(--ifm-color-content-secondary)',
          marginBottom: '1.5rem',
          fontSize: '0.875rem',
          lineHeight: '1.6'
        }}>
          Talos is designed to be a comprehensive companion for security operations teams. The platform understands the unique challenges of alarm management and provides tools that adapt to your operational needs.
        </p>
        <div style={{
          display: 'grid',
          gridTemplateColumns: 'repeat(auto-fit, minmax(200px, 1fr))',
          gap: '1rem'
        }}>
          {benefits.map((b, i) => (
            <article 
              key={i} 
              style={{
                ...cardStyle,
                padding: '1rem',
                borderRadius: '0.5rem'
              }}
              onMouseEnter={(e) => {
                e.currentTarget.style.transform = 'translateY(-2px)';
                e.currentTarget.style.boxShadow = cardHoverStyle.boxShadow;
              }}
              onMouseLeave={(e) => {
                e.currentTarget.style.transform = 'translateY(0)';
                e.currentTarget.style.boxShadow = cardStyle.boxShadow;
              }}
            >
              <div style={{
                marginBottom: '0.75rem',
                padding: '0.5rem',
                background: 'var(--ifm-color-emphasis-100)',
                width: 'fit-content',
                borderRadius: '0.375rem'
              }} aria-hidden="true">
                <span style={{ fontSize: '1.25rem' }}>{b.icon}</span>
              </div>
              <h4 style={{
                fontWeight: '600',
                color: 'var(--ifm-heading-color)',
                fontSize: '0.875rem',
                marginBottom: '0.25rem'
              }}>
                {b.title}
              </h4>
              <p style={{
                fontSize: '0.75rem',
                color: 'var(--ifm-color-content-secondary)',
                margin: 0,
                lineHeight: '1.5'
              }}>
                {b.desc}
              </p>
            </article>
          ))}
        </div>
      </div>
    </section>
  );
};

export const DocCoreFeatures = () => {
  const [activeTab, setActiveTab] = useState('alarms');

  const sectionStyle = {
    padding: '2rem 0',
    borderTop: '1px solid var(--ifm-color-emphasis-200)'
  };
  
  const headingStyle = {
    fontSize: '1.5rem',
    fontWeight: '700',
    color: 'var(--ifm-heading-color)',
    marginBottom: '1.5rem',
    lineHeight: '1.3'
  };
  
  const cardStyle = {
    background: 'var(--ifm-background-surface-color)',
    border: '1px solid var(--ifm-color-emphasis-200)',
    borderRadius: '0.75rem',
    padding: '1.5rem',
    boxShadow: '0 1px 3px 0 rgba(0, 0, 0, 0.05)',
    transition: 'all 0.2s ease'
  };

  const tabs = [
    { id: 'alarms', label: 'Alarm Management', icon: '🚨' },
    { id: 'workflow', label: 'Workflow Engine', icon: '⚡' },
    { id: 'operator', label: 'Operator Interface', icon: '👥' }
  ];

  const handleKeyDown = (e, tabId) => {
    if (e.key === 'Enter' || e.key === ' ') {
      e.preventDefault();
      setActiveTab(tabId);
    }
  };

  return (
    <section style={sectionStyle} aria-labelledby="core-features-heading">
      <h2 id="core-features-heading" style={headingStyle}>
        Core Features
      </h2>
      
      <div 
        role="tablist" 
        aria-label="Core features tabs"
        style={{
          display: 'flex',
          gap: '0.25rem',
          background: 'var(--ifm-color-emphasis-100)',
          padding: '0.25rem',
          borderRadius: '0.5rem',
          width: 'fit-content',
          marginBottom: '1.5rem',
          overflowX: 'auto'
        }}
      >
        {tabs.map(tab => (
          <button
            key={tab.id}
            role="tab"
            aria-selected={activeTab === tab.id}
            aria-controls={`panel-${tab.id}`}
            id={`tab-${tab.id}`}
            onClick={() => setActiveTab(tab.id)}
            onKeyDown={(e) => handleKeyDown(e, tab.id)}
            style={{
              padding: '0.5rem 1rem',
              fontSize: '0.875rem',
              fontWeight: '500',
              borderRadius: '0.375rem',
              transition: 'all 0.2s',
              border: 'none',
              cursor: 'pointer',
              background: activeTab === tab.id ? 'var(--ifm-background-surface-color)' : 'transparent',
              color: activeTab === tab.id ? 'var(--ifm-color-primary)' : 'var(--ifm-color-content-secondary)',
              boxShadow: activeTab === tab.id ? '0 1px 2px 0 rgba(0, 0, 0, 0.05)' : 'none',
              whiteSpace: 'nowrap',
              display: 'flex',
              alignItems: 'center',
              gap: '0.5rem'
            }}
          >
            <span aria-hidden="true">{tab.icon}</span>
            {tab.label}
          </button>
        ))}
      </div>

      <div style={cardStyle}>
        {activeTab === 'alarms' && (
          <div 
            role="tabpanel" 
            id="panel-alarms" 
            aria-labelledby="tab-alarms"
          >
            <h3 style={{
              fontSize: '1.125rem',
              fontWeight: '700',
              color: 'var(--ifm-heading-color)',
              marginBottom: '1rem',
              display: 'flex',
              alignItems: 'center',
              gap: '0.5rem'
            }}>
              <span aria-hidden="true">🚨</span>
              Alarm Receiving and Management
            </h3>
            <p style={{
              color: 'var(--ifm-color-content-secondary)',
              marginBottom: '1rem',
              fontSize: '0.875rem',
              lineHeight: '1.6'
            }}>
              Talos serves as the central hub for receiving, processing, and managing security alarms. An alarm is defined as a single notification (e.g., motion detected, door sensor) requiring operator attention.
            </p>
            <ul style={{
              display: 'grid',
              gridTemplateColumns: 'repeat(auto-fit, minmax(250px, 1fr))',
              gap: '0.75rem',
              listStyle: 'none',
              padding: 0,
              margin: 0
            }}>
              {[
                { label: 'Multi-Source', desc: 'Receives alarms from various devices.' },
                { label: 'Priority Mgmt', desc: 'Sorted by priority or time.' },
                { label: 'Real-Time', desc: 'Immediate reception and routing.' },
                { label: 'Enriched Data', desc: 'Follow-up alarms with GCXONE AI.' }
              ].map((item, i) => (
                <li key={i} style={{
                  display: 'flex',
                  alignItems: 'start',
                  fontSize: '0.875rem',
                  color: 'var(--ifm-color-content)',
                  gap: '0.5rem'
                }}>
                  <span 
                    style={{ 
                      color: 'var(--ifm-color-primary)', 
                      marginTop: '0.125rem',
                      fontWeight: 'bold'
                    }} 
                    aria-hidden="true"
                  >
                    ✓
                  </span>
                  <span>
                    <strong>{item.label}:</strong> {item.desc}
                  </span>
                </li>
              ))}
            </ul>
          </div>
        )}

        {activeTab === 'workflow' && (
          <div 
            role="tabpanel" 
            id="panel-workflow" 
            aria-labelledby="tab-workflow"
          >
            <h3 style={{
              fontSize: '1.125rem',
              fontWeight: '700',
              color: 'var(--ifm-heading-color)',
              marginBottom: '1rem',
              display: 'flex',
              alignItems: 'center',
              gap: '0.5rem'
            }}>
              <span aria-hidden="true">⚡</span>
              Workflow Engine
            </h3>
            <p style={{
              color: 'var(--ifm-color-content-secondary)',
              marginBottom: '1rem',
              fontSize: '0.875rem',
              lineHeight: '1.6'
            }}>
              A powerful engine that automates standard security procedures.
            </p>
            <div style={{
              background: 'var(--ifm-color-emphasis-100)',
              padding: '1rem',
              borderRadius: '0.5rem',
              border: '1px solid var(--ifm-color-emphasis-200)',
              marginBottom: '1rem'
            }}>
              <div style={{
                fontSize: '0.75rem',
                fontWeight: '600',
                color: 'var(--ifm-color-content-secondary)',
                textTransform: 'uppercase',
                letterSpacing: '0.05em',
                marginBottom: '0.5rem'
              }}>
                Example Workflow
              </div>
              <div style={{
                display: 'flex',
                alignItems: 'center',
                fontSize: '0.875rem',
                color: 'var(--ifm-color-content)',
                gap: '0.5rem',
                overflowX: 'auto',
                paddingBottom: '0.5rem',
                flexWrap: 'wrap'
              }} role="list" aria-label="Workflow steps">
                {[
                  { label: 'Device Alarm', highlight: false },
                  { label: 'Forward to GCXONE', highlight: false },
                  { label: 'AI Analysis', highlight: true },
                  { label: 'Verified Return', highlight: false }
                ].map((step, i) => (
                  <div key={i} style={{ display: 'flex', alignItems: 'center', gap: '0.5rem' }}>
                    <span style={{
                      background: step.highlight ? 'var(--ifm-color-primary-lightest)' : 'var(--ifm-background-surface-color)',
                      padding: '0.25rem 0.5rem',
                      borderRadius: '0.25rem',
                      border: `1px solid ${step.highlight ? 'var(--ifm-color-primary)' : 'var(--ifm-color-emphasis-300)'}`,
                      color: step.highlight ? 'var(--ifm-color-primary-darkest)' : 'var(--ifm-color-content)',
                      fontWeight: step.highlight ? '600' : '400',
                      whiteSpace: 'nowrap'
                    }}>
                      {step.label}
                    </span>
                    {i < 3 && (
                      <span 
                        style={{ color: 'var(--ifm-color-content-secondary)' }}
                        aria-hidden="true"
                      >
                        →
                      </span>
                    )}
                  </div>
                ))}
              </div>
            </div>
            <ul style={{
              fontSize: '0.875rem',
              color: 'var(--ifm-color-content)',
              display: 'flex',
              flexDirection: 'column',
              gap: '0.5rem',
              listStyle: 'none',
              padding: 0,
              margin: 0
            }}>
              <li>• Automated Routing to appropriate operators</li>
              <li>• Automatic status updates and state management</li>
              <li>• Customizable rules to match operational needs</li>
            </ul>
          </div>
        )}

        {activeTab === 'operator' && (
          <div 
            role="tabpanel" 
            id="panel-operator" 
            aria-labelledby="tab-operator"
          >
            <h3 style={{
              fontSize: '1.125rem',
              fontWeight: '700',
              color: 'var(--ifm-heading-color)',
              marginBottom: '1rem',
              display: 'flex',
              alignItems: 'center',
              gap: '0.5rem'
            }}>
              <span aria-hidden="true">👥</span>
              Operator Interface
            </h3>
            <p style={{
              color: 'var(--ifm-color-content-secondary)',
              marginBottom: '1rem',
              fontSize: '0.875rem',
              lineHeight: '1.6'
            }}>
              An intuitive, multi-screen interface designed for efficient management.
            </p>
            <div style={{
              display: 'grid',
              gridTemplateColumns: 'repeat(auto-fit, minmax(200px, 1fr))',
              gap: '1rem'
            }}>
              {[
                { title: '1. Alarm Receiving', desc: 'Real-time incoming list, sortable by priority, color-coded.' },
                { title: '2. Video Screen', desc: 'Live/recorded clips linked to alarms, syncs with timeline.' },
                { title: '3. Dashboard', desc: 'System status, KPIs, statistics, and quick access.' }
              ].map((item, i) => (
                <div 
                  key={i}
                  style={{
                    border: '1px solid var(--ifm-color-emphasis-200)',
                    borderRadius: '0.5rem',
                    padding: '0.75rem',
                    background: 'var(--ifm-background-surface-color)'
                  }}
                >
                  <h4 style={{
                    fontWeight: '600',
                    color: 'var(--ifm-heading-color)',
                    fontSize: '0.875rem',
                    marginBottom: '0.25rem'
                  }}>
                    {item.title}
                  </h4>
                  <p style={{
                    fontSize: '0.75rem',
                    color: 'var(--ifm-color-content-secondary)',
                    margin: 0,
                    lineHeight: '1.5'
                  }}>
                    {item.desc}
                  </p>
                </div>
              ))}
            </div>
          </div>
        )}
      </div>
    </section>
  );
};

export const DocArchitecture = () => {
  const sectionStyle = {
    padding: '2rem 0',
    borderTop: '1px solid var(--ifm-color-emphasis-200)'
  };
  
  const headingStyle = {
    fontSize: '1.5rem',
    fontWeight: '700',
    color: 'var(--ifm-heading-color)',
    marginBottom: '1.5rem',
    lineHeight: '1.3'
  };

  return (
    <section style={sectionStyle} aria-labelledby="architecture-heading">
      <h2 id="architecture-heading" style={headingStyle}>
      Platform Architecture
    </h2>
    <div style={{
      background: 'var(--ifm-color-emphasis-100)',
      borderRadius: '0.75rem',
      border: '1px solid var(--ifm-color-emphasis-200)',
      padding: '1.5rem'
    }}>
      <div style={{
        display: 'grid',
        gridTemplateColumns: 'repeat(auto-fit, minmax(300px, 1fr))',
        gap: '2rem'
      }}>
        <article>
          <h3 style={{
            fontSize: '1.125rem',
            fontWeight: '700',
            color: 'var(--ifm-heading-color)',
            marginBottom: '1rem',
            display: 'flex',
            alignItems: 'center',
            gap: '0.5rem'
          }}>
            <span aria-hidden="true">🌍</span>
            AWS Infrastructure
          </h3>
          <p style={{
            fontSize: '0.875rem',
            color: 'var(--ifm-color-content-secondary)',
            marginBottom: '1rem',
            lineHeight: '1.6'
          }}>
            Talos leverages enterprise-grade Amazon Web Services infrastructure for maximum reliability.
          </p>
          <ul style={{
            display: 'flex',
            flexDirection: 'column',
            gap: '0.5rem',
            fontSize: '0.875rem',
            color: 'var(--ifm-color-content)',
            listStyle: 'none',
            padding: 0,
            margin: 0
          }}>
            {[
              'Global Presence (Americas, Europe, Asia)',
              'Multiple Availability Zones',
              'Auto-Scaling Resources',
              'No Infrastructure Management needed'
            ].map((item, i) => (
              <li key={i} style={{ display: 'flex', alignItems: 'center', gap: '0.5rem' }}>
                <div style={{
                  width: '6px',
                  height: '6px',
                  background: 'var(--ifm-color-primary)',
                  borderRadius: '50%',
                  flexShrink: 0
                }} aria-hidden="true"></div>
                <span>{item}</span>
              </li>
            ))}
          </ul>
        </article>
        <article>
          <h3 style={{
            fontSize: '1.125rem',
            fontWeight: '700',
            color: 'var(--ifm-heading-color)',
            marginBottom: '1rem',
            display: 'flex',
            alignItems: 'center',
            gap: '0.5rem'
          }}>
            <span aria-hidden="true">⚙️</span>
            Microservices
          </h3>
          <p style={{
            fontSize: '0.875rem',
            color: 'var(--ifm-color-content-secondary)',
            marginBottom: '1rem',
            lineHeight: '1.6'
          }}>
            The platform is composed of many small, independent services.
          </p>
          <div style={{
            display: 'grid',
            gridTemplateColumns: 'repeat(2, 1fr)',
            gap: '0.75rem',
            fontSize: '0.75rem'
          }}>
            {[
              { label: 'Resilience', desc: 'If one service fails, others continue.' },
              { label: 'Scalability', desc: 'Services scale independently.' },
              { label: 'Flexibility', desc: 'Add features without breakage.' },
              { label: 'Maintenance', desc: 'Easy component updates.' }
            ].map((item, i) => (
              <div 
                key={i}
                style={{
                  background: 'var(--ifm-background-surface-color)',
                  padding: '0.5rem',
                  borderRadius: '0.25rem',
                  border: '1px solid var(--ifm-color-emphasis-200)'
                }}
              >
                <strong>{item.label}:</strong> {item.desc}
              </div>
            ))}
          </div>
        </article>
      </div>
    </div>
  </section>
  );
};

export const DocUseCases = () => {
  const sectionStyle = {
    padding: '2rem 0',
    borderTop: '1px solid var(--ifm-color-emphasis-200)'
  };
  
  const headingStyle = {
    fontSize: '1.5rem',
    fontWeight: '700',
    color: 'var(--ifm-heading-color)',
    marginBottom: '1.5rem',
    lineHeight: '1.3'
  };
  
  const cardStyle = {
    background: 'var(--ifm-background-surface-color)',
    border: '1px solid var(--ifm-color-emphasis-200)',
    borderRadius: '0.75rem',
    padding: '1.5rem',
    boxShadow: '0 1px 3px 0 rgba(0, 0, 0, 0.05)',
    transition: 'all 0.2s ease'
  };
  
  const cardHoverStyle = {
    boxShadow: '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)'
  };
  
  const iconContainerStyle = {
    width: '40px',
    height: '40px',
    background: 'var(--ifm-color-primary-lightest)',
    borderRadius: '0.5rem',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    marginBottom: '1rem'
  };

  const useCases = [
    {
      icon: '🖥️',
      title: 'Central Monitoring Stations',
      features: ['Multi-Tenant Support', 'Scalable Operations', 'Efficient Workflows']
    },
    {
      icon: '💼',
      title: 'Security Service Providers',
      features: ['Service Differentiation', 'Operational Efficiency', 'Integration Ready']
    },
    {
      icon: '👥',
      title: 'Enterprise Security',
      features: ['Centralized Management', 'Custom Workflows', 'Compliance Support']
    }
  ];

  return (
    <section style={sectionStyle} aria-labelledby="use-cases-heading">
      <h2 id="use-cases-heading" style={headingStyle}>
        Use Cases
      </h2>
      <div style={{
        display: 'grid',
        gridTemplateColumns: 'repeat(auto-fit, minmax(250px, 1fr))',
        gap: '1.5rem'
      }}>
        {useCases.map((useCase, i) => (
          <article
            key={i}
            style={{
              ...cardStyle,
              padding: '1.25rem'
            }}
            onMouseEnter={(e) => {
              e.currentTarget.style.transform = 'translateY(-2px)';
              e.currentTarget.style.boxShadow = cardHoverStyle.boxShadow;
            }}
            onMouseLeave={(e) => {
              e.currentTarget.style.transform = 'translateY(0)';
              e.currentTarget.style.boxShadow = cardStyle.boxShadow;
            }}
          >
            <div style={iconContainerStyle} aria-hidden="true">
              <span style={{ fontSize: '1.5rem' }}>{useCase.icon}</span>
            </div>
            <h3 style={{
              fontWeight: '700',
              color: 'var(--ifm-heading-color)',
              marginBottom: '0.5rem',
              fontSize: '1rem'
            }}>
              {useCase.title}
            </h3>
            <ul style={{
              fontSize: '0.875rem',
              color: 'var(--ifm-color-content-secondary)',
              display: 'flex',
              flexDirection: 'column',
              gap: '0.25rem',
              listStyle: 'none',
              padding: 0,
              margin: 0
            }}>
              {useCase.features.map((feature, j) => (
                <li key={j} style={{ display: 'flex', alignItems: 'center', gap: '0.5rem' }}>
                  <span 
                    style={{ 
                      color: 'var(--ifm-color-primary)', 
                      fontSize: '0.75rem'
                    }}
                    aria-hidden="true"
                  >
                    •
                  </span>
                  {feature}
                </li>
              ))}
            </ul>
          </article>
        ))}
      </div>
    </section>
  );
};

export const DocIntegration = () => {
  const sectionStyle = {
    padding: '2rem 0',
    borderTop: '1px solid var(--ifm-color-emphasis-200)'
  };

  return (
    <section style={sectionStyle} aria-labelledby="integration-heading">
    <div style={{
      background: 'var(--ifm-color-emphasis-100)',
      borderRadius: '0.75rem',
      overflow: 'hidden',
      boxShadow: '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)',
      border: '1px solid var(--ifm-color-emphasis-200)'
    }}>
      <div style={{
        display: 'grid',
        gridTemplateColumns: 'repeat(auto-fit, minmax(300px, 1fr))'
      }}>
        <div style={{ padding: '2rem' }}>
          <div style={{
            textTransform: 'uppercase',
            letterSpacing: '0.05em',
            fontSize: '0.75rem',
            fontWeight: '700',
            color: 'var(--ifm-color-primary-light)',
            marginBottom: '0.5rem'
          }}>
            Strategic Partnership
          </div>
          <h2 
            id="integration-heading"
            style={{
              fontSize: '1.25rem',
              fontWeight: '700',
              color: 'var(--ifm-heading-color)',
              marginBottom: '1rem'
            }}
          >
            GCXONE & Talos Integration
          </h2>
          <p style={{
            color: 'var(--ifm-color-content-secondary)',
            fontSize: '0.875rem',
            marginBottom: '1.5rem',
            lineHeight: '1.6'
          }}>
            Combines GCXONE's advanced AI analytics with Talos' specialized alarm management. GCXONE handles video/AI, Talos handles dispatching.
          </p>
          
          <div style={{
            display: 'flex',
            flexDirection: 'column',
            gap: '1rem'
          }}>
            {[
              { 
                value: '80%', 
                title: 'False Alarm Reduction', 
                desc: 'AI pre-verifies alarms before they reach operators.' 
              },
              { 
                value: 'Sync', 
                title: 'Seamless Integration', 
                desc: 'Automatic site synchronization & real-time MQTT exchange.' 
              }
            ].map((item, i) => (
              <div key={i} style={{ display: 'flex', alignItems: 'start', gap: '0.75rem' }}>
                <div style={{
                  flexShrink: 0,
                  height: '24px',
                  width: '24px',
                  borderRadius: '50%',
                  background: 'var(--ifm-color-primary-lightest)',
                  border: '1px solid var(--ifm-color-primary)',
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                  color: 'var(--ifm-color-primary-darkest)',
                  fontWeight: '700',
                  fontSize: '0.75rem'
                }} aria-label={item.value}>
                  {item.value}
                </div>
                <div>
                  <h4 style={{
                    color: 'var(--ifm-heading-color)',
                    fontSize: '0.875rem',
                    fontWeight: '700',
                    margin: 0,
                    marginBottom: '0.25rem'
                  }}>
                    {item.title}
                  </h4>
                  <p style={{
                    color: 'var(--ifm-color-content-secondary)',
                    fontSize: '0.75rem',
                    margin: 0,
                    lineHeight: '1.5'
                  }}>
                    {item.desc}
                  </p>
                </div>
              </div>
            ))}
          </div>
        </div>
        
        <div style={{
          background: 'var(--ifm-background-surface-color)',
          padding: '2rem',
          borderTop: '1px solid var(--ifm-color-emphasis-200)',
          display: 'flex',
          flexDirection: 'column',
          justifyContent: 'center'
        }}>
          <h3 style={{
            color: 'var(--ifm-heading-color)',
            fontWeight: '700',
            fontSize: '0.875rem',
            marginBottom: '1rem'
          }}>
            Alarm Lifecycle
          </h3>
          <ol style={{
            display: 'flex',
            flexDirection: 'column',
            gap: '0.75rem',
            position: 'relative',
            listStyle: 'none',
            padding: 0,
            margin: 0,
            paddingLeft: '2rem'
          }} aria-label="Alarm lifecycle steps">
            {[
              { label: 'Device Detection', active: false },
              { label: 'Talos Processing', active: true },
              { label: 'GCXONE AI Analysis', active: true },
              { label: 'Verified Operator Alert', active: true }
            ].map((step, i) => (
              <li 
                key={i}
                style={{ 
                  position: 'relative',
                  paddingLeft: '0'
                }}
              >
                <div style={{
                  position: 'absolute',
                  left: '-1.5rem',
                  top: '6px',
                  width: '12px',
                  height: '12px',
                  borderRadius: '50%',
                  background: step.active ? 'var(--ifm-color-primary)' : 'var(--ifm-color-emphasis-300)',
                  border: `2px solid var(--ifm-background-surface-color)`,
                  zIndex: 1
                }} aria-hidden="true"></div>
                {i > 0 && (
                  <div style={{
                    position: 'absolute',
                    left: '-1.44rem',
                    top: '-0.75rem',
                    bottom: '18px',
                    width: '2px',
                    background: 'var(--ifm-color-emphasis-300)',
                    zIndex: 0
                  }} aria-hidden="true"></div>
                )}
                <div style={{
                  fontSize: '0.75rem',
                  color: step.active ? 'var(--ifm-color-content)' : 'var(--ifm-color-content-secondary)',
                  fontWeight: step.active ? '500' : '400'
                }}>
                  {step.label}
                </div>
              </li>
            ))}
          </ol>
        </div>
      </div>
    </div>
  </section>
);

export const DocGuides = () => {
  const sectionStyle = {
    padding: '2rem 0',
    borderTop: '1px solid var(--ifm-color-emphasis-200)'
  };

  return (
    <section style={sectionStyle} aria-labelledby="guides-heading">
    <div style={{
      display: 'grid',
      gridTemplateColumns: 'repeat(auto-fit, minmax(300px, 1fr))',
      gap: '2rem'
    }}>
      <article>
        <h2 
          id="guides-heading"
          style={{
            fontSize: '1.25rem',
            fontWeight: '700',
            color: 'var(--ifm-heading-color)',
            marginBottom: '1rem'
          }}
        >
          Getting Started
        </h2>
        <div style={{
          background: 'var(--ifm-color-emphasis-100)',
          borderRadius: '0.5rem',
          padding: '1.25rem',
          border: '1px solid var(--ifm-color-emphasis-200)',
          height: '100%'
        }}>
          <h3 style={{
            fontWeight: '600',
            color: 'var(--ifm-heading-color)',
            marginBottom: '0.75rem',
            fontSize: '0.875rem'
          }}>
            Prerequisites
          </h3>
          <ul style={{
            fontSize: '0.75rem',
            color: 'var(--ifm-color-content-secondary)',
            marginBottom: '1rem',
            display: 'flex',
            flexDirection: 'column',
            gap: '0.25rem',
            listStyle: 'none',
            padding: 0
          }}>
            {[
              'Active GCXONE Account',
              'Network Configuration (Firewall)',
              'API Credentials',
              'Device Configuration'
            ].map((item, i) => (
              <li key={i} style={{ display: 'flex', alignItems: 'center', gap: '0.5rem' }}>
                <span 
                  style={{ color: 'var(--ifm-color-primary)', fontSize: '0.75rem' }}
                  aria-hidden="true"
                >
                  •
                </span>
                {item}
              </li>
            ))}
          </ul>
          <h3 style={{
            fontWeight: '600',
            color: 'var(--ifm-heading-color)',
            marginBottom: '0.75rem',
            fontSize: '0.875rem'
          }}>
            Integration Setup
          </h3>
          <ol style={{
            fontSize: '0.75rem',
            color: 'var(--ifm-color-content-secondary)',
            display: 'flex',
            flexDirection: 'column',
            gap: '0.5rem',
            listStyle: 'decimal',
            paddingLeft: '1.25rem',
            fontWeight: '500',
            margin: 0
          }}>
            <li>Configure Talos Integration in GCXONE Root</li>
            <li>Set Up Workflows (Forwarding & Follow-up)</li>
            <li>Verify Synchronization (Test Site)</li>
          </ol>
        </div>
      </article>

      <article>
        <h2 style={{
          fontSize: '1.25rem',
          fontWeight: '700',
          color: 'var(--ifm-heading-color)',
          marginBottom: '1rem'
        }}>
          Best Practices
        </h2>
        <div style={{
          display: 'flex',
          flexDirection: 'column',
          gap: '0.75rem'
        }}>
          {[
            { icon: '⚙️', title: 'Operational', desc: 'Regularly review and optimize workflows. Train operators on interface features.' },
            { icon: '📋', title: 'Integration', desc: 'Monitor connection status and regularly rotate API credentials.' },
            { icon: '🔒', title: 'Security', desc: 'Implement role-based access control and review audit logs.' }
          ].map((item, i) => (
            <div 
              key={i}
              style={{
                display: 'flex',
                padding: '0.75rem',
                borderRadius: '0.5rem',
                border: '1px solid var(--ifm-color-emphasis-200)',
                background: 'var(--ifm-background-surface-color)',
                gap: '0.75rem'
              }}
            >
              <span 
                style={{ fontSize: '1.25rem', flexShrink: 0 }}
                aria-hidden="true"
              >
                {item.icon}
              </span>
              <div>
                <h4 style={{
                  fontSize: '0.875rem',
                  fontWeight: '600',
                  color: 'var(--ifm-heading-color)',
                  margin: 0,
                  marginBottom: '0.25rem'
                }}>
                  {item.title}
                </h4>
                <p style={{
                  fontSize: '0.75rem',
                  color: 'var(--ifm-color-content-secondary)',
                  margin: 0,
                  lineHeight: '1.5'
                }}>
                  {item.desc}
                </p>
              </div>
            </div>
          ))}
        </div>
      </article>
    </div>
  </section>
  );
};

export const DocFooter = () => (
  <footer style={{
    marginTop: '2rem',
    paddingTop: '2rem',
    borderTop: '1px solid var(--ifm-color-emphasis-200)'
  }}>
    <div style={{
      display: 'grid',
      gridTemplateColumns: 'repeat(auto-fit, minmax(200px, 1fr))',
      gap: '1.5rem',
      marginBottom: '2rem'
    }}>
      {[
        { icon: '⏱️', title: 'Save Time', desc: 'Automated workflows reduce admin tasks significantly.' },
        { icon: '🛡️', title: 'Trust Guaranteed', desc: 'Enterprise-grade infrastructure & data encryption.' },
        { icon: '🚀', title: 'Future Ready', desc: 'API-first, cloud-native architecture that evolves.' }
      ].map((item, i) => (
        <div key={i} style={{ textAlign: 'center' }}>
          <h3 style={{
            fontWeight: '700',
            color: 'var(--ifm-heading-color)',
            marginBottom: '0.25rem',
            fontSize: '1rem'
          }}>
            <span aria-hidden="true">{item.icon}</span> {item.title}
          </h3>
          <p style={{
            fontSize: '0.75rem',
            color: 'var(--ifm-color-content-secondary)',
            marginTop: '0.25rem',
            lineHeight: '1.5'
          }}>
            {item.desc}
          </p>
        </div>
      ))}
    </div>

    <div style={{
      display: 'flex',
      flexWrap: 'wrap',
      alignItems: 'center',
      justifyContent: 'space-between',
      gap: '1rem',
      padding: '1rem',
      background: 'var(--ifm-color-emphasis-100)',
      borderRadius: '0.5rem',
      border: '1px solid var(--ifm-color-emphasis-200)'
    }}>
      <span style={{
        fontSize: '0.75rem',
        fontWeight: '700',
        color: 'var(--ifm-color-content-secondary)',
        textTransform: 'uppercase',
        letterSpacing: '0.05em'
      }}>
        Certifications
      </span>
      <div style={{
        display: 'flex',
        gap: '1.5rem',
        flexWrap: 'wrap'
      }}>
        {[
          { icon: '🔒', label: 'ISO 27001' },
          { icon: '🛡️', label: 'VdS Approved' },
          { icon: '✓', label: 'SIA Compliant' }
        ].map((cert, i) => (
          <div 
            key={i}
            style={{
              display: 'flex',
              alignItems: 'center',
              gap: '0.5rem'
            }}
          >
            <span 
              style={{ fontSize: '1rem', color: 'var(--ifm-color-content-secondary)' }}
              aria-hidden="true"
            >
              {cert.icon}
            </span>
            <span style={{
              fontSize: '0.75rem',
              fontWeight: '600',
              color: 'var(--ifm-color-content)'
            }}>
              {cert.label}
            </span>
          </div>
        ))}
      </div>
    </div>
  </footer>
);

<DocHeader />
<DocWhyTalos />
<DocCoreFeatures />
<DocArchitecture />
<DocUseCases />
<DocIntegration />
<DocGuides />
<DocFooter />

---

## Need Help?

If you have questions about Evalink Talos or need assistance with integration, check our [Troubleshooting Guide](/docs/troubleshooting) or [contact support](/docs/support).

For comprehensive Evalink Talos documentation, visit the **[Evalink Documentation Site](https://documentation.evalink.io/)** which includes:

- **Role-based guides** for Company Admin, Manager, Operator, and Operator Minimal
- **API documentation** for integration
- **Device documentation** for evalink devices
- **Tutorials and help center** resources

---

## Related Articles

- [GCXONE & Talos Interaction](/docs/getting-started/gcxone-talos-interaction)
- [What is NXGEN GCXONE?](/docs/getting-started/what-is-nxgen-gcxone)
- [Key Benefits & Value Propositions](/docs/getting-started/key-benefits)
- [Cloud Architecture Overview](/docs/getting-started/cloud-architecture)
