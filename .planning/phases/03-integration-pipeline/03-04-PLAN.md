---
phase: 03-integration-pipeline
plan: 04
type: execute
wave: 3
depends_on:
  - 03-02
files_modified: []
autonomous: false
requirements:
  - INTG-03

must_haves:
  truths:
    - "POST /functions/page-feedback on the live site returns HTTP 200 with {success:true} when given a valid payload"
    - "ZEPTO_API_KEY is set in Cloudflare Pages production environment variables"
    - "No classic/api/page-feedback.ts (Vercel format) or netlify/ directory exists in the repo"
    - "An email is delivered to abed.badarnah@nxgen.io after the smoke-test POST"
  artifacts:
    - path: "classic/functions/page-feedback.ts"
      provides: "Cloudflare Pages Function using fetch() to ZeptoMail — ALREADY COMPLETE from Phase 1"
      contains: "ZeptoMail"
    - path: "Cloudflare Pages env vars (production)"
      provides: "ZEPTO_API_KEY set in production environment"
      contains: "ZEPTO_API_KEY"
  key_links:
    - from: "Feedback widget (browser)"
      to: "classic/functions/page-feedback.ts"
      via: "POST /functions/page-feedback (Cloudflare Pages Functions routing)"
      pattern: "fetch.*page-feedback"
    - from: "classic/functions/page-feedback.ts"
      to: "ZeptoMail HTTP API"
      via: "fetch() with ZEPTO_API_KEY — no nodemailer, no SMTP"
      pattern: "ZeptoMail"
---

<objective>
Verify that the already-complete feedback widget works end-to-end on the live Cloudflare Pages deployment — ZEPTO_API_KEY is set, the function endpoint returns 200, and emails are delivered.

Purpose: INTG-03 was coded in Phase 1 (classic/functions/page-feedback.ts exists). Phase 3 closes the loop by confirming the live environment is correctly configured and the function works. No new code is written here.

Output: Confirmed working feedback pipeline — browser → CF Pages Function → ZeptoMail → email inbox — with human sign-off that the smoke test passed.
</objective>

<execution_context>
@C:/Users/abdel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/abdel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-integration-pipeline/03-CONTEXT.md
@.planning/phases/03-integration-pipeline/03-02-SUMMARY.md

<interfaces>
<!-- No code changes in this plan — verification only. -->

Existing implementation (from Phase 1):
  classic/functions/page-feedback.ts — Cloudflare Pages Function
  Uses: fetch() to ZeptoMail HTTP API (not nodemailer/SMTP)
  Reads env var: ZEPTO_API_KEY (must be set in CF Pages production env)
  Deployed at: https://docs.nxgen.cloud/functions/page-feedback

Smoke test payload (from RESEARCH.md):
  curl -X POST https://docs.nxgen.cloud/functions/page-feedback
    -H "Content-Type: application/json"
    -d '{"type":"page_feedback","rating":"helpful","context":{"pageTitle":"INTG-03 smoke test","pageUrl":"https://docs.nxgen.cloud/docs/test"}}'
  Expected: {"success":true} with HTTP 200

File absence checks (Phase 1 deleted these):
  classic/api/page-feedback.ts  → must NOT exist (Vercel format — deleted Phase 1)
  netlify/                       → must NOT exist (deleted Phase 1)

STATE.md confirms Phase 1 cleanup decisions:
  "Delete classic/api/page-feedback.ts (Vercel) and netlify/functions/page-feedback.mjs:
   both platform-incompatible with Cloudflare Pages and superseded by classic/functions/page-feedback.ts"
  "ZEPTO_API_KEY must be added to Cloudflare Pages env vars"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Confirm no legacy function files remain</name>
  <files></files>
  <action>
    Run automated checks to confirm Phase 1 cleanup is complete and the live implementation is correct.

    Check 1 — Legacy file absence (Phase 1 should have deleted these):
      test ! -f /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/classic/api/page-feedback.ts
      echo "Vercel file absent: OK"
      test ! -d /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/netlify
      echo "Netlify dir absent: OK"

    Check 2 — Live function file references ZeptoMail:
      grep -qi "zepto" /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/classic/functions/page-feedback.ts
      echo "ZeptoMail reference found: OK"

    Check 3 — No nodemailer import remains:
      grep -qi "nodemailer" /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/classic/functions/page-feedback.ts
      If this grep finds a match, the function still uses nodemailer — that is a failure condition.
      Expected: grep finds nothing (exits 1 = good).

    If any check fails, investigate and resolve before proceeding to Task 2.
  </action>
  <verify>
    <automated>test ! -f /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/classic/api/page-feedback.ts &amp;&amp; test ! -d /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/netlify &amp;&amp; grep -qi "zepto" /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/classic/functions/page-feedback.ts &amp;&amp; ! grep -qi "nodemailer" /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/classic/functions/page-feedback.ts &amp;&amp; echo "all presence checks passed"</automated>
  </verify>
  <done>
    - classic/api/page-feedback.ts does not exist (Vercel format gone)
    - netlify/ directory does not exist
    - classic/functions/page-feedback.ts exists and references ZeptoMail
    - classic/functions/page-feedback.ts does not reference nodemailer
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Smoke-test live feedback endpoint and confirm email delivery</name>
  <files></files>
  <action>
    Verify the live Cloudflare Pages Function responds correctly and delivers email via ZeptoMail.
    Requires: site deployed to CF Pages with ZEPTO_API_KEY set in production env vars.
  </action>
  <verify>
    <automated>MISSING — requires live deployment and email inbox observation; automated curl is the smoke test but email delivery requires human confirmation</automated>
  </verify>
  <done>POST to /functions/page-feedback returns {"success":true} with HTTP 200 and email is delivered to abed.badarnah@nxgen.io within 60 seconds.</done>
  <what-built>
    classic/functions/page-feedback.ts was written in Phase 1. This checkpoint confirms the live
    Cloudflare Pages deployment has ZEPTO_API_KEY set and the function works end-to-end.
  </what-built>
  <how-to-verify>
    PRE-CHECK — Confirm ZEPTO_API_KEY is set in CF Pages:
    1. Cloudflare Dashboard → Workers and Pages → nxgen → Settings → Environment Variables (Production)
    2. Confirm ZEPTO_API_KEY appears in the list (value is hidden — confirm the key name exists)
    3. If missing: add it now (value from your ZeptoMail dashboard → API Tokens)

    SMOKE TEST — POST to the live endpoint:
    Run from your local terminal:

      curl -X POST https://docs.nxgen.cloud/functions/page-feedback \
        -H "Content-Type: application/json" \
        -d '{"type":"page_feedback","rating":"helpful","context":{"pageTitle":"INTG-03 smoke test","pageUrl":"https://docs.nxgen.cloud/docs/test"}}'

    Expected response: {"success":true} with HTTP 200.

    EMAIL VERIFICATION:
    Check the inbox at abed.badarnah@nxgen.io for an email from the page feedback function.
    Expected: An email arrives within ~60 seconds with the feedback content from the smoke test.

    BROWSER WIDGET TEST (optional but recommended):
    Visit https://docs.nxgen.cloud/docs/ and use the actual feedback widget on the page.
    Submit feedback — confirm it submits without error and an email arrives.

    Success criteria:
    - curl returns {"success":true} with HTTP 200
    - Email arrives at abed.badarnah@nxgen.io
    - No 5xx errors in CF Pages logs (check via CF Dashboard → nxgen → Functions → Logs)
  </how-to-verify>
  <resume-signal>Type "approved" if curl returned 200 and email was received. Describe any failures (missing env var, 500 error, email not received) if not.</resume-signal>
</task>

</tasks>

<verification>
INTG-03 is verified when:
1. `test ! -f classic/api/page-feedback.ts` passes — Vercel format gone
2. `test ! -d netlify/` passes — Netlify format gone
3. `grep -qi "zepto" classic/functions/page-feedback.ts` passes — ZeptoMail implementation confirmed
4. `! grep -qi "nodemailer" classic/functions/page-feedback.ts` passes — no nodemailer
5. Live curl to /functions/page-feedback returns {"success":true} with HTTP 200 (human-verified)
6. Email arrived at abed.badarnah@nxgen.io (human-verified)
</verification>

<success_criteria>
- No Vercel (classic/api/) or Netlify (netlify/) function files exist in the repo
- classic/functions/page-feedback.ts exists and uses fetch() to ZeptoMail (no nodemailer)
- ZEPTO_API_KEY is set in Cloudflare Pages production environment
- POST to https://docs.nxgen.cloud/functions/page-feedback returns {"success":true} and delivers email
</success_criteria>

<output>
After completion, create `.planning/phases/03-integration-pipeline/03-04-SUMMARY.md`
</output>
