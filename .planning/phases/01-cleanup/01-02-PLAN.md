---
phase: 01-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - classic/src/components/ParticleBackground/
  - classic/src/components/PDFExport/
  - classic/src/components/PDFExportButton/
  - classic/src/components/LanguageSwitcher/
  - classic/src/components/TranslationGlow/
  - classic/src/components/TranslationProgressBar/
  - classic/src/components/ScrollIndicator/
  - classic/src/components/TypingAnimation/
  - classic/src/components/BeforeAfter/
  - classic/src/components/AnimatedStats/
  - classic/src/components/ImageGallery/
  - classic/src/components/BreakthroughsGatewayCard.tsx
  - classic/src/components/HomepageFeatures/
  - classic/src/components/GCXOneLandingPage/
  - classic/src/components/RoleLandingPage.tsx
  - classic/src/css/custom.css
autonomous: true
requirements:
  - CLEN-03
  - CLEN-04

must_haves:
  truths:
    - "Every component directory remaining in src/components/ is imported by at least one active caller in pages/, theme/, or docs/"
    - "No TypeScript import errors after component deletions"
    - "CSS rules for deleted components are removed in the same commit as the component"
    - "Build passes after every deletion group"
  artifacts:
    - path: "classic/src/components/"
      provides: "Directory containing only actively-rendered components"
    - path: "classic/src/css/custom.css"
      provides: "Stylesheet with dead component rules removed (co-located with deletions)"
  key_links:
    - from: "classic/src/components/ParticleBackground/"
      to: "classic/src/css/custom.css"
      via: "CSS class names scoped to ParticleBackground"
      pattern: "particle\|ParticleBackground"
    - from: "classic/src/components/LanguageSwitcher/"
      to: "classic/src/css/custom.css"
      via: "i18n CSS rules"
      pattern: "language-switcher\|translation\|LanguageSwitcher"
---

<objective>
Run grep-based usage audits on ambiguous components, then delete all confirmed-orphaned component directories and their co-located CSS rules in small, build-verified groups.

Purpose: The component directory has ~50 directories. Roughly one-third are orphaned from previous CMS/feature attempts. Deleting them removes dead code that will otherwise confuse Phase 3 integration work and keeps the CSS audit tractable. CSS rules must be removed alongside the component that owns them — not in a separate sweep — to avoid accidental deletion of shared rules.

Output: Only actively-rendered components remain in src/components/. CSS custom.css is trimmed proportionally. Build passes after each deletion group.
</objective>

<execution_context>
@C:/Users/abdel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/abdel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-cleanup/01-RESEARCH.md
@.planning/phases/01-cleanup/01-01-SUMMARY.md

<interfaces>
<!-- Active component consumers confirmed by codebase inspection. Do NOT delete these. -->
<!-- Executor should use this list as an allow-list; anything NOT here needs a usage grep before deletion. -->

Active callers confirmed:
- AlgoliaInsights.tsx → imported in classic/src/theme/Root.tsx
- NXGENSphereHero.tsx → imported in classic/src/pages/index.tsx
- breakthroughs/ (BreakthroughGrid) → classic/src/pages/index.tsx
- GettingStarted/ → classic/src/pages/getting-started.tsx
- RoleSwitcher.tsx → classic/src/theme/DocSidebar/Desktop/index.tsx, NavbarItem/ComponentTypes.tsx
- LanguageToggle/ → classic/src/theme/NavbarItem/ComponentTypes.tsx
- ShareSection/ → classic/src/theme/DocItem/Layout/index.tsx
- ScrollProgress/ → classic/src/theme/Root.tsx
- VoCWidget/ → classic/src/theme/Root.tsx
- BackgroundPattern/ → classic/src/theme/Root.tsx
- Checklist/ → classic/docs/getting-started/quick-start-checklist.mdx
- OnboardingDiagrams/ → classic/docs/getting-started/quick-start-checklist.mdx
- GradientText/ → classic/src/pages/user-management.tsx
- Callout/, Steps/, Tabs/, CodeBlock/ → widely used in MDX docs
- FeatureCard → classic/src/pages/index.tsx
- QuickLink → classic/src/pages/index.tsx, classic/src/pages/404.tsx
- LandingPageBackground → classic/src/pages/getting-started.tsx, docs/breakthroughs.tsx, integration-hub.tsx, internal-releases/*.tsx
- PageHeader → classic/src/pages/internal-releases/sprint-2025-12-b.tsx, sprint-2026-01-a.tsx
- ErrorBoundary/ → audit classic/src/theme/Root.tsx before deciding

Confirmed orphans (no callers found in pages/, theme/, or docs/):
- storyblok/ (deleted in Plan 01)
- ParticleBackground/
- PDFExport/ + PDFExportButton/
- LanguageSwitcher/
- TranslationGlow/
- TranslationProgressBar/
- ScrollIndicator/
- TypingAnimation/
- BeforeAfter/
- AnimatedStats/
- ImageGallery/
- BreakthroughsGatewayCard.tsx
- HomepageFeatures/ (no import found in pages or theme)
- GCXOneLandingPage/ (no import found in pages or theme)
- RoleLandingPage.tsx (no import found in pages or theme)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grep-confirm ambiguous components then delete all confirmed orphans</name>
  <files>
    classic/src/components/ParticleBackground/,
    classic/src/components/PDFExport/,
    classic/src/components/PDFExportButton/,
    classic/src/components/LanguageSwitcher/,
    classic/src/components/TranslationGlow/,
    classic/src/components/TranslationProgressBar/,
    classic/src/components/ScrollIndicator/,
    classic/src/components/TypingAnimation/,
    classic/src/components/BeforeAfter/,
    classic/src/components/AnimatedStats/,
    classic/src/components/ImageGallery/,
    classic/src/components/BreakthroughsGatewayCard.tsx,
    classic/src/components/HomepageFeatures/,
    classic/src/components/GCXOneLandingPage/,
    classic/src/components/RoleLandingPage.tsx
  </files>
  <action>
**Step 1: Run final grep confirmation on each ambiguous component.**

For every component in the list below, run the exact grep command shown. If grep returns any import line, the component is ACTIVE — do NOT delete it and note it in the summary. If grep returns no output, proceed to delete it.

```bash
# Run each grep from the repo root
grep -r "HomepageFeatures" /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/classic/src/ /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/classic/docs/ 2>/dev/null
grep -r "GCXOneLandingPage" /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/classic/src/ /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/classic/docs/ 2>/dev/null
grep -r "RoleLandingPage" /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/classic/src/ /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/classic/docs/ 2>/dev/null
grep -r "ParticleBackground" /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/classic/src/ /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/classic/docs/ 2>/dev/null
grep -r "PDFExport\b" /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/classic/src/ /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/classic/docs/ 2>/dev/null
grep -r "LanguageSwitcher" /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/classic/src/ /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/classic/docs/ 2>/dev/null
grep -r "TranslationGlow\|TranslationProgressBar" /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/classic/src/ /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/classic/docs/ 2>/dev/null
grep -r "ScrollIndicator" /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/classic/src/ /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/classic/docs/ 2>/dev/null
grep -r "TypingAnimation" /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/classic/src/ /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/classic/docs/ 2>/dev/null
grep -r "BeforeAfter" /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/classic/src/ /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/classic/docs/ 2>/dev/null
grep -r "AnimatedStats" /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/classic/src/ /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/classic/docs/ 2>/dev/null
grep -r "ImageGallery" /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/classic/src/ /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/classic/docs/ 2>/dev/null
grep -r "BreakthroughsGatewayCard" /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/classic/src/ /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/classic/docs/ 2>/dev/null
```

**Step 2: Delete all components confirmed orphaned in Step 1.**

Delete these in groups and run `npm run build` after each group:

GROUP 1 (i18n/translation — safe to batch; no shared class names expected):
- `classic/src/components/LanguageSwitcher/`
- `classic/src/components/TranslationGlow/`
- `classic/src/components/TranslationProgressBar/`

GROUP 2 (visual effects — isolated):
- `classic/src/components/ParticleBackground/`
- `classic/src/components/ScrollIndicator/`
- `classic/src/components/TypingAnimation/`
- `classic/src/components/AnimatedStats/`
- `classic/src/components/BeforeAfter/`
- `classic/src/components/ImageGallery/`

GROUP 3 (PDF / media export):
- `classic/src/components/PDFExport/`
- `classic/src/components/PDFExportButton/`

GROUP 4 (landing pages — confirmed orphaned):
- `classic/src/components/HomepageFeatures/` (no callers in pages/theme/docs)
- `classic/src/components/GCXOneLandingPage/` (no callers in pages/theme/docs)
- `classic/src/components/RoleLandingPage.tsx` (no callers in pages/theme/docs)
- `classic/src/components/BreakthroughsGatewayCard.tsx`

After GROUP 4, run `npm run build` — it MUST pass before proceeding.

IMPORTANT: If any component from the pre-planning grep list turns out to have a caller discovered during Step 1 above, do NOT delete it. Note the exception in the SUMMARY.
  </action>
  <verify>
    <automated>cd /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/classic && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Build exits 0 after all group deletions. All listed component directories are removed from the filesystem. No TypeScript import errors in build output.</done>
</task>

<task type="auto">
  <name>Task 2: Remove dead CSS rules co-located with deleted components</name>
  <files>
    classic/src/css/custom.css
  </files>
  <action>
Open `classic/src/css/custom.css` (currently 3,832 lines). Remove CSS rule blocks that were exclusively owned by the components deleted in Task 1.

**Strategy:**
1. Search for CSS selectors/comments matching each deleted component by name:
   - `particle`, `ParticleBackground` — remove all rules
   - `language-switcher`, `LanguageSwitcher`, `lang-switcher` — remove all rules
   - `translation-glow`, `TranslationGlow`, `translation-progress`, `TranslationProgressBar` — remove all rules
   - `pdf-export`, `PDFExport`, `pdf-button` — remove all rules
   - `scroll-indicator`, `ScrollIndicator` — remove all rules
   - `typing-animation`, `TypingAnimation` — remove all rules
   - `before-after`, `BeforeAfter` — remove all rules
   - `animated-stats`, `AnimatedStats` — remove all rules
   - `image-gallery`, `ImageGallery` — remove all rules
   - `homepage-features`, `HomepageFeatures` — remove all rules
   - `gcxone-landing`, `GCXOneLandingPage` — remove all rules
   - `role-landing`, `RoleLandingPage` — remove all rules
   - `breakthroughs-gateway`, `BreakthroughsGatewayCard` — remove all rules
   - `storyblok`, `tiptap`, `monaco` — remove any remaining CMS-related rules (these may have survived from prior attempts)

2. **Do NOT remove rules that use generic class names that could be shared across components** (e.g., `.card`, `.button`, `.container`, `.grid`). Only remove rules whose selector or comment block clearly identifies a single deleted component.

3. After removal, run the build and do a quick visual check: open `npm run serve` and verify the homepage still renders correctly in both light and dark mode. Spot-check one docs page.

4. Note the new line count in the summary. Target is under 2,000 lines, but the final push below 2,000 is handled in Plan 04 (full CSS consolidation pass). This task handles the mechanical removal tied to deleted components.
  </action>
  <verify>
    <automated>wc -l /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/classic/src/css/custom.css && cd /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/classic && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>
- Build exits 0 after CSS changes
- Line count of custom.css is lower than 3,832 (the pre-cleanup baseline)
- No missing CSS rule errors in build output
- Record the new line count in the SUMMARY for Plan 04 to use as its starting point
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

```bash
# Confirm no orphaned component directories remain
ls /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/classic/src/components/ | grep -i "particle\|PDFExport\|language-switcher\|translation\|scroll-indicator\|typing\|before-after\|animated-stats\|image-gallery\|homepage-features\|gcxone-landing\|role-landing\|breakthroughs-gateway"

# Confirm CSS line reduction
wc -l /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/classic/src/css/custom.css

# Full build passes
cd /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/classic && npm run build
```
</verification>

<success_criteria>
- All 15 confirmed-orphaned component directories/files are deleted
- `npm run build` exits 0 after all deletions
- CSS line count is measurably lower than 3,832 (note exact count in SUMMARY)
- No TypeScript module-not-found errors in build output
</success_criteria>

<output>
After completion, create `.planning/phases/01-cleanup/01-02-SUMMARY.md` following the summary template.
Include the post-deletion CSS line count — Plan 04 uses it as the starting baseline.
</output>
