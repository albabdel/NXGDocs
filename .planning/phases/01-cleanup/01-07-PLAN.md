---
phase: 01-cleanup
plan: 07
type: execute
wave: 7
depends_on: []
files_modified:
  - classic/src/components/BackToTop/
  - classic/src/components/Badge/
  - classic/src/components/Collapsible/
  - classic/src/components/EnhancedFeatureCard/
  - classic/src/components/ErrorBoundary/
  - classic/src/components/FeaturesGrid/
  - classic/src/components/PrevNext/
  - classic/src/components/QuickLinks/
  - classic/src/components/Skeleton/
  - classic/src/components/VideoEmbed/
autonomous: true
gap_closure: true
requirements:
  - CLEN-03

must_haves:
  truths:
    - "Every directory in classic/src/components/ has at least one import in live classic/src/ or classic/docs/ code"
    - "npm run build completes without TypeScript errors after the deletions"
  artifacts:
    - path: "classic/src/components/"
      provides: "Only actively-rendered components"
      does_not_contain: "BackToTop, Badge, Collapsible, EnhancedFeatureCard, ErrorBoundary, FeaturesGrid, PrevNext, QuickLinks, Skeleton, VideoEmbed"
  key_links:
    - from: "classic/src/components/ (remaining)"
      to: "classic/src/ or classic/docs/ callers"
      via: "import statements"
      pattern: "import.*@site/src/components/"
---

<objective>
Delete the 10 React component directories that the verifier confirmed have zero imports in live
classic/src/ or classic/docs/ code. This closes the gap in success criterion #3 ("No unused React
components remain in src/components/").

Purpose: Plan 02 deleted 15 confirmed-orphaned components but left these 10 because they were not
in its explicit deletion groups. The verifier's grep analysis confirmed all 10 have zero live callers.
Deleting them completes CLEN-03 and eliminates dead maintenance surface before Sanity integration begins.

Output: 10 fewer component directories; clean build; CLEN-03 fully satisfied.
</objective>

<execution_context>
@C:/Users/abdel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/abdel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cleanup/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grep-verify each component is orphaned, then delete all 10</name>
  <files>
    classic/src/components/BackToTop/,
    classic/src/components/Badge/,
    classic/src/components/Collapsible/,
    classic/src/components/EnhancedFeatureCard/,
    classic/src/components/ErrorBoundary/,
    classic/src/components/FeaturesGrid/,
    classic/src/components/PrevNext/,
    classic/src/components/QuickLinks/,
    classic/src/components/Skeleton/,
    classic/src/components/VideoEmbed/
  </files>
  <action>
    For each of the 10 component directories, run a grep to confirm zero imports before deleting.
    Use these exact grep patterns — a non-empty result means the component IS used and must NOT be deleted:

    ```bash
    cd /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs

    grep -r "BackToTop" classic/src classic/docs --include="*.tsx" --include="*.ts" --include="*.mdx" --include="*.md" -l 2>/dev/null | grep -v "classic/src/components/BackToTop"
    grep -r "Badge" classic/src classic/docs --include="*.tsx" --include="*.ts" --include="*.mdx" --include="*.md" -l 2>/dev/null | grep -v "classic/src/components/Badge"
    grep -r "Collapsible" classic/src classic/docs --include="*.tsx" --include="*.ts" --include="*.mdx" --include="*.md" -l 2>/dev/null | grep -v "classic/src/components/Collapsible"
    grep -r "EnhancedFeatureCard" classic/src classic/docs --include="*.tsx" --include="*.ts" --include="*.mdx" --include="*.md" -l 2>/dev/null | grep -v "classic/src/components/EnhancedFeatureCard"
    grep -r "ErrorBoundary" classic/src classic/docs --include="*.tsx" --include="*.ts" --include="*.mdx" --include="*.md" -l 2>/dev/null | grep -v "classic/src/components/ErrorBoundary"
    grep -r "FeaturesGrid" classic/src classic/docs --include="*.tsx" --include="*.ts" --include="*.mdx" --include="*.md" -l 2>/dev/null | grep -v "classic/src/components/FeaturesGrid"
    grep -r "PrevNext" classic/src classic/docs --include="*.tsx" --include="*.ts" --include="*.mdx" --include="*.md" -l 2>/dev/null | grep -v "classic/src/components/PrevNext"
    grep -r "QuickLinks" classic/src classic/docs --include="*.tsx" --include="*.ts" --include="*.mdx" --include="*.md" -l 2>/dev/null | grep -v "classic/src/components/QuickLinks"
    grep -r "Skeleton" classic/src classic/docs --include="*.tsx" --include="*.ts" --include="*.mdx" --include="*.md" -l 2>/dev/null | grep -v "classic/src/components/Skeleton"
    grep -r "VideoEmbed" classic/src classic/docs --include="*.tsx" --include="*.ts" --include="*.mdx" --include="*.md" -l 2>/dev/null | grep -v "classic/src/components/VideoEmbed"
    ```

    Important notes:
    - `QuickLinks` (plural) is distinct from `QuickLink` (singular, which IS used). The grep above already
      targets the exact string "QuickLinks" — a match in a file that imports QuickLink but not QuickLinks
      will show only if the word "QuickLinks" appears. Confirm the match is not the QuickLinks directory
      itself before deciding.
    - `ErrorBoundary` — Docusaurus has its own internal ErrorBoundary in node_modules. Grep is scoped
      to classic/src and classic/docs which excludes node_modules. Any result here would be a project
      import, not a framework reference.
    - `Badge` — the verifier noted "only inline Badge vars in pages" (e.g., a variable called badge, not
      an import of the Badge component). If grep returns pages that use a lowercase "badge" variable or
      CSS class but not an import of @site/src/components/Badge, those are NOT callers.

    If any grep returns a result showing a live import, do NOT delete that component and note it in the
    task output. For all components confirmed to have zero live callers, delete the entire directory:

    ```bash
    rm -rf classic/src/components/BackToTop
    rm -rf classic/src/components/Badge
    rm -rf classic/src/components/Collapsible
    rm -rf classic/src/components/EnhancedFeatureCard
    rm -rf classic/src/components/ErrorBoundary
    rm -rf classic/src/components/FeaturesGrid
    rm -rf classic/src/components/PrevNext
    rm -rf classic/src/components/QuickLinks
    rm -rf classic/src/components/Skeleton
    rm -rf classic/src/components/VideoEmbed
    ```

    Also check and delete the COMPONENT_USAGE_GUIDE.md file if it references these now-deleted
    components as available (it is documentation, not live code, but it misleads):

    ```bash
    # Optional: update or delete guide if it still lists removed components as available
    cat classic/src/components/COMPONENT_USAGE_GUIDE.md | grep -E "VideoEmbed|Badge|BackToTop|Collapsible|EnhancedFeatureCard|ErrorBoundary|FeaturesGrid|PrevNext|QuickLinks|Skeleton"
    ```

    If the guide lists removed components as usable without caveats, remove those entries or add a
    "removed" note. If the guide is primarily about remaining components, leave it. Use judgment.
  </action>
  <verify>
    <automated>
      # Confirm directories are gone
      ls classic/src/components/ | grep -E "^(BackToTop|Badge|Collapsible|EnhancedFeatureCard|ErrorBoundary|FeaturesGrid|PrevNext|QuickLinks|Skeleton|VideoEmbed)$" || echo "PASS: all 10 component directories deleted"
    </automated>
  </verify>
  <done>
    All 10 directories absent from classic/src/components/. Each was confirmed orphaned via grep
    before deletion. Any exception (component found to have a live caller) is documented in task output.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run build to confirm no TypeScript errors from deletions</name>
  <files>classic/src/components/</files>
  <action>
    Run the Docusaurus build to confirm the TypeScript compiler finds no broken imports after the
    10 component deletions:

    ```bash
    cd /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/classic
    npm run build 2>&1 | tail -30
    ```

    If the build fails with a "Cannot find module" or "Module not found" error pointing to any of the
    10 deleted components, that is evidence of a hidden caller that grep did not surface (e.g., a
    dynamic import string or a re-export barrel file). In that case:

    1. Note the exact file and line number from the error
    2. Check if the caller is live code or dead code
    3. If dead code: delete the caller too and re-run the build
    4. If live code: restore the component with `git checkout -- classic/src/components/{Name}/`
       and document the exception

    A successful build (exit 0 with "Generated static files" in output) is the authoritative gate.
    The `onBrokenLinks: 'throw'` setting means the build will also catch any link breakage introduced
    by the deletions — though component deletions should not create broken doc links.
  </action>
  <verify>
    <automated>cd /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/classic && npm run build 2>&1 | grep -E "(Generated static files|ERROR|error TS)" | head -10</automated>
  </verify>
  <done>
    Build exits 0 and output contains "Generated static files". No TypeScript errors referencing
    any of the 10 deleted component paths.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `ls classic/src/components/` does not contain BackToTop, Badge, Collapsible, EnhancedFeatureCard,
   ErrorBoundary, FeaturesGrid, PrevNext, QuickLinks, Skeleton, or VideoEmbed.

2. `npm run build` exits 0 with "Generated static files" output.

3. CLEN-03 is now fully satisfied: every remaining directory in classic/src/components/ has at least
   one confirmed import in live classic/src/ or classic/docs/ code.
</verification>

<success_criteria>
- All 10 orphaned component directories deleted (or documented if a live caller was found)
- Build passes clean after deletions
- CLEN-03: "No unused React components remain in src/components/" is fully satisfied
- Success criterion #3 from the phase goal is verified PASS
</success_criteria>

<output>
After completion, create `.planning/phases/01-cleanup/01-07-SUMMARY.md` following the summary template.
Include: which components were deleted, which greps ran, whether any exceptions were found, build result.
</output>
