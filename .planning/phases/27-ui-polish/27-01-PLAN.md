---
phase: 27-ui-polish
plan: 01
type: execute
wave: 1
depends_on: [26-02]
files_modified: [classic/src/css/tokens.css, classic/src/css/base.css]
autonomous: true
requirements: [PLSH-01, PLSH-02, PLSH-03]
user_setup: []

must_haves:
  truths:
    - "Shadow tokens use layered system (ambient + direct)"
    - "Focus-visible global styles exist"
    - "prefers-reduced-motion media query exists"
  artifacts:
    - path: "classic/src/css/tokens.css"
      provides: "Layered shadow tokens for depth"
      contains: "--shadow-md"
    - path: "classic/src/css/base.css"
      provides: "Focus and motion accessibility styles"
      contains: ":focus-visible"
  key_links:
    - from: "tokens.css"
      to: "components"
      via: "var(--shadow-*)"
      pattern: "var\\(--shadow-"
---

<objective>
Add premium visual details: layered shadows, focus states, and reduced motion support.

Purpose: Elevate visual quality and accessibility using what's already in the codebase.
Output: Professional depth, keyboard navigation visibility, and motion preferences respected.
</objective>

<execution_context>
@~\.config\opencode/get-shit-done/workflows/execute-plan.md
@~\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP-design-system.md
@.planning/STATE.md
@DESIGN_SYSTEM_ANALYSIS_AND_IMPLEMENTATION_PLAN.md
@classic/src/css/tokens.css
</context>

<tasks>

<task type="auto">
  <name>task 1: Upgrade shadow tokens to layered system</name>
  <files>classic/src/css/tokens.css</files>
  <action>
    Replace single-layer shadows with ambient + direct layered shadows for natural depth.
    
    Add/replace in the `:root` block:
    ```css
    /* === LAYERED SHADOWS - Natural Depth === */
    --shadow-sm: 0 1px 2px rgba(0,0,0,0.06), 0 1px 3px rgba(0,0,0,0.10);
    --shadow-md: 0 4px 6px rgba(0,0,0,0.07), 0 2px 4px rgba(0,0,0,0.06), 0 0 0 1px rgba(0,0,0,0.04);
    --shadow-lg: 0 10px 15px rgba(0,0,0,0.10), 0 4px 6px rgba(0,0,0,0.05), 0 0 0 1px rgba(0,0,0,0.03);
    --shadow-xl: 0 20px 25px rgba(0,0,0,0.10), 0 8px 10px rgba(0,0,0,0.04), 0 0 0 1px rgba(0,0,0,0.02);
    
    /* Gold glow shadows for special elements */
    --shadow-gold-sm: 0 2px 8px rgba(200,148,70,0.15), 0 1px 2px rgba(200,148,70,0.10);
    --shadow-gold: 0 4px 16px rgba(200,148,70,0.20), 0 1px 3px rgba(200,148,70,0.10);
    --shadow-gold-lg: 0 8px 24px rgba(200,148,70,0.25), 0 2px 6px rgba(200,148,70,0.12);
    ```
    
    Add in `[data-theme='dark']` block:
    ```css
    /* Dark mode shadows - more intense for contrast */
    --shadow-sm: 0 1px 2px rgba(0,0,0,0.30), 0 1px 3px rgba(0,0,0,0.40);
    --shadow-md: 0 4px 6px rgba(0,0,0,0.35), 0 2px 4px rgba(0,0,0,0.30), 0 0 0 1px rgba(255,255,255,0.03);
    --shadow-lg: 0 10px 15px rgba(0,0,0,0.50), 0 4px 6px rgba(0,0,0,0.35), 0 0 0 1px rgba(255,255,255,0.02);
    --shadow-xl: 0 20px 25px rgba(0,0,0,0.60), 0 8px 10px rgba(0,0,0,0.40), 0 0 0 1px rgba(255,255,255,0.02);
    
    /* Gold glow - more subtle in dark mode */
    --shadow-gold-sm: 0 2px 8px rgba(200,148,70,0.12), 0 1px 2px rgba(200,148,70,0.08);
    --shadow-gold: 0 4px 16px rgba(200,148,70,0.15), 0 1px 3px rgba(200,148,70,0.08);
    --shadow-gold-lg: 0 8px 24px rgba(200,148,70,0.18), 0 2px 6px rgba(200,148,70,0.10);
    ```
  </action>
  <verify>
    <automated>grep -c "shadow-gold" classic/src/css/tokens.css</automated>
    <manual>Verify layered shadow tokens exist in both light and dark</manual>
    <sampling_rate>run after task commits</sampling_rate>
  </verify>
  <done>Layered shadow tokens exist for light and dark modes</done>
</task>

<task type="auto">
  <name>task 2: Add focus-visible global styles</name>
  <files>classic/src/css/base.css, classic/src/css/custom.css</files>
  <action>
    Create `classic/src/css/base.css` with focus and accessibility styles:
    
    ```css
    /* Base Accessibility Styles */
    @layer base {
      /* Focus visible - keyboard navigation indicator */
      :focus-visible {
        outline: 2px solid var(--ifm-color-primary);
        outline-offset: 2px;
        border-radius: 4px;
      }
      
      /* Remove outline for mouse users */
      :focus:not(:focus-visible) {
        outline: none;
      }
      
      /* Reduced motion preference */
      @media (prefers-reduced-motion: reduce) {
        *,
        *::before,
        *::after {
          animation-duration: 0.01ms !important;
          animation-iteration-count: 1 !important;
          transition-duration: 0.01ms !important;
          scroll-behavior: auto !important;
        }
      }
      
      /* High contrast mode support */
      @media (prefers-contrast: high) {
        :root {
          --border-subtle: rgba(0, 0, 0, 0.3);
          --glass-border: rgba(200, 148, 70, 0.4);
        }
      }
      
      /* Selection styling */
      ::selection {
        background: rgba(200, 148, 70, 0.3);
        color: inherit;
      }
      
      /* Smooth scrolling (respects reduced motion) */
      html {
        scroll-behavior: smooth;
      }
      
      @media (prefers-reduced-motion: reduce) {
        html {
          scroll-behavior: auto;
        }
      }
    }
    ```
    
    Add import to custom.css:
    ```css
    @import './base.css';
    ```
  </action>
  <verify>
    <automated>test -f classic/src/css/base.css && grep -c "focus-visible" classic/src/css/base.css</automated>
    <manual>Verify base.css exists with focus-visible styles</manual>
    <sampling_rate>run after task commits</sampling_rate>
  </verify>
  <done>base.css created with focus-visible, reduced-motion, and high-contrast support</done>
</task>

<task type="auto">
  <name>task 3: Verify build and accessibility</name>
  <files>classic/src/css/custom.css</files>
  <action>
    Verify the build passes and all imports are correct:
    
    ```bash
    cd classic && npm run build 2>&1 | head -50
    ```
    
    Check that custom.css imports base.css:
    ```bash
    grep "base.css" classic/src/css/custom.css
    ```
    
    Run typecheck to ensure no TypeScript errors:
    ```bash
    cd classic && npm run typecheck
    ```
  </action>
  <verify>
    <automated>cd classic && npm run build 2>&1 | grep -c "SUCCESS"</automated>
    <manual>Verify build passes with new base.css imported</manual>
    <sampling_rate>run after task commits</sampling_rate>
  </verify>
  <done>Build passes, base.css imported correctly</done>
</task>

</tasks>

<verification>
1. `grep -c "shadow-gold" classic/src/css/tokens.css` shows tokens for both themes
2. `test -f classic/src/css/base.css` passes
3. `grep -c "focus-visible" classic/src/css/base.css` ≥ 1
4. `grep -c "prefers-reduced-motion" classic/src/css/base.css` ≥ 1
5. `npm run build` exits 0
</verification>

<success_criteria>
- Layered shadow tokens exist in tokens.css
- base.css exists with focus-visible styles
- prefers-reduced-motion media query exists
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/27-ui-polish/27-01-SUMMARY.md`
</output>
