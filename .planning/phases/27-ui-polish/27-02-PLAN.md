---
phase: 27-ui-polish
plan: 02
type: execute
wave: 2
depends_on: [27-01]
files_modified: [classic/src/css/components/cards.css, classic/src/css/components/buttons.css]
autonomous: true
requirements: [PLSH-04, PLSH-05]
user_setup: []

must_haves:
  truths:
    - "Hover states on cards include transform and shadow changes"
    - "Hover states on buttons are visually distinct"
    - "Featured content blocks have accent top borders"
  artifacts:
    - path: "classic/src/css/components/cards.css"
      provides: "Card hover states with depth"
      contains: ":hover"
    - path: "classic/src/css/components/buttons.css"
      provides: "Button hover states"
      contains: ":hover"
  key_links:
    - from: "cards.css"
      to: "tokens.css"
      via: "var(--shadow-gold)"
      pattern: "var\\(--shadow"
---

<objective>
Improve hover interactions and add accent borders for premium feel.

Purpose: Make interactive elements feel more responsive and polished.
Output: Cards that lift and glow on hover, buttons with clear states, featured content with accent lines.
</objective>

<execution_context>
@~\.config\opencode/get-shit-done/workflows/execute-plan.md
@~\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP-design-system.md
@.planning/STATE.md
@DESIGN_SYSTEM_ANALYSIS_AND_IMPLEMENTATION_PLAN.md
@classic/src/css/components/cards.css
</context>

<tasks>

<task type="auto">
  <name>task 1: Add hover improvements to cards</name>
  <files>classic/src/css/components/cards.css</files>
  <action>
    Add premium hover states to cards that include transform and shadow changes.
    
    Add/enhance in cards.css:
    ```css
    /* Feature Card - Premium Hover */
    .feature-card {
      transition: all 0.25s cubic-bezier(0.4, 0, 0.2, 1);
    }
    
    .feature-card:hover {
      transform: translateY(-4px);
      box-shadow: var(--shadow-gold);
      border-color: var(--glass-border-hover);
    }
    
    .feature-card:active {
      transform: translateY(-2px);
    }
    
    /* Card Featured Accent */
    .card-featured {
      border-top: 2px solid var(--ifm-color-primary);
    }
    
    .card-featured:hover {
      border-top-color: var(--ifm-color-primary-light);
    }
    
    /* Card Elevated Variant */
    .card-elevated {
      box-shadow: var(--shadow-md);
      transition: all 0.25s cubic-bezier(0.4, 0, 0.2, 1);
    }
    
    .card-elevated:hover {
      box-shadow: var(--shadow-lg);
      transform: translateY(-2px);
    }
    
    /* Card Icon Hover */
    .card-icon {
      transition: all 0.2s ease;
    }
    
    .feature-card:hover .card-icon {
      transform: scale(1.05);
      background: var(--icon-bg-hover);
    }
    
    /* Card Title Hover */
    .card-title {
      transition: color 0.2s ease;
    }
    
    .feature-card:hover .card-title {
      color: var(--gold-text);
    }
    ```
    
    Ensure these classes work in both light and dark modes by using CSS tokens.
  </action>
  <verify>
    <automated>grep -c "translateY" classic/src/css/components/cards.css</automated>
    <manual>Verify cards have transform and shadow hover states</manual>
    <sampling_rate>run after task commits</sampling_rate>
  </verify>
  <done>Cards have premium hover states with transform and shadow changes</done>
</task>

<task type="auto">
  <name>task 2: Add hover improvements to buttons</name>
  <files>classic/src/css/components/buttons.css</files>
  <action>
    Add premium hover states to buttons.
    
    Add/enhance in buttons.css:
    ```css
    /* Button Base */
    .btn {
      transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
      position: relative;
      overflow: hidden;
    }
    
    /* Button Hover Lift */
    .btn:hover {
      transform: translateY(-1px);
    }
    
    .btn:active {
      transform: translateY(0);
    }
    
    /* Button Primary */
    .btn-primary:hover {
      box-shadow: var(--shadow-gold-sm);
    }
    
    /* Button Secondary */
    .btn-secondary:hover {
      box-shadow: var(--shadow-sm);
    }
    
    /* Button Ghost */
    .btn-ghost {
      position: relative;
    }
    
    .btn-ghost::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--gold-bg);
      border-radius: inherit;
      opacity: 0;
      transition: opacity 0.2s ease;
    }
    
    .btn-ghost:hover::before {
      opacity: 1;
    }
    
    /* Button Destructive */
    .btn-destructive:hover {
      box-shadow: 0 4px 12px rgba(239, 68, 68, 0.3);
    }
    
    /* Button Focus Ring */
    .btn:focus-visible {
      outline: 2px solid var(--ifm-color-primary);
      outline-offset: 2px;
    }
    
    /* Button Disabled */
    .btn:disabled {
      opacity: 0.5;
      cursor: not-allowed;
      transform: none;
    }
    ```
  </action>
  <verify>
    <automated>grep -c "btn:hover" classic/src/css/components/buttons.css</automated>
    <manual>Verify buttons have distinct hover states</manual>
    <sampling_rate>run after task commits</sampling_rate>
  </verify>
  <done>Buttons have premium hover states with visual feedback</done>
</task>

<task type="auto">
  <name>task 3: Verify build and visual consistency</name>
  <files>classic/src/css/custom.css</files>
  <action>
    Verify the build passes with all CSS changes:
    
    ```bash
    cd classic && npm run build 2>&1 | head -50
    ```
    
    Ensure all imports are correct in custom.css:
    ```bash
    grep -c "@import" classic/src/css/custom.css
    ```
    
    Run a quick lint check:
    ```bash
    cd classic && npm run lint -- --max-warnings 0 2>&1 | head -20
    ```
  </action>
  <verify>
    <automated>cd classic && npm run build 2>&1 | grep -c "SUCCESS"</automated>
    <manual>Verify build passes with hover state improvements</manual>
    <sampling_rate>run after task commits</sampling_rate>
  </verify>
  <done>Build passes, hover states work correctly</done>
</task>

</tasks>

<verification>
1. `grep -c "translateY" classic/src/css/components/cards.css` ≥ 1
2. `grep -c "btn:hover" classic/src/css/components/buttons.css` ≥ 1
3. `grep -c "card-featured" classic/src/css/components/cards.css` ≥ 1
4. `npm run build` exits 0
</verification>

<success_criteria>
- Cards have transform and shadow hover states
- Buttons have distinct hover states
- Featured cards have accent top border
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/27-ui-polish/27-02-SUMMARY.md`
</output>
