---
phase: 30-auth-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [AUTH-01]
user_setup:
  - service: auth0
    why: "Authentication provider"
    env_vars:
      - name: AUTH0_CLIENT_SECRET
        source: "Auth0 Dashboard -> Applications -> docs-portal -> Settings"
    dashboard_config:
      - task: "Create Auth0 Action for custom claims"
        location: "Auth0 Dashboard -> Actions -> Library -> Create Action"

must_haves:
  truths:
    - "User can log in from any page on the site"
    - "User sees login button in navbar when not authenticated"
    - "User sees profile dropdown when authenticated"
    - "SSO works with other NXGEN apps (no re-login required)"
    - "Site loads normally without authentication"
  artifacts:
    - path: "classic/src/theme/Root.tsx"
      provides: "Auth0Provider wrapper for entire app"
      exports: ["Root"]
    - path: "classic/src/components/Auth/LoginButton.tsx"
      provides: "Login button component"
    - path: "classic/src/components/Auth/ProfileDropdown.tsx"
      provides: "User profile dropdown with logout"
    - path: "classic/src/components/Auth/index.ts"
      provides: "Barrel export for auth components"
  key_links:
    - from: "classic/src/theme/Root.tsx"
      to: "@auth0/auth0-react"
      via: "Auth0Provider import"
      pattern: "Auth0Provider"
    - from: "classic/src/theme/Navbar/index.tsx"
      to: "LoginButton/ProfileDropdown"
      via: "conditional render based on isAuthenticated"
      pattern: "isAuthenticated.*ProfileDropdown"
---

<objective>
Integrate Auth0Provider across the entire Docusaurus application and add authentication UI to the navbar.

Purpose: Enable product-wide authentication that works seamlessly with existing SSO setup, allowing users to log in from anywhere on the site and see their authentication state in the navbar.

Output: Auth0Provider wrapper in Root.tsx, login button and profile dropdown in navbar, authentication state available throughout the app.
</objective>

<execution_context>
@~\.config\opencode/get-shit-done/workflows/execute-plan.md
@~\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP-auth0-upgrade.md
@.planning/research/auth0-upgrade-FEATURES.md
@.planning/research/auth0-upgrade-EXISTING-AUTH.md
@SUPPORT-CONTEXT.md

@classic/src/theme/Root.tsx
@classic/src/theme/Navbar/index.tsx
</context>

<tasks>

<task type="auto">
  <name>task 1: Install Auth0 React SDK and create Auth components</name>
  <files>
    classic/package.json
    classic/src/components/Auth/LoginButton.tsx
    classic/src/components/Auth/ProfileDropdown.tsx
    classic/src/components/Auth/AuthCallback.tsx
    classic/src/components/Auth/index.ts
  </files>
  <action>
    1. Install @auth0/auth0-react package:
       ```bash
       cd classic && npm install @auth0/auth0-react
       ```

    2. Create `classic/src/components/Auth/LoginButton.tsx`:
       - Simple button component that calls `loginWithRedirect()`
       - Props: `variant` ('navbar' | 'hero'), `size` ('sm' | 'md' | 'lg')
       - Uses gold/blue button styling matching site theme
       - Include "Sign in" text and shield icon
       - DO NOT add `prompt: 'login'` to authorization params (breaks SSO)

    3. Create `classic/src/components/Auth/ProfileDropdown.tsx`:
       - Shows user avatar (first letter of name if no avatar)
       - Dropdown menu with: Profile, Settings, Logout
       - Logout calls `logout({ logoutParams: { returnTo: window.location.origin } })`
       - Use lucide-react icons: User, Settings, LogOut
       - Match existing navbar dropdown styling

    4. Create `classic/src/components/Auth/AuthCallback.tsx`:
       - Handles Auth0 redirect callback
       - Uses `useAuth0().handleRedirectCallback`
       - Redirects to stored path or home after success
       - Shows loading state during callback processing

    5. Create barrel export in `classic/src/components/Auth/index.ts`

    6. Update `classic/package.json` dependencies

    Reference existing patterns:
    - Button styling from `classic/src/components/ZohoTickets/LoginScreen.tsx`
    - Dropdown patterns from `classic/src/components/ZohoTickets/CustomerSidebar.tsx`
  </action>
  <verify>
    <automated>npm run build --prefix classic 2>&1 | grep -q "Build successful" && echo "Build passes"</automated>
    <manual>Verify components render without errors in dev mode</manual>
    <sampling_rate>Run after task 1 commits, before task 2 begins</sampling_rate>
  </verify>
  <done>
    - @auth0/auth0-react installed
    - LoginButton component renders with correct styling
    - ProfileDropdown shows user info and logout option
    - AuthCallback handles redirect
    - All components exported from index.ts
    - Build passes
  </done>
</task>

<task type="auto">
  <name>task 2: Wrap Docusaurus with Auth0Provider</name>
  <files>
    classic/src/theme/Root.tsx
    classic/src/pages/auth/callback.tsx
  </files>
  <action>
    1. Update `classic/src/theme/Root.tsx`:
       - Import Auth0Provider from @auth0/auth0-react
       - Wrap existing Root component with Auth0Provider
       - Configuration:
         ```tsx
         <Auth0Provider
           domain="nxgen.eu.auth0.com"
           clientId="jqiJJISVmCmWWB46m0wMI7CO91KyliIm"
           authorizationParams={{
             redirect_uri: typeof window !== 'undefined' 
               ? `${window.location.origin}/auth/callback`
               : undefined,
             scope: 'openid profile email'
           }}
           useRefreshTokens={true}
           cacheLocation="memory"  // NOT localStorage for security
         >
           <OriginalRoot {...props} />
         </Auth0Provider>
         ```
       - DO NOT add `prompt` to authorizationParams (breaks SSO)
       - Use `cacheLocation="memory"` per Auth0 security best practices

    2. Update `classic/src/pages/auth/callback.tsx`:
       - Import AuthCallback component
       - Create callback page that uses AuthCallback
       - Preserve existing Zoho callback logic (for support portal)
       - Add routing logic: if Auth0 callback, use AuthCallback; if Zoho callback, use existing logic

    3. Add login state persistence:
       - Store redirect path in sessionStorage before login
       - Restore path after successful callback

    Reference:
    - `SUPPORT-CONTEXT.md` for existing auth patterns
    - `.planning/research/auth0-upgrade-FEATURES.md` for Auth0Provider setup
  </action>
  <verify>
    <automated>npm run build --prefix classic 2>&1 | grep -q "Build successful" && echo "Build passes"</automated>
    <manual>Test: visit /auth/callback with id_token in hash, verify redirect to home</manual>
    <sampling_rate>Run after task 2 commits, before task 3 begins</sampling_rate>
  </verify>
  <done>
    - Auth0Provider wraps entire Docusaurus app
    - Auth callback page handles Auth0 redirects
    - Session state available via useAuth0() hook
    - SSO preserved (no prompt parameter)
    - Build passes
  </done>
</task>

<task type="auto">
  <name>task 3: Add authentication UI to navbar</name>
  <files>
    classic/src/theme/Navbar/index.tsx
    classic/src/css/components/navbar-auth.css
  </files>
  <action>
    1. Create `classic/src/css/components/navbar-auth.css`:
       - Styles for login button in navbar
       - Styles for profile dropdown
       - Mobile-responsive variants
       - Use existing CSS tokens (gold-text, glass-bg)

    2. Update `classic/src/theme/Navbar/index.tsx`:
       - Import useAuth0 hook
       - Import LoginButton and ProfileDropdown components
       - Add authentication section to right side of navbar
       - Conditional rendering:
         ```tsx
         const { isAuthenticated, isLoading, user } = useAuth0();
         
         // In navbar right section:
         {isLoading ? (
           <LoadingSpinner size="sm" />
         ) : isAuthenticated ? (
           <ProfileDropdown user={user} />
         ) : (
           <LoginButton variant="navbar" />
         )}
         ```
       - Ensure mobile hamburger menu includes auth state
       - Add CSS import for navbar-auth.css

    3. Ensure navbar remains functional without auth:
       - Don't block navbar render if Auth0 not loaded
       - Show loading state briefly during auth check

    Reference:
    - Existing navbar in `classic/src/theme/Navbar/index.tsx`
    - Button styling from existing components
  </action>
  <verify>
    <automated>npm run build --prefix classic 2>&1 | grep -q "Build successful" && echo "Build passes"</automated>
    <manual>
      1. Visit site without login - see "Sign in" button in navbar
      2. Click login, complete Auth0 flow, return to site
      3. See profile dropdown with user initials/name
      4. Click dropdown, see Profile, Settings, Logout options
    </manual>
    <sampling_rate>Run after task 3 commits, before checkpoint</sampling_rate>
  </verify>
  <done>
    - Login button visible in navbar when not authenticated
    - Profile dropdown visible when authenticated
    - Auth UI works on mobile
    - Site loads normally without login blocking content
    - Build passes
  </done>
</task>

</tasks>

<verification>
**Automated:**
- Build passes: `npm run build --prefix classic`
- No TypeScript errors
- Components export correctly

**Manual:**
1. Visit homepage without login - verify content loads normally
2. Verify "Sign in" button appears in navbar
3. Click login, complete Auth0 flow
4. Verify redirect back to site with profile dropdown visible
5. Test logout - verify return to unauthenticated state
6. Test SSO - login on gcx.nxgen.cloud, visit docs, verify auto-authenticated
</verification>

<success_criteria>
1. Auth0Provider wraps entire Docusaurus application
2. Login button visible in navbar when not authenticated
3. Profile dropdown visible when authenticated
4. SSO preserved (users logged into other NXGEN apps are auto-authenticated)
5. Site loads normally without login (no content gating)
6. Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/30-auth-foundation/30-01-SUMMARY.md`
</output>
