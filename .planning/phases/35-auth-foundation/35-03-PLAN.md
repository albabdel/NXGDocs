---
phase: 35-auth-foundation
plan: 03
type: execute
wave: 2
depends_on: [35-01]
files_modified:
  - functions/page-feedback.ts
  - functions/voc-feedback.ts
  - functions/request-article.ts
  - functions/zoho-customer-verify.ts
  - classic/src/utils/content-visibility.ts
autonomous: true
requirements: [AUTH-04, AUTH-05, MPROD-02]

must_haves:
  truths:
    - "Cloudflare Functions validate product access before processing requests"
    - "Content visibility tiers (public, authenticated, role-based) are defined"
    - "Users cannot submit feedback or requests for products they cannot access"
    - "Product isolation enforced at function level"
  artifacts:
    - path: "functions/page-feedback.ts"
      provides: "Product-guarded feedback endpoint"
      contains: "requireProductAccess"
    - path: "functions/zoho-customer-verify.ts"
      provides: "Session verification with productAccess"
      contains: "productAccess"
    - path: "classic/src/utils/content-visibility.ts"
      provides: "Content visibility utilities"
      exports: ["canViewContent", "VisibilityTier"]
  key_links:
    - from: "functions/page-feedback.ts"
      to: "requireProductAccess"
      via: "guard import"
      pattern: "requireProductAccess"
    - from: "functions/voc-feedback.ts"
      to: "requireProductAccess"
      via: "guard import"
      pattern: "requireProductAccess"
---

<objective>
Add requireProductAccess guard to Cloudflare Functions and create content visibility utilities. This enforces product isolation at the function level.

Purpose: Functions that handle user-submitted data must validate product access to prevent cross-product data leakage.
Output: Guarded functions, content visibility utilities, session endpoint with productAccess.
</objective>

<execution_context>
@~\.config\opencode/get-shit-done/workflows/execute-plan.md
@~\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/PITFALLS.md

# Prior plan output
@.planning/phases/35-auth-foundation/35-01-SUMMARY.md

# Existing function patterns
@functions/page-feedback.ts
@functions/voc-feedback.ts
@functions/zoho-customer-verify.ts
@functions/lib/require-product-access.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: Add requireProductAccess guard to page-feedback</name>
  <files>functions/page-feedback.ts</files>
  <action>
Import requireProductAccess and getProductFromRequest from lib/require-product-access.ts. In onRequestPost handler, call requireProductAccess at the start. Extract product from request (page URL in body or X-Product header). Guard call should return validated session or throw 401/403. Update function to use the validated session instead of separate session check.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('functions/page-feedback.ts','utf8'); console.log(c.includes('requireProductAccess') || c.includes('getProductFromRequest') ? 'PASS' : 'FAIL')"</automated>
    <sampling_rate>after task commit</sampling_rate>
  </verify>
  <done>page-feedback.ts validates product access before processing feedback.</done>
</task>

<task type="auto">
  <name>task 2: Add requireProductAccess guard to voc-feedback</name>
  <files>functions/voc-feedback.ts</files>
  <action>
Import requireProductAccess from lib/require-product-access.ts. In onRequestPost handler, add guard call at the start. The function handles voice-of-customer feedback submissions which should be product-scoped. Ensure product is extracted from request body or header.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('functions/voc-feedback.ts','utf8'); console.log(c.includes('requireProductAccess') ? 'PASS' : 'FAIL')"</automated>
    <sampling_rate>after task commit</sampling_rate>
  </verify>
  <done>voc-feedback.ts validates product access before processing submissions.</done>
</task>

<task type="auto">
  <name>task 3: Add requireProductAccess guard to request-article</name>
  <files>functions/request-article.ts</files>
  <action>
Import requireProductAccess from lib/require-product-access.ts. In onRequestPost handler, add guard call. Article requests should be product-scoped to prevent users from requesting articles for products they do not have access to.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('functions/request-article.ts','utf8'); console.log(c.includes('requireProductAccess') ? 'PASS' : 'FAIL')"</automated>
    <sampling_rate>after task commit</sampling_rate>
  </verify>
  <done>request-article.ts validates product access before processing requests.</done>
</task>

<task type="auto">
  <name>task 4: Update zoho-customer-verify to return productAccess</name>
  <files>functions/zoho-customer-verify.ts</files>
  <action>
Update zoho-customer-verify.ts to include productAccess in the response. Import getSessionFromHeader from lib/zoho-session.ts. Extract session and return productAccess array in the JSON response. This endpoint is used by the frontend to check session state and now needs to provide product entitlements.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('functions/zoho-customer-verify.ts','utf8'); console.log(c.includes('productAccess') ? 'PASS' : 'FAIL')"</automated>
    <sampling_rate>after task commit</sampling_rate>
  </verify>
  <done>zoho-customer-verify returns productAccess in response for frontend use.</done>
</task>

<task type="auto">
  <name>task 5: Create content visibility utilities</name>
  <files>classic/src/utils/content-visibility.ts</files>
  <action>
Create new file classic/src/utils/content-visibility.ts. Define VisibilityTier type: 'public' | 'authenticated' | 'restricted'. Define ContentMetadata interface with product: string and visibility: VisibilityTier. Export canViewContent function that: (1) returns true for public content, (2) requires authentication for authenticated tier, (3) checks productAccess.includes(product) for restricted tier. This utility will be used by components to determine content visibility.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('classic/src/utils/content-visibility.ts','utf8'); console.log(c.includes('VisibilityTier') && c.includes('canViewContent') && c.includes('public') && c.includes('authenticated') && c.includes('restricted') ? 'PASS' : 'FAIL')"</automated>
    <sampling_rate>after task commit</sampling_rate>
  </verify>
  <done>content-visibility.ts defines VisibilityTier and canViewContent utility.</done>
</task>

</tasks>

<verification>
- All user-facing functions validate product access
- Session endpoint returns productAccess
- Content visibility utilities support tiered access
</verification>

<success_criteria>
- page-feedback, voc-feedback, request-article all use requireProductAccess guard
- zoho-customer-verify returns productAccess array
- canViewContent correctly evaluates visibility tiers
</success_criteria>

<output>
After completion, create .planning/phases/35-auth-foundation/35-03-SUMMARY.md
</output>
