---
phase: 35-auth-foundation
plan: 02
type: execute
wave: 2
depends_on: [35-01]
files_modified:
  - functions/zoho-customer-auth.ts
  - functions/admin-auth-callback.ts
  - classic/src/contexts/ProductAccessContext.tsx
  - classic/src/hooks/useProductAccess.ts
  - classic/src/theme/Root.tsx
autonomous: false
requirements: [AUTH-01, AUTH-02]

user_setup:
  - service: auth0
    why: "Auth0 Actions must be configured to add product_access claim to JWT"
    env_vars:
      - name: AUTH0_DOMAIN
        source: "Auth0 Dashboard -> Applications -> Your App -> Settings"
      - name: AUTH0_CLIENT_ID
        source: "Auth0 Dashboard -> Applications -> Your App -> Settings"
    dashboard_config:
      - task: "Create Post-Login Action to add product_access claim"
        location: "Auth0 Dashboard -> Actions -> Library -> Create Action"
        details: |
          Action name: "Add Product Access Claim"
          Trigger: Post User Login
          Code: See AUTH0-ACTION-SETUP.md for complete code
          The Action reads product entitlements from user.app_metadata.productAccess
          and adds them as a namespaced claim: https://nxgen.cloud/claims/product_access
      - task: "Add productAccess to app_metadata for test users"
        location: "Auth0 Dashboard -> User Management -> Users -> [user] -> Details tab"
        details: |
          Add app_metadata: { "productAccess": ["gcxone"] }
          For admins: { "productAccess": ["gcxone", "gcsurge"] }

must_haves:
  truths:
    - "Auth0 id_token contains product_access claim in namespace"
    - "Customer auth function extracts productAccess from JWT and stores in session"
    - "React context provides productAccess to client-side components"
    - "Users only see content for products in their productAccess array"
  artifacts:
    - path: "functions/zoho-customer-auth.ts"
      provides: "Session creation with productAccess from Auth0"
      contains: "product_access"
    - path: "classic/src/contexts/ProductAccessContext.tsx"
      provides: "React context for product access"
      exports: ["ProductAccessProvider", "useProductAccess"]
    - path: "classic/src/hooks/useProductAccess.ts"
      provides: "Hook for accessing product entitlements"
      exports: ["useProductAccess"]
  key_links:
    - from: "functions/zoho-customer-auth.ts"
      to: "Auth0 JWT claims"
      via: "claim extraction from id_token"
      pattern: "https://nxgen.cloud/claims/product_access"
    - from: "classic/src/theme/Root.tsx"
      to: "ProductAccessContext"
      via: "provider wrapper"
      pattern: "ProductAccessProvider"
---

<objective>
Extract product_access claim from Auth0 JWT and populate session with product entitlements. Create React context for client-side product access checks.

Purpose: Authentication must include product entitlements so users can only access content for products they own.
Output: Session populated with productAccess from Auth0, React context for product visibility checks.
</objective>

<execution_context>
@~\.config\opencode/get-shit-done/workflows/execute-plan.md
@~\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/research/PITFALLS.md

# Prior plan output
@.planning/phases/35-auth-foundation/35-01-SUMMARY.md

# Existing auth patterns
@functions/zoho-customer-auth.ts
@functions/lib/zoho-session.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: Extract product_access claim from Auth0 JWT</name>
  <files>functions/zoho-customer-auth.ts</files>
  <action>
Modify verifyAuth0IdToken function or add helper to extract custom claims from the JWT payload. Look for the namespaced claim at https://nxgen.cloud/claims/product_access in the decoded token. Update the auth0-exchange handler to extract productAccess from claims (default to ['gcxone'] if claim missing for backwards compatibility). Pass productAccess to createSessionCookie call (modified in plan 01). Also update the email-lookup handler to derive productAccess from Zoho contact custom field or default to ['gcxone'].
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('functions/zoho-customer-auth.ts','utf8'); console.log(c.includes('product_access') || c.includes('productAccess') ? 'PASS' : 'FAIL')"</automated>
    <sampling_rate>after task commit</sampling_rate>
  </verify>
  <done>zoho-customer-auth.ts extracts productAccess from Auth0 claims and passes to session creation.</done>
</task>

<task type="auto">
  <name>task 2: Update admin-auth-callback with product access</name>
  <files>functions/admin-auth-callback.ts</files>
  <action>
Update admin-auth-callback.ts to pass productAccess when creating admin session. Since admins should see all products, pass ['gcxone', 'gcsurge'] as productAccess to createAdminSession. This ensures admin sessions have full product access for administrative purposes. Update the AdminUser object construction to include productAccess field.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('functions/admin-auth-callback.ts','utf8'); console.log(c.includes('productAccess') ? 'PASS' : 'FAIL')"</automated>
    <sampling_rate>after task commit</sampling_rate>
  </verify>
  <done>admin-auth-callback.ts passes productAccess to session creation; admins have all products.</done>
</task>

<task type="auto">
  <name>task 3: Create ProductAccessContext</name>
  <files>classic/src/contexts/ProductAccessContext.tsx</files>
  <action>
Create new file classic/src/contexts/ProductAccessContext.tsx. Define ProductAccessContext with: productAccess (string array), hasAccess(product: string) function, isLoading boolean. Create ProductAccessProvider component that: (1) fetches /zoho-customer-verify or /admin-session endpoint on mount, (2) extracts productAccess from response, (3) provides context value to children. Export useProductAccess hook for consuming components.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('classic/src/contexts/ProductAccessContext.tsx','utf8'); console.log(c.includes('ProductAccessContext') && c.includes('ProductAccessProvider') && c.includes('useProductAccess') && c.includes('hasAccess') ? 'PASS' : 'FAIL')"</automated>
    <sampling_rate>after task commit</sampling_rate>
  </verify>
  <done>ProductAccessContext.tsx exists with ProductAccessProvider and useProductAccess hook.</done>
</task>

<task type="auto">
  <name>task 4: Create useProductAccess hook</name>
  <files>classic/src/hooks/useProductAccess.ts</files>
  <action>
Create new file classic/src/hooks/useProductAccess.ts that re-exports useProductAccess from ProductAccessContext. This provides a cleaner import path for components. Export both the hook and the ProductAccess type.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('classic/src/hooks/useProductAccess.ts','utf8'); console.log(c.includes('useProductAccess') ? 'PASS' : 'FAIL')"</automated>
    <sampling_rate>after task commit</sampling_rate>
  </verify>
  <done>useProductAccess hook exists and re-exports from context.</done>
</task>

<task type="auto">
  <name>task 5: Wrap Root with ProductAccessProvider</name>
  <files>classic/src/theme/Root.tsx</files>
  <action>
Import ProductAccessProvider from contexts/ProductAccessContext.tsx. Wrap the existing Root component children with ProductAccessProvider. Place it inside any existing auth providers (like AdminAuthProvider) so product access is available alongside session data.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('classic/src/theme/Root.tsx','utf8'); console.log(c.includes('ProductAccessProvider') ? 'PASS' : 'FAIL')"</automated>
    <sampling_rate>after task commit</sampling_rate>
  </verify>
  <done>Root.tsx wraps app with ProductAccessProvider.</done>
</task>

</tasks>

<verification>
- Auth0 JWT claims extracted and stored in session
- React context provides productAccess to components
- Admin sessions have all product access
</verification>

<success_criteria>
- Session includes productAccess array from Auth0 claims
- useProductAccess hook returns user's product entitlements
- ProductAccessProvider wraps app in Root.tsx
</success_criteria>

<output>
After completion, create .planning/phases/35-auth-foundation/35-02-SUMMARY.md
</output>
