---
phase: 8
plan: 1
wave: 1
---

# Plan 8.1: Roadmap Page with Filtering and Search

## Objective

Replace the `SanityLandingPageRoute` wrapper in `roadmap.tsx` with a full React page that imports `sanity-roadmap.generated.json` directly and renders a filterable, searchable list of roadmap items with expandable details. Shipped items link to their corresponding release detail pages.

## Context

- `.planning/phases/08-roadmap-page/08-RESEARCH.md` — All patterns for this phase
- `.planning/phases/06-schema-and-data-pipeline/06-RESEARCH.md` — JSON structure
- `.planning/phases/07-releases-page/07-RESEARCH.md` — Phase 7 patterns (direct import)
- `classic/src/legacy-pages/roadmap.tsx` — UI patterns to adapt (filtering, expand)
- `classic/src/data/sanity-roadmap.generated.json` — Data source (Phase 6 output)

## Prerequisite

Phase 6 must produce `sanity-roadmap.generated.json` with mock data (MOCK-02). If JSON is empty, page shows empty state.

**Gap Fix Required:** Phase 6 GROQ query must include `_updatedAt` for ROAD-07. Add to fetch query before this plan executes.

## Tasks

<task type="auto">
  <name>Create roadmap page with JSON import and status filtering</name>
  <files>
    - classic/src/pages/roadmap.tsx
  </files>
  <action>
    Replace the entire content of `classic/src/pages/roadmap.tsx`:
    
    1. Remove the `SanityLandingPageRoute` import and wrapper
    2. Remove the `LegacyPage` import
    3. Add static import: `import roadmapData from '../data/sanity-roadmap.generated.json'`
    4. Add type interface matching JSON structure from Phase 6
    5. Render roadmap list with:
       - Layout wrapper (keep existing `Layout`, `LandingPageBackground`, `PageHeader`)
       - Page title: "Product Roadmap"
       - Hero section with description text
       - Status filter buttons: "All", "Planned", "In Progress", "Shipped"
       - Results count showing `filteredItems.length` of `roadmapData.length`
    6. Status filter implementation:
       - Use `useState` for `statusFilter` state
       - Use `useMemo` for filtered items
       - Filter: `statusFilter === 'All' || item.status === statusFilter`
    7. Keep existing framer-motion animations for consistency
    
    Do NOT:
    - Use SanityLandingPageRoute (roadmap is always Sanity-driven)
    - Import from `@sanity/client` (JSON is already built)
    - Include year/quarter/category filters (not in v1.1 requirements)
    - Include product backlog section (not in v1.1 requirements)
  </action>
  <verify>`npm run build` compiles without TypeScript errors</verify>
  <done>
    - roadmap.tsx imports sanity-roadmap.generated.json directly
    - No SanityLandingPageRoute or legacy fallback references remain
    - Status filter buttons render and filter correctly
    - Results count updates when filter changes
  </done>
</task>

<task type="auto">
  <name>Add keyword search and empty state</name>
  <files>
    - classic/src/pages/roadmap.tsx
  </files>
  <action>
    Enhance the roadmap page with search functionality:
    
    1. Add search input above status filters:
       - Use `useState` for `searchQuery` state
       - Input with placeholder: "Search roadmap items..."
       - Search icon from lucide-react
       - Clear button when searchQuery is not empty
    2. Update `useMemo` filter to include search:
       ```tsx
       const matchesSearch = searchQuery === '' || 
         item.title.toLowerCase().includes(searchQuery.toLowerCase()) ||
         (item.description?.toLowerCase().includes(searchQuery.toLowerCase()) ?? false);
       ```
    3. Add empty state render:
       - When `filteredItems.length === 0`
       - Message: "No roadmap items match your filters."
       - "Clear filters" button that resets both status and search
    4. Style to match existing site patterns (check `custom.css` or legacy page)
  </action>
  <verify>Visual inspection: search filters items; empty state shows with clear button</verify>
  <done>
    - Search input renders and filters items
    - Empty state shows when no matches
    - "Clear filters" button resets both filters
  </done>
</task>

<task type="auto">
  <name>Create expandable roadmap item cards</name>
  <files>
    - classic/src/pages/roadmap.tsx
  </files>
  <action>
    Create expandable cards for each roadmap item:
    
    1. Card header (always visible):
       - Title (`item.title`)
       - Status badge with icon (reuse getStatusConfig pattern adapted for new status values)
       - Projected release (`item.projectedRelease`)
       - Expand/collapse chevron icon
    2. Expanded content (use framer-motion AnimatePresence):
       - Description (`item.description`)
       - Business Value section with icon
       - Change Type badge (feature/fix/improvement/breaking)
       - UI Change indicator (if `item.uiChange === true`)
       - Entities Impacted tags (`item.entitiesImpacted[]`)
    3. Shipped item release link:
       - Render only when `item.status === 'Shipped' && item.releaseSlug`
       - Link to `/releases/${item.releaseSlug}`
       - Text: "Released in [Sprint X] →" (format releaseSlug nicely)
    4. Click handler to toggle expanded state:
       - Track `expandedId` in useState
       - Only one item expanded at a time (accordion behavior)
    
    Use existing card styling from legacy page as reference.
  </action>
  <verify>Visual: items expand/collapse; all fields visible; Shipped items show release link</verify>
  <done>
    - Cards expand/collapse with animation
    - All fields render in expanded state
    - Shipped items show clickable release link
  </done>
</task>

<task type="auto">
  <name>Add last updated footer</name>
  <files>
    - classic/src/pages/roadmap.tsx
  </files>
  <action>
    Add footer section showing when roadmap was last updated:
    
    1. Calculate latest update date:
       - Find max `_updatedAt` from all roadmap items
       - Format as human-readable date (e.g., "March 13, 2026")
    2. Render footer at bottom of page:
       - "Last updated: [date]" text
       - Style consistently with page footer
    3. Handle empty data gracefully:
       - If no items, show "Last updated: --" or hide footer
    
    Note: Requires `_updatedAt` in JSON from Phase 6. If not present, use current date as fallback and log warning.
  </action>
  <verify>Visual: footer shows last updated date</verify>
  <done>
    - Footer shows "Last updated: [date]"
    - Date updates based on roadmap data
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Visual verification of roadmap page</name>
  <files>
    - classic/src/pages/roadmap.tsx
  </files>
  <action>
    Human verification checkpoint. Run `npm run start` and visit /roadmap.
    
    Verify:
    1. Page renders without errors
    2. Roadmap items appear from Sanity data
    3. Status filter buttons work (All, Planned, In Progress, Shipped)
    4. Search input filters items by title/description
    5. Results count updates correctly
    6. Empty state shows "Clear filters" button when no matches
    7. Items expand/collapse with animation
    8. Expanded item shows: description, business value, change type, UI change flag, entities
    9. Shipped items show "Released in [Sprint X]" link
    10. Footer shows "Last updated: [date]"
    11. Page is responsive on mobile viewport
    
    Test with mock data from Phase 6. Verify Shipped items link to correct release pages.
  </action>
  <verify>Human confirms all 11 checks pass</verify>
  <done>
    - Human approves roadmap page layout
    - All filtering and expansion works correctly
    - Shipped items link to releases
    - Ready to proceed to Plan 8.2
  </done>
</task>

## Success Criteria

- [ ] roadmap.tsx imports sanity-roadmap.generated.json directly (no SanityLandingPageRoute)
- [ ] Status filter (All/Planned/In Progress/Shipped) works correctly
- [ ] Keyword search filters by title and description
- [ ] Results count shows current/total items
- [ ] Empty state has "Clear filters" action
- [ ] Items expand to show all fields
- [ ] Shipped items show release link
- [ ] Footer shows "Last updated" date
- [ ] `npm run build` succeeds
