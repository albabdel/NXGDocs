---
phase: 31-user-profile
plan: 02
type: execute
wave: 2
depends_on: [31-01]
files_modified: []
autonomous: true
requirements: [AUTH-02, AUTH-05]

must_haves:
  truths:
    - "User sees their profile in the navbar dropdown"
    - "User can access a profile/settings page"
    - "Dark mode preference syncs across devices"
    - "Profile is created automatically on first login"
    - "Preferences are saved and loaded correctly"
  artifacts:
    - path: "classic/src/pages/profile/index.tsx"
      provides: "User profile page"
    - path: "classic/src/pages/profile/settings.tsx"
      provides: "Settings/preferences page"
    - path: "classic/src/hooks/useUserProfile.ts"
      provides: "User profile state hook"
    - path: "classic/src/hooks/useThemeSync.ts"
      provides: "Theme sync with preferences"
  key_links:
    - from: "useUserProfile hook"
      to: "user_profiles table"
      via: "Supabase client"
      pattern: "user_profiles.*select"
    - from: "useThemeSync hook"
      to: "Docusaurus theme"
      via: "useColorMode"
      pattern: "useColorMode.*setTheme"
---

<objective>
Create user profile UI components and settings page with theme synchronization across devices.

Purpose: Give users a visible profile experience and allow them to customize preferences that sync across devices, making the logged-in experience feel personalized.

Output: Profile page, settings page, profile hooks, and theme synchronization.
</objective>

<execution_context>
@~\.config\opencode/get-shit-done/workflows/execute-plan.md
@~\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP-auth0-upgrade.md
@.planning/research/auth0-upgrade-UX-PATTERNS.md

@classic/src/services/user-profile.ts
@classic/src/components/Auth/ProfileDropdown.tsx
</context>

<tasks>

<task type="auto">
  <name>task 1: Create user profile hooks</name>
  <files>
    classic/src/hooks/useUserProfile.ts
    classic/src/hooks/useThemeSync.ts
  </files>
  <action>
    1. Create `classic/src/hooks/useUserProfile.ts`:
       - Manages user profile state
       - Auto-creates profile on first login
       - Provides profile, preferences, loading state
       - Functions: updateProfile, updatePreferences
       ```typescript
       interface UseUserProfileReturn {
         profile: UserProfile | null;
         preferences: UserPreferences | null;
         loading: boolean;
         error: Error | null;
         updateProfile: (data: Partial<UserProfile>) => Promise<void>;
         updatePreferences: (prefs: Partial<UserPreferences>) => Promise<void>;
         refetch: () => Promise<void>;
       }
       
       export function useUserProfile(): UseUserProfileReturn
       ```

    2. Create `classic/src/hooks/useThemeSync.ts`:
       - Syncs Docusaurus theme with user preferences
       - On login: apply saved theme
       - On theme change: save to preferences
       - Handles 'system' mode correctly
       ```typescript
       interface UseThemeSyncReturn {
         theme: 'light' | 'dark' | 'system';
         setTheme: (theme: 'light' | 'dark' | 'system') => void;
         synced: boolean;  // true when synced with server
       }
       
       export function useThemeSync(): UseThemeSyncReturn
       ```

    3. Integration points:
       - useAuth0() for auth state
       - useColorMode() for Docusaurus theme
       - getUserProfile/createOrUpdateProfile from services
       - getUserPreferences/updateUserPreferences from services

    Reference:
    - Docusaurus useColorMode hook
    - User preferences schema from Phase 31-01
  </action>
  <verify>
    <automated>npm run build --prefix classic 2>&1 | grep -q "Build successful" && echo "Build passes"</automated>
    <manual>Verify hooks export correctly</manual>
    <sampling_rate>Run after task 1 commits, before task 2 begins</sampling_rate>
  </verify>
  <done>
    - useUserProfile hook manages profile state
    - useThemeSync syncs theme with preferences
    - Auto-creation of profile on first login
    - Build passes
  </done>
</task>

<task type="auto">
  <name>task 2: Create profile and settings pages</name>
  <files>
    classic/src/pages/profile/index.tsx
    classic/src/pages/profile/settings.tsx
    classic/src/css/components/profile.css
  </files>
  <action>
    1. Create `classic/src/css/components/profile.css`:
       - Profile page layout
       - Settings form styling
       - Match existing site design (gold accent, glass cards)
       - Mobile responsive

    2. Create `classic/src/pages/profile/index.tsx`:
       - Shows user avatar, name, email
       - Shows role (if applicable)
       - Shows member since date
       - Links to settings, bookmarks, history
       - Uses ProtectedRoute pattern (must be logged in)
       - Redirects to login if not authenticated

    3. Create `classic/src/pages/profile/settings.tsx`:
       - Theme selector (light/dark/system)
       - Language preference (for future i18n)
       - Notification preferences (toggles)
       - Sidebar default state (collapsed/expanded)
       - Homepage view preference
       - Save button with loading state
       - Success/error toast notifications

    4. Layout structure:
       ```tsx
       <Layout>
         <ProfileHeader user={profile} />
         <SettingsForm 
           preferences={preferences}
           onSave={updatePreferences}
         />
       </Layout>
       ```

    Reference:
    - Existing page patterns from admin pages
    - Form patterns from admin/settings.tsx
  </action>
  <verify>
    <automated>npm run build --prefix classic 2>&1 | grep -q "Build successful" && echo "Build passes"</automated>
    <manual>
      1. Login and visit /profile
      2. Verify profile shows user info
      3. Click settings, change theme
      4. Verify theme changes and persists on refresh
    </manual>
    <sampling_rate>Run after task 2 commits, before task 3 begins</sampling_rate>
  </verify>
  <done>
    - Profile page shows user info
    - Settings page with theme toggle
    - Preferences save to Supabase
    - Theme syncs across devices
    - Build passes
  </done>
</task>

<task type="auto">
  <name>task 3: Update ProfileDropdown with links to profile/settings</name>
  <files>
    classic/src/components/Auth/ProfileDropdown.tsx
    classic/src/theme/Navbar/index.tsx
  </files>
  <action>
    1. Update `classic/src/components/Auth/ProfileDropdown.tsx`:
       - Add "Profile" link → /profile
       - Add "Settings" link → /profile/settings
       - Add "Bookmarks" link → /profile/bookmarks (Phase 32)
       - Add "History" link → /profile/history (Phase 32)
       - Add divider before logout
       - Show user avatar (or initials with colored background)
       - Show user name and email

    2. Ensure dropdown styling matches navbar:
       - Glassmorphism background
       - Gold accent on hover
       - Proper spacing
       - Mobile responsive

    3. Add loading state handling:
       - Show skeleton while profile loads
       - Don't block navbar render

    Reference:
    - Existing dropdown patterns
    - Navbar component structure
  </action>
  <verify>
    <automated>npm run build --prefix classic 2>&1 | grep -q "Build successful" && echo "Build passes"</automated>
    <manual>
      1. Login and click profile dropdown
      2. Verify Profile, Settings, Logout options
      3. Click Profile - navigate to /profile
      4. Click Settings - navigate to /profile/settings
    </manual>
    <sampling_rate>Run after task 3 commits</sampling_rate>
  </verify>
  <done>
    - ProfileDropdown shows user avatar and name
    - Links to /profile and /profile/settings
    - Logout works
    - Styling matches navbar
    - Build passes
  </done>
</task>

</tasks>

<verification>
**Automated:**
- Build passes
- All pages exist

**Manual:**
1. Login to docs site
2. Click profile dropdown in navbar
3. Click Profile - see profile page
4. Click Settings - see settings page
5. Change theme to 'dark'
6. Refresh page - verify theme persists
7. Open in different browser - verify theme synced
</verification>

<success_criteria>
1. Profile page displays user information
2. Settings page allows preference changes
3. Theme preference syncs across devices
4. Profile created automatically on first login
5. Dropdown links work correctly
</success_criteria>

<output>
After completion, create `.planning/phases/31-user-profile/31-02-SUMMARY.md`
</output>
