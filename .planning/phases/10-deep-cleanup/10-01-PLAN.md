---
phase: 10-deep-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - classic/.netlify/
  - classic/.storyblok/
  - classic/.vercel/
  - classic/_migrated_content/
  - content-staging/
  - Implementation plan/
  - classic/scripts/
  - scripts/
  - classic/*.md
  - classic/*.log
  - classic/*.txt
  - classic/*.json
  - classic/vercel.json
  - docusaurus.config.ts
autonomous: true
requirements:
  - CLEN-01 (extended)
  - ARCH-01

must_haves:
  truths:
    - "No dead CMS directories remain (netlify, storyblok, vercel, migrated_content)"
    - "No dead scripts remain (Hygraph, Strapi, Payload, old sync)"
    - "No build logs or temp files cluttering the repo"
    - "Build passes after all deletions"
  artifacts:
    - path: "classic/"
      provides: "Clean directory with only active files"
    - path: "scripts/"
      provides: "Only Sanity-related scripts remain"
  key_links: []
---

<objective>
Remove all dead CMS artifacts, legacy scripts, build logs, and documentation files accumulated from 4+ failed CMS attempts (Hygraph, Storyblok, Strapi, Payload) and platform migrations (Vercel, Netlify). This is a deep cleanup following Phase 1 surface cleanup.

Purpose: The codebase has accumulated ~150+ dead files totaling 2-3 MB across directories that were missed in Phase 1. These include:
- Hidden dot-directories (.netlify, .storyblok, .vercel)
- Old migration staging (content-staging/, Implementation plan/)
- Hygraph test scripts
- Strapi/Payload sync scripts
- 20+ build log files
- Old documentation and config files

Output: Clean codebase with only Sanity-active files. Build passes.
</objective>

<execution_context>
@C:/Users/abdel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/abdel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
Phase 1 cleanup removed Storyblok components and dead npm packages. This phase removes the remaining artifacts that don't affect the active Sanity-based build.

<inventory>
DIRECTORIES TO DELETE (7):
1. classic/.netlify/ - Netlify state directory
2. classic/.storyblok/ - Storyblok cache/types
3. classic/.vercel/ - Vercel project config
4. classic/_migrated_content/ - Old migration artifacts
5. content-staging/ - Entire old staging area (~100 files)
6. Implementation plan/ - Old Strapi implementation docs (10 files)

SCRIPTS TO DELETE - classic/scripts/ (4):
- test-single-image-upload.js (Hygraph)
- test-full-base64-upload.js (Hygraph)
- test-direct-asset-upload.js (Hygraph)
- test-create-article.js (Hygraph)

SCRIPTS TO DELETE - scripts/ (6):
- sync-strapi.js, seed-strapi.js (Strapi)
- sync-payload.js, sync-from-payload.js, import-to-payload.js (Payload)
- sync-new-articles.js (old sync)

CONFIG/DOCS TO DELETE in classic/:
- vercel.json, .vercelignore, netlify.toml.disabled
- algolia-config.json, image-mapping.json
- ALGOLIA_*.md (3 files)
- CMS_*.md (4 files)
- BUILD_FIX_SUMMARY.md, VERCEL_FIX.md, FIX_VERCEL_ROOT_DIR.md
- CLOUDINARY_*.md (2 files)
- E2E_*.md (6 files)
- DEPLOY_INSTRUCTIONS.md, REDEPLOY*.md, FIXES_COMPLETED.md
- SIDEBAR_GUIDE.md, EDITOR_*.md, MISSION_COMPLETE.md
- CONTENT_CLEANUP_COMPLETE.md, IMPLEMENTATION_SUMMARY.md
- RESTART_INSTRUCTIONS.md, TEST_QUICK_START.md
- device-links-raw.txt, link-report.txt

BUILD LOGS TO DELETE (all):
- build.log, build_error.txt, build_links.log
- build_log.txt, build_log_2.txt, build_log_3.txt
- build_log_cmd.txt, build_output.txt, clean.txt
- clean_build.log, deploy.log, error.log
- start_error.txt, output.txt, migration-log.json

CONFIG COMMENTS TO CLEAN:
- docusaurus.config.ts lines 119, 132, 147, 162, 177: "Disabled for Vercel builds"
</inventory>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead dot-directories and migration artifacts</name>
  <files>
    classic/.netlify/
    classic/.storyblok/
    classic/.vercel/
    classic/_migrated_content/
    content-staging/
    Implementation plan/
  </files>
  <action>
Delete all dead directories in one batch. These are entirely dead CMS/platform artifacts.

```bash
# Delete dead dot-directories
rm -rf classic/.netlify
rm -rf classic/.storyblok
rm -rf classic/.vercel
rm -rf classic/_migrated_content

# Delete dead staging/implementation directories
rm -rf "content-staging"
rm -rf "Implementation plan"
```

Verify deletion:
```bash
ls -la classic/.netlify classic/.storyblok classic/.vercel classic/_migrated_content 2>&1 | grep "No such file"
ls -la "content-staging" "Implementation plan" 2>&1 | grep "No such file"
```
  </action>
  <verify>
    <automated>test ! -d classic/.netlify && test ! -d classic/.storyblok && test ! -d classic/.vercel && test ! -d classic/_migrated_content && test ! -d content-staging && test ! -d "Implementation plan" && echo "PASS: all dead directories deleted"</automated>
  </verify>
  <done>
- classic/.netlify does not exist
- classic/.storyblok does not exist
- classic/.vercel does not exist
- classic/_migrated_content does not exist
- content-staging does not exist
- "Implementation plan" does not exist
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete dead Hygraph and Strapi/Payload scripts</name>
  <files>
    classic/scripts/test-single-image-upload.js
    classic/scripts/test-full-base64-upload.js
    classic/scripts/test-direct-asset-upload.js
    classic/scripts/test-create-article.js
    scripts/sync-strapi.js
    scripts/seed-strapi.js
    scripts/sync-payload.js
    scripts/sync-from-payload.js
    scripts/import-to-payload.js
    scripts/sync-new-articles.js
  </files>
  <action>
Delete all dead CMS-related scripts.

```bash
# Delete Hygraph test scripts
rm -f classic/scripts/test-single-image-upload.js
rm -f classic/scripts/test-full-base64-upload.js
rm -f classic/scripts/test-direct-asset-upload.js
rm -f classic/scripts/test-create-article.js

# Delete Strapi scripts
rm -f scripts/sync-strapi.js
rm -f scripts/seed-strapi.js

# Delete Payload scripts
rm -f scripts/sync-payload.js
rm -f scripts/sync-from-payload.js
rm -f scripts/import-to-payload.js

# Delete old sync script
rm -f scripts/sync-new-articles.js
```
  </action>
  <verify>
    <automated>test ! -f classic/scripts/test-single-image-upload.js && test ! -f classic/scripts/test-create-article.js && test ! -f scripts/sync-strapi.js && test ! -f scripts/sync-payload.js && echo "PASS: all dead scripts deleted"</automated>
  </verify>
  <done>
- All 4 Hygraph test scripts deleted
- All 6 Strapi/Payload scripts deleted
- scripts/ directory contains only Sanity-active scripts
  </done>
</task>

<task type="auto">
  <name>Task 3: Delete dead config files and documentation</name>
  <files>
    classic/vercel.json
    classic/.vercelignore
    classic/netlify.toml.disabled
    classic/algolia-config.json
    classic/image-mapping.json
    classic/ALGOLIA_CREDENTIALS.md
    classic/ALGOLIA_CREDENTIALS_CHECK.md
    classic/ALGOLIA_EVENTS_SETUP.md
    classic/CMS_CHANGES.md
    classic/CMS_FINAL_UPDATES.md
    classic/CMS_GUIDE.md
    classic/CMS_SETUP.md
    classic/BUILD_FIX_SUMMARY.md
    classic/VERCEL_FIX.md
    classic/FIX_VERCEL_ROOT_DIR.md
    classic/CLOUDINARY_SETUP.md
    classic/CLOUDINARY_VIDEO_SETUP.md
    classic/E2E_CHECK_REPORT.md
    classic/E2E_COMPLETION_SUMMARY.md
    classic/E2E_FIXES_COMPLETE.md
    classic/E2E_TEST_SETUP_SUMMARY.md
    classic/E2E_TESTING.md
    classic/DEPLOY_INSTRUCTIONS.md
    classic/REDEPLOY.md
    classic/REDEPLOY_COMMANDS.md
    classic/FIXES_COMPLETED.md
    classic/SIDEBAR_GUIDE.md
    classic/EDITOR_AND_SIDEBAR_SUMMARY.md
    classic/MISSION_COMPLETE.md
    classic/CONTENT_CLEANUP_COMPLETE.md
    classic/IMPLEMENTATION_SUMMARY.md
    classic/RESTART_INSTRUCTIONS.md
    classic/TEST_QUICK_START.md
    classic/content_structure.md
    classic/device-links-raw.txt
    classic/link-report.txt
  </files>
  <action>
Delete all dead config and documentation files in one batch.

```bash
cd classic

# Config files
rm -f vercel.json .vercelignore netlify.toml.disabled
rm -f algolia-config.json image-mapping.json

# Algolia docs
rm -f ALGOLIA_CREDENTIALS.md ALGOLIA_CREDENTIALS_CHECK.md ALGOLIA_EVENTS_SETUP.md

# CMS docs
rm -f CMS_CHANGES.md CMS_FINAL_UPDATES.md CMS_GUIDE.md CMS_SETUP.md

# Build/deploy docs
rm -f BUILD_FIX_SUMMARY.md VERCEL_FIX.md FIX_VERCEL_ROOT_DIR.md
rm -f CLOUDINARY_SETUP.md CLOUDINARY_VIDEO_SETUP.md
rm -f E2E_CHECK_REPORT.md E2E_COMPLETION_SUMMARY.md E2E_FIXES_COMPLETE.md
rm -f E2E_TEST_SETUP_SUMMARY.md E2E_TESTING.md
rm -f DEPLOY_INSTRUCTIONS.md REDEPLOY.md REDEPLOY_COMMANDS.md FIXES_COMPLETED.md

# Misc docs
rm -f SIDEBAR_GUIDE.md EDITOR_AND_SIDEBAR_SUMMARY.md MISSION_COMPLETE.md
rm -f CONTENT_CLEANUP_COMPLETE.md IMPLEMENTATION_SUMMARY.md
rm -f RESTART_INSTRUCTIONS.md TEST_QUICK_START.md content_structure.md

# Data files
rm -f device-links-raw.txt link-report.txt
```
  </action>
  <verify>
    <automated>test ! -f classic/vercel.json && test ! -f classic/netlify.toml.disabled && test ! -f classic/CMS_SETUP.md && echo "PASS: dead config/docs deleted"</automated>
  </verify>
  <done>
- All dead config files deleted
- All dead documentation files deleted
- Only active files remain in classic/
  </done>
</task>

<task type="auto">
  <name>Task 4: Delete all build logs and temp files</name>
  <files>
    classic/build.log
    classic/build_error.txt
    classic/build_links.log
    classic/build_log.txt
    classic/build_log_2.txt
    classic/build_log_3.txt
    classic/build_log_cmd.txt
    classic/build_output.txt
    classic/clean.txt
    classic/clean_build.log
    classic/deploy.log
    classic/error.log
    classic/start_error.txt
    classic/output.txt
    classic/migration-log.json
  </files>
  <action>
Delete all build log files and temporary output files.

```bash
cd classic

# Build logs
rm -f build.log build_error.txt build_links.log
rm -f build_log.txt build_log_2.txt build_log_3.txt
rm -f build_log_cmd.txt build_output.txt
rm -f clean.txt clean_build.log deploy.log error.log
rm -f start_error.txt output.txt migration-log.json
```

These are all temporary files that accumulate during development and have no value in the repo.
  </action>
  <verify>
    <automated>ls classic/*.log classic/*.txt 2>/dev/null | wc -l | xargs -I {} bash -c 'if [ {} -eq 0 ]; then echo "PASS: no log/txt files remain"; else echo "FAIL: {} log/txt files remain"; fi'</automated>
  </verify>
  <done>
- No .log files in classic/
- No .txt files in classic/
- No migration-log.json
  </done>
</task>

<task type="auto">
  <name>Task 5: Clean Vercel comments from docusaurus.config.ts</name>
  <files>
    classic/docusaurus.config.ts
  </files>
  <action>
Remove the "// Disabled for Vercel builds (no git access)" comments from the config file since we're on Cloudflare Pages.

Open classic/docusaurus.config.ts and find lines with this comment pattern, then remove just the comment part while keeping the config value.

The pattern appears at lines ~119, 132, 147, 162, 177:
```ts
showLastUpdateTime: false, // Disabled for Vercel builds (no git access)
```

Change to:
```ts
showLastUpdateTime: false,
```

This is a comment cleanup only - no functional change.
  </action>
  <verify>
    <automated>grep -c "Vercel builds" classic/docusaurus.config.ts || echo "0"</automated>
  </verify>
  <done>
- grep -c "Vercel builds" classic/docusaurus.config.ts returns 0
- Config file is clean of dead platform references
  </done>
</task>

<task type="auto">
  <name>Task 6: Verify build passes and run final verification</name>
  <files>
    classic/
  </files>
  <action>
Run full build to verify nothing was broken by the deletions.

```bash
cd classic && npm run build
```

Then verify the cleanup is complete:
```bash
# Count remaining .md files in classic/ (should be minimal)
ls classic/*.md 2>/dev/null | wc -l

# Count remaining .log/.txt files (should be 0)
ls classic/*.log classic/*.txt 2>/dev/null | wc -l

# Verify no dead directories
ls -d classic/.netlify classic/.storyblok classic/.vercel 2>&1 | grep "No such file"
```
  </action>
  <verify>
    <automated>cd classic && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
- npm run build exits 0
- No TypeScript errors
- No missing file errors
- Build output shows "Generated static files"
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

```bash
# Verify no dead directories
test ! -d classic/.netlify && test ! -d classic/.storyblok && test ! -d classic/.vercel && echo "PASS: dead dot-dirs gone"

# Verify no dead top-level dirs
test ! -d content-staging && test ! -d "Implementation plan" && echo "PASS: dead dirs gone"

# Count remaining scripts
ls classic/scripts/*.js | wc -l  # Should be ~15 (Sanity-related only)
ls scripts/*.js | wc -l  # Should be ~9 (Sanity-related only)

# Full build
cd classic && npm run build
```
</verification>

<success_criteria>
- All 7 dead directories deleted
- All 10 dead scripts deleted
- All 35+ dead config/docs deleted
- All 15 build logs deleted
- Vercel comments removed from config
- npm run build exits 0
- No module-not-found errors
</success_criteria>

<output>
After completion, create `.planning/phases/10-deep-cleanup/10-01-SUMMARY.md` following the summary template.
Include counts: directories deleted, files deleted, disk space freed.
</output>
