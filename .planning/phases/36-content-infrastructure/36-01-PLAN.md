---
phase: 36-content-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - studio/schemaTypes/fields/product.ts
  - studio/schemaTypes/doc.ts
  - studio/schemaTypes/release.ts
  - studio/schemaTypes/roadmapItem.ts
  - studio/schemaTypes/article.ts
  - studio/schemaTypes/landingPage.ts
  - studio/schemaTypes/sidebarConfig.ts
  - studio/schemaTypes/update.ts
  - studio/schemaTypes/deviceIntegration.ts
  - studio/scripts/migrate-product-field.ts
autonomous: false
requirements:
  - CONT-01
  - CONT-02
  - CONT-04
user_setup: []

must_haves:
  truths:
    - "Every Sanity document has a product field with enum value (gcxone, gcsurge, shared)"
    - "Editor can select product from radio buttons in Sanity Studio"
    - "All existing documents have product field populated (no undefined values)"
    - "Preview shows product badge next to document title"
  artifacts:
    - path: "studio/schemaTypes/fields/product.ts"
      provides: "Reusable product field definition with enum and validation"
      min_lines: 20
    - path: "studio/scripts/migrate-product-field.ts"
      provides: "Migration script to backfill existing content"
      exports: ["migrateProductField"]
  key_links:
    - from: "studio/schemaTypes/doc.ts"
      to: "studio/schemaTypes/fields/product.ts"
      via: "import { productField } from './fields/product'"
      pattern: "import.*productField"
---

<objective>
Add required product field to all Sanity document schemas and create migration script to backfill existing content with product=gcxone.

Purpose: Enable multi-product content ownership. Every document must belong to a product (or be shared). This is the foundation for content isolation between GCXONE and GCSurge.

Output: All 8 document schemas modified with product field, migration script created, backfill verified.
</objective>

<execution_context>
@~\.config\opencode/get-shit-done/workflows/execute-plan.md
@~\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md

# Existing schema patterns
@studio/schemaTypes/doc.ts
@studio/schemaTypes/release.ts
@studio/schemaTypes/update.ts

# Phase 29 patterns (type enum with emoji badges)
</context>

<tasks>

<task type="auto">
  <name>task 1: Create reusable product field definition</name>
  <files>studio/schemaTypes/fields/product.ts</files>
  <action>
Create a reusable Sanity field definition for the product enum. This follows the pattern from ARCHITECTURE.md Pattern 3.

The field should:
1. Export a `productField` object with `defineField` from 'sanity'
2. Name: 'product', type: 'string', title: 'Product'
3. Options list with radio layout:
   - { title: 'GCXONE', value: 'gcxone' }
   - { title: 'GC Surge', value: 'gcsurge' }
   - { title: 'Shared (All Products)', value: 'shared' }
4. Validation: Rule.required()
5. Initial value: 'gcxone' (existing content defaults to GCXONE)
6. Description: "Which product(s) this content belongs to. Use 'shared' for content that appears in all products."

This field will be imported and spread into all document schemas.
  </action>
  <verify>
    <automated>test -f studio/schemaTypes/fields/product.ts && grep -q "productField" studio/schemaTypes/fields/product.ts && grep -q "gcxone" studio/schemaTypes/fields/product.ts && grep -q "gcsurge" studio/schemaTypes/fields/product.ts && grep -q "shared" studio/schemaTypes/fields/product.ts && echo "OK" || echo "FAIL"</automated>
    <manual>Open Sanity Studio, verify product field appears with radio options</manual>
    <sampling_rate>run after task commits</sampling_rate>
  </verify>
  <done>Product field definition file exists with enum values gcxone, gcsurge, shared and required validation</done>
</task>

<task type="auto">
  <name>task 2: Add product field to all document schemas</name>
  <files>
    studio/schemaTypes/doc.ts
    studio/schemaTypes/release.ts
    studio/schemaTypes/roadmapItem.ts
    studio/schemaTypes/article.ts
    studio/schemaTypes/landingPage.ts
    studio/schemaTypes/sidebarConfig.ts
    studio/schemaTypes/update.ts
    studio/schemaTypes/deviceIntegration.ts
  </files>
  <action>
Import the product field and add it to all 8 document schemas. For each schema:

1. Add import at top: `import {productField} from './fields/product'`
2. Add the product field after the core fields (title, slug) in the fields array using spread: `productField,`
3. Update the preview configuration to show product badge in subtitle

For preview updates, follow existing pattern from doc.ts (statusEmoji + subtitle pattern):
```typescript
preview: {
  select: {
    title: 'title',
    status: 'status',
    product: 'product',
    // ... other existing fields
  },
  prepare({title, status, product, ...}) {
    const productLabel = product?.toUpperCase() || 'NO PRODUCT';
    return {
      title: `${emoji} ${title ?? 'Untitled'}`,
      subtitle: `${productLabel} - ${status || 'draft'}`,
    };
  },
}
```

Schemas to modify:
- doc.ts: Add after slug field, update preview
- release.ts: Add after slug field, update preview
- roadmapItem.ts: Add after description field, update preview
- article.ts: Add after slug field, update preview
- landingPage.ts: Add after slug field, update preview
- sidebarConfig.ts (sidebarCategoryType): Add after slug field, update preview
- update.ts: Add after slug field, update preview
- deviceIntegration.ts: Add after slug field, update preview

IMPORTANT: Do NOT add product field to sidebarConfigType (the config document) - only add to sidebarCategoryType.
  </action>
  <verify>
    <automated>grep -l "productField" studio/schemaTypes/doc.ts studio/schemaTypes/release.ts studio/schemaTypes/roadmapItem.ts studio/schemaTypes/article.ts studio/schemaTypes/landingPage.ts studio/schemaTypes/sidebarConfig.ts studio/schemaTypes/update.ts studio/schemaTypes/deviceIntegration.ts | wc -l | grep -q "8" && echo "OK - all 8 schemas have productField" || echo "FAIL - missing schemas"</automated>
    <manual>Open Sanity Studio, create new doc, verify product field appears with radio options and default gcxone</manual>
    <sampling_rate>run after task commits</sampling_rate>
  </verify>
  <done>All 8 document schemas have product field imported and configured with preview showing product badge</done>
</task>

<task type="auto">
  <name>task 3: Create migration script to backfill existing content</name>
  <files>studio/scripts/migrate-product-field.ts</files>
  <action>
Create a Node.js migration script that:
1. Connects to Sanity using @sanity/client
2. Reads environment variables: SANITY_PROJECT_ID, SANITY_DATASET, SANITY_API_TOKEN
3. For each document type (doc, release, roadmapItem, article, landingPage, sidebarCategory, update, deviceIntegration):
   - Query: `*[_type == "${type}" && !defined(product)]`
   - Patch each document with: `.set({product: 'gcxone'})`
   - Log count of documents migrated
4. After migration, verify with count query: `*[_type == "${type}" && !defined(product)]` returns 0 for all types
5. Exit with success code if all counts are 0, error code otherwise

Use the existing pattern from fetch-sanity-content.js for Sanity client setup:
```typescript
const client = createClient({
  projectId: process.env.SANITY_PROJECT_ID,
  dataset: process.env.SANITY_DATASET || 'production',
  apiVersion: '2025-02-06',
  useCdn: false,
  token: process.env.SANITY_API_TOKEN,
});
```

The script should be runnable with: `npx tsx studio/scripts/migrate-product-field.ts`

Include dry-run mode: `--dry-run` flag shows what would be migrated without making changes.
  </action>
  <verify>
    <automated>test -f studio/scripts/migrate-product-field.ts && grep -q "createClient" studio/scripts/migrate-product-field.ts && grep -q "product.*gcxone" studio/scripts/migrate-product-field.ts && echo "OK" || echo "FAIL"</automated>
    <manual>Run migration script, verify output shows 0 documents without product field</manual>
    <sampling_rate>run after task commits, before Wave 2 begins</sampling_rate>
  </verify>
  <done>Migration script exists and can backfill all existing documents with product=gcxone</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>task 4: Verify schema changes and migration</name>
  <files>studio/schemaTypes/fields/product.ts</files>
  <action>
    Human verification checkpoint. Start Sanity Studio and verify the product field implementation.

    Steps:
    1. Open Sanity Studio: `cd studio && npm run dev`
    2. Create a new document of each type (doc, release, article, etc.)
    3. Verify "Product" field appears with radio buttons: GCXONE, GC Surge, Shared
    4. Verify default selection is GCXONE
    5. Run migration script: `npx tsx studio/scripts/migrate-product-field.ts`
    6. Verify output shows all existing documents backfilled
    7. Run migration again to verify 0 documents need migration

    Confirm all 7 checks pass before proceeding.
  </action>
  <verify>Human confirms all 7 verification steps pass</verify>
  <done>
    - Product field visible in all document types
    - Migration script backfills existing content
    - Ready to proceed to Plan 02 (GROQ filtering)
  </done>
</task>

</tasks>

<verification>
1. All 8 schemas import productField
2. All schemas have product in preview prepare function
3. Migration script runs without errors
4. GROQ Vision query `*[_type == "doc" && !defined(product)]` returns 0 results
</verification>

<success_criteria>
- CONT-01: Every Sanity document has required product field with enum values
- CONT-02: Editor can assign content to a product via radio buttons in Studio
- CONT-04: Migration script backfills all existing content with product=gcxone
</success_criteria>

<output>
After completion, create `.planning/phases/36-content-infrastructure/36-01-SUMMARY.md`
</output>
