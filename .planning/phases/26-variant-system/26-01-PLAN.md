---
phase: 26-variant-system
plan: 01
type: execute
wave: 1
depends_on: [25-02]
files_modified: [classic/package.json, classic/src/components/ui/button.tsx, classic/src/components/ui/index.ts]
autonomous: true
requirements: [CVA-01, CVA-02, CVA-03]
user_setup: []

must_haves:
  truths:
    - "class-variance-authority is installed and available"
    - "src/components/ui/ directory exists"
    - "Button component has 4 variants (primary, secondary, ghost, destructive)"
    - "Button component has 3 sizes (sm, md, lg)"
  artifacts:
    - path: "classic/src/components/ui/button.tsx"
      provides: "Type-safe Button component with variants"
      contains: "buttonVariants"
      min_lines: 40
    - path: "classic/src/components/ui/index.ts"
      provides: "UI primitives barrel export"
      contains: "export"
  key_links:
    - from: "classic/src/components/ui/button.tsx"
      to: "class-variance-authority"
      via: "import { cva }"
      pattern: "from 'class-variance-authority'"
---

<objective>
Install CVA and create the Button primitive with type-safe variants.

Purpose: Establish a consistent, type-safe component API. No more hunting through CSS for button styles.
Output: Button component with 4 variants and 3 sizes, ready for migration.
</objective>

<execution_context>
@~\.config\opencode/get-shit-done/workflows/execute-plan.md
@~\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP-design-system.md
@.planning/STATE.md
@DESIGN_SYSTEM_ANALYSIS_AND_IMPLEMENTATION_PLAN.md
@classic/package.json
</context>

<tasks>

<task type="auto">
  <name>task 1: Install class-variance-authority</name>
  <files>classic/package.json</files>
  <action>
    Install class-variance-authority:
    
    ```bash
    cd classic && npm install class-variance-authority
    ```
    
    Verify installation:
    ```bash
    npm list class-variance-authority
    ```
  </action>
  <verify>
    <automated>cd classic && npm list class-variance-authority 2>&1 | grep -c "class-variance-authority"</automated>
    <manual>Verify CVA is installed</manual>
    <sampling_rate>run after task commits</sampling_rate>
  </verify>
  <done>class-variance-authority installed and listed in package.json</done>
</task>

<task type="auto">
  <name>task 2: Create src/components/ui/ directory and Button component</name>
  <files>classic/src/components/ui/button.tsx, classic/src/components/ui/index.ts</files>
  <action>
    Create the UI primitives directory and Button component:
    
    1. Create directory:
       ```bash
       mkdir -p classic/src/components/ui
       ```
    
    2. Create `classic/src/components/ui/button.tsx`:
       ```tsx
       import { cva, type VariantProps } from 'class-variance-authority';
       import { clsx } from 'clsx';
       import type { ButtonHTMLAttributes } from 'react';
       
       const buttonVariants = cva(
         'inline-flex items-center justify-center font-medium transition-all rounded-full focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-offset-2 disabled:pointer-events-none disabled:opacity-50',
         {
           variants: {
             variant: {
               primary: 'bg-[--ifm-color-primary] text-white hover:brightness-110 active:brightness-95',
               secondary: 'bg-[--glass-bg] border border-[--glass-border] text-[--text-primary] hover:bg-[--glass-bg-hover] hover:border-[--glass-border-hover]',
               ghost: 'text-[--gold-text] hover:bg-[--gold-bg] active:bg-[--gold-bg-hover]',
               destructive: 'bg-[--color-danger] text-white hover:brightness-110 active:brightness-95',
             },
             size: {
               sm: 'h-8 px-3 text-sm',
               md: 'h-10 px-4 text-base',
               lg: 'h-11 px-6 text-lg',
             },
           },
           defaultVariants: {
             variant: 'primary',
             size: 'md',
           },
         }
       );
       
       export interface ButtonProps
         extends ButtonHTMLAttributes<HTMLButtonElement>,
           VariantProps<typeof buttonVariants> {}
       
       export function Button({ className, variant, size, ...props }: ButtonProps) {
         return (
           <button
             className={clsx(buttonVariants({ variant, size }), className)}
             {...props}
           />
         );
       }
       
       export { buttonVariants };
       ```
    
    3. Create `classic/src/components/ui/index.ts`:
       ```tsx
       export { Button, buttonVariants, type ButtonProps } from './button';
       ```
  </action>
  <verify>
    <automated>test -f classic/src/components/ui/button.tsx && test -f classic/src/components/ui/index.ts && echo "Files created"</automated>
    <manual>Verify UI primitives directory and Button component exist</manual>
    <sampling_rate>run after task commits</sampling_rate>
  </verify>
  <done>Button component created with 4 variants and 3 sizes</done>
</task>

<task type="auto">
  <name>task 3: Verify Button component builds correctly</name>
  <files>classic/src/components/ui/button.tsx</files>
  <action>
    Verify the Button component builds without errors:
    
    ```bash
    cd classic && npm run build 2>&1 | head -50
    ```
    
    If TypeScript errors occur:
    - Check that all CSS variable references are valid (they should be from tokens.css)
    - Check that clsx import is correct (already installed)
    - Fix any type errors in VariantProps usage
    
    Also run typecheck:
    ```bash
    cd classic && npm run typecheck
    ```
  </action>
  <verify>
    <automated>cd classic && npm run build 2>&1 | grep -c "SUCCESS"</automated>
    <manual>Verify build passes with new Button component</manual>
    <sampling_rate>run after task commits</sampling_rate>
  </verify>
  <done>Button component builds successfully, no TypeScript errors</done>
</task>

</tasks>

<verification>
1. `npm list class-variance-authority` shows installed
2. `test -f classic/src/components/ui/button.tsx` passes
3. `test -f classic/src/components/ui/index.ts` passes
4. `npm run build` exits 0
5. `npm run typecheck` exits 0
</verification>

<success_criteria>
- class-variance-authority installed
- src/components/ui/ directory exists
- Button component has 4 variants and 3 sizes
- Build and typecheck pass
</success_criteria>

<output>
After completion, create `.planning/phases/26-variant-system/26-01-SUMMARY.md`
</output>
