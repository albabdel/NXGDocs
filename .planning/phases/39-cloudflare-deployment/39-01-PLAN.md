---
phase: 39-cloudflare-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [wrangler.toml, scripts/deploy-multi-product.js, scripts/create-cloudflare-projects.js]
autonomous: true
requirements: [MPROD-03, DOM-01, DOM-02]
user_setup:
  - service: cloudflare
    why: "Domain DNS configuration for docs.gcxone.com and docs.gcsurge.com"
    env_vars:
      - name: CLOUDFLARE_API_TOKEN
        source: "keys.md - Edit Cloudflare Workers token"
    dashboard_config:
      - task: "Add DNS records for docs.gcxone.com and docs.gcsurge.com pointing to Cloudflare Pages"
        location: "Cloudflare Dashboard -> DNS -> Add Record (CNAME to gcxone-docs.pages.dev)"
      - task: "Verify domain ownership"
        location: "Cloudflare Dashboard -> SSL/TLS -> Verify certificates"

must_haves:
  truths:
    - "Two separate Cloudflare Pages projects exist (gcxone-docs, gcsurge-docs)"
    - "docs.gcxone.com is configured as custom domain for gcxone-docs project"
    - "docs.gcsurge.com is configured as custom domain for gcsurge-docs project"
    - "Each project has PRODUCT environment variable set correctly"
    - "Each project builds from correct output directory"
  artifacts:
    - path: "wrangler.toml"
      provides: "Multi-project Cloudflare configuration"
      contains: "pages_build_output_dir"
    - path: "scripts/create-cloudflare-projects.js"
      provides: "Automated project creation script"
      exports: ["createPagesProject", "configureCustomDomain"]
    - path: "scripts/deploy-multi-product.js"
      provides: "Deployment orchestration"
      exports: ["deployProduct"]
  key_links:
    - from: "Cloudflare Pages Project"
      to: "PRODUCT env var"
      via: "project configuration"
      pattern: "PRODUCT=gcxone|gcsurge"
    - from: "Cloudflare Pages Project"
      to: "Build output"
      via: "build output directory"
      pattern: "build/(gcxone|gcsurge)"
---

<objective>
Create separate Cloudflare Pages projects for each product domain with proper environment configuration and custom domains.

Purpose: Enable product-specific deployments where each product has its own domain, build output, and environment configuration. This provides isolation between products and allows independent rebuilds.

Output: Two Cloudflare Pages projects (gcxone-docs, gcsurge-docs) configured with custom domains and product-specific environment variables.
</objective>

<execution_context>
@~\.config\opencode/get-shit-done/workflows/execute-plan.md
@~\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/ARCHITECTURE.md
@.planning/research/multi-product-FEATURES.md
@wrangler.toml
@keys.md

# Cloudflare Account Info
- Account ID: ff9df0a2daf8c9eb1032f67dd551c784
- Existing domain: gcxone.pages.dev
- Token: cfat_3U8hBMWTxsZzs2JynzDAWgfNgx1Io9on5r78wlMv1e218039 (Edit Cloudflare Workers)
</context>

<tasks>

<task type="auto">
  <name>task 1: Create Cloudflare Pages project creation script</name>
  <files>scripts/create-cloudflare-projects.js</files>
  <action>
Create a Node.js script that automates Cloudflare Pages project creation using the Cloudflare API.

The script should:
1. Use the Cloudflare API with the provided token (cfat_3U8hBMWTxsZzs2JynzDAWgfNgx1Io9on5r78wlMv1e218039)
2. Account ID: ff9df0a2daf8c9eb1032f67dd551c784
3. Create two Pages projects:
   - `gcxone-docs` with production branch from GitLab
   - `gcsurge-docs` with production branch from GitLab
4. Configure each project with:
   - Build command: `node scripts/build-product.js` (Phase 37 creates this)
   - Build output directory: `classic/build/{product}` (e.g., `classic/build/gcxone`)
   - Root directory: `/` (project root)
5. Set environment variables per project:
   - SANITY_PROJECT_ID: fjjuacab
   - SANITY_DATASET: production
   - SANITY_API_TOKEN: (from Cloudflare secrets)
   - PRODUCT: gcxone (for gcxone-docs) or gcsurge (for gcsurge-docs)

Use the Cloudflare Pages API:
- POST https://api.cloudflare.com/client/v4/accounts/{account_id}/pages/projects
- For project creation and configuration

Handle idempotency - if project already exists, update configuration instead of failing.

DO NOT hardcode secrets in the script - read from environment variables.
  </action>
  <verify>
    <automated>node scripts/create-cloudflare-projects.js --dry-run 2>&1 | Select-String "gcxone-docs|gcsurge-docs"</automated>
    <manual>Script runs without errors and outputs project creation plan</manual>
  </verify>
  <done>Script exists at scripts/create-cloudflare-projects.js and can create/update Pages projects via Cloudflare API</done>
</task>

<task type="auto">
  <name>task 2: Configure custom domains for each project</name>
  <files>scripts/create-cloudflare-projects.js, scripts/configure-domains.js</files>
  <action>
Extend the project creation script or create a separate domain configuration script.

Configure custom domains:
1. `docs.gcxone.com` -> gcxone-docs Pages project
2. `docs.gcsurge.com` -> gcsurge-docs Pages project

The script should:
1. Add custom domain to each Pages project using Cloudflare API:
   - POST https://api.cloudflare.com/client/v4/accounts/{account_id}/pages/projects/{project_name}/domains
2. Verify DNS configuration requirements and output instructions
3. Check SSL certificate status

If domains are not yet configured in Cloudflare DNS:
- Output DNS record requirements (CNAME records pointing to respective pages.dev URLs)
- User must configure DNS manually before domains become active

DNS Configuration for user (document in script output):
- docs.gcxone.com -> CNAME -> gcxone-docs.pages.dev
- docs.gcsurge.com -> CNAME -> gcsurge-docs.pages.dev
  </action>
  <verify>
    <automated>node scripts/configure-domains.js --check 2>&1 | Select-String "docs.gcxone.com|docs.gcsurge.com"</automated>
    <manual>Script outputs domain configuration status and DNS requirements</manual>
  </verify>
  <done>Custom domains configured or DNS requirements documented for manual configuration</done>
</task>

<task type="auto">
  <name>task 3: Create multi-project deployment script</name>
  <files>scripts/deploy-multi-product.js, wrangler.toml</files>
  <action>
Create a deployment script that orchestrates deploying each product to its respective Cloudflare Pages project.

The script should:
1. Build each product using the multi-build pipeline from Phase 37
2. Deploy each build output to the correct Pages project:
   - build/gcxone -> gcxone-docs project
   - build/gcsurge -> gcsurge-docs project
3. Use wrangler for deployment:
   - `npx wrangler pages deploy build/gcxone --project-name=gcxone-docs`
   - `npx wrangler pages deploy build/gcsurge --project-name=gcsurge-docs`
4. Support deploying a single product via --product flag
5. Support deploying all products (default behavior)
6. Output deployment URLs and status

Update wrangler.toml to support multi-project configuration:
```toml
# Multi-project configuration
name = "gcxone-docs"
compatibility_date = "2024-01-01"

# Environment-specific settings are in Cloudflare Pages dashboard
# This file is for local development defaults
```

The script should:
- Check that builds exist before deploying
- Handle deployment failures gracefully
- Provide clear output about what was deployed where
  </action>
  <verify>
    <automated>node scripts/deploy-multi-product.js --help 2>&1</automated>
    <manual>Script shows help with --product flag and deployment options</manual>
  </verify>
  <done>Deployment script can deploy single or all products to their respective Pages projects</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>Cloudflare Pages project creation scripts, domain configuration, and multi-project deployment script</what-built>
  <how-to-verify>
    1. Run `node scripts/create-cloudflare-projects.js` to create projects
    2. Run `node scripts/configure-domains.js` to configure custom domains
    3. Configure DNS records in Cloudflare dashboard:
       - docs.gcxone.com CNAME -> gcxone-docs.pages.dev
       - docs.gcsurge.com CNAME -> gcsurge-docs.pages.dev
    4. Verify SSL certificates are provisioned
    5. Visit https://dash.cloudflare.com/{account_id}/pages to see both projects
  </how-to-verify>
  <resume-signal>Type "approved" once both projects are created and domains are accessible, or describe any issues encountered</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:
1. Two Cloudflare Pages projects exist: gcxone-docs and gcsurge-docs
2. Each project has PRODUCT environment variable set
3. Custom domains are configured (pending DNS)
4. Deployment script can deploy to correct projects
</verification>

<success_criteria>
- [ ] gcxone-docs Pages project exists in Cloudflare dashboard
- [ ] gcsurge-docs Pages project exists in Cloudflare dashboard
- [ ] docs.gcxone.com domain configured for gcxone-docs
- [ ] docs.gcsurge.com domain configured for gcsurge-docs
- [ ] PRODUCT environment variable set correctly in each project
- [ ] deploy-multi-product.js script can deploy to both projects
</success_criteria>

<output>
After completion, create `.planning/phases/39-cloudflare-deployment/39-01-SUMMARY.md`
</output>
