---
phase: 05-polish
plan: 06
type: execute
wave: 2
depends_on: [05-04, 05-05]
files_modified:
  - classic/src/css/custom.css
autonomous: false
gap_closure: true
requirements: [PLSH-01, PLSH-02]

must_haves:
  truths:
    - "All hardcoded #E8B058 instances visible in light mode have an accessible override or are non-text decorative elements"
    - "index.tsx Quick Start h2 reads as #996B1F (not #E8B058) in light mode"
    - "index.tsx Releases h2 reads as #996B1F (not #E8B058) in light mode"
    - "index.tsx View All Releases link reads as #996B1F on hover in light mode"
    - "Human approves Phase 5 visual sign-off: hero, contrast, VoC modal, search UI"
  artifacts:
    - path: "classic/src/css/custom.css"
      provides: "Any additional light-mode overrides identified during sweep"
      contains: "data-theme='light'"
  key_links:
    - from: "[data-theme='light'] CSS overrides"
      to: "index.tsx text-[#E8B058] elements"
      via: "custom.css existing override at ~line 1753"
      pattern: "data-theme.*light.*E8B058"
---

<objective>
Complete the light mode contrast sweep and obtain human Phase 5 sign-off.

Purpose: This is the final Phase 5 gap-closure plan. Plan 03's checkpoint was rejected for three reasons: (1) Pagefind migration incomplete (fixed in 05-04), (2) hero light mode broken (fixed in 05-05), (3) systematic light mode contrast sweep not done and human sign-off withheld. This plan performs the sweep and ends with the human verification checkpoint.

Output: Confirmed that all gold color instances on the home page path are either already covered by custom.css overrides or are decorative (non-text) elements that do not need WCAG contrast treatment. Any gaps found are fixed. Human approves the final Phase 5 sign-off.
</objective>

<execution_context>
@C:/Users/abdel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/abdel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish/05-03-SUMMARY.md
@.planning/phases/05-polish/05-04-SUMMARY.md
@.planning/phases/05-polish/05-05-SUMMARY.md

<interfaces>
<!-- Key facts the executor needs — no codebase exploration required. -->

Current state of #E8B058 in index.tsx (4 instances — all analyzed):

  Line 205: h2 className "text-[#E8B058]" → Quick Start heading
    STATUS: COVERED — custom.css line 1753: [data-theme='light'] .text-\[\#E8B058\] { color: #996B1F !important; }

  Line 246: h2 className "text-[#E8B058]" → Releases heading
    STATUS: COVERED — same override at custom.css line 1753

  Line 251: className "text-[#E8B058] hover:text-[#D4A047]" → View All Releases link
    STATUS: COVERED — custom.css line 1753 (text) + line 1782 (hover:text-[#D4A047] → var(--nxgen-gold-accessible-hover))

  Line 316: className "bg-gradient-to-r from-[#E8B058]/5 via-transparent to-[#E8B058]/5"
    STATUS: DECORATIVE — 5% opacity gold background tint on Support CTA section.
    Not text. Not a contrast concern. No override needed.

Components rendered on the home page path (confirmed via index.tsx imports):
  - NXGENSphereHero: #E8B058 instances are all gated isDark OR use !isDark with #996B1F directly
    (hero "Reimagined." uses text-[#996B1F] in light, "By NXGEN" uses text-[#E8B058] covered by CSS override)
  - FeatureCard: NO hardcoded gold colors (confirmed by grep)
  - QuickLink: NO hardcoded gold colors (confirmed by grep)
  - BreakthroughGrid: NO hardcoded gold colors (confirmed by grep)

Components NOT on the home page path (not rendered by index.tsx):
  - GettingStarted/* (Checklist, ContextHeader, NextRequiredAction, PrimaryOnboardingPath, RoleModeSelector, SideLearningPanel)
  - GradientText/index.tsx
  These components have hardcoded #E8B058 but they are NOT light mode concerns for Phase 5 scope.

CSS overrides already in custom.css (confirmed in VERIFICATION.md):
  - line 1753: [data-theme='light'] .text-\[\#E8B058\] → #996B1F ✓
  - line 1757: [data-theme='light'] .bg-\[\#E8B058\]\/10 ✓
  - line 1761: [data-theme='light'] .border-\[\#E8B058\]\/20 ✓
  - line 1765: [data-theme='light'] .hover\:border-\[\#E8B058\]\/50 ✓
  - line 1769: [data-theme='light'] .group-hover\:text-\[\#E8B058\] ✓
  - line 1778: [data-theme='light'] .hover\:text-\[\#E8B058\]:hover ✓
  - line 1782: [data-theme='light'] .hover\:text-\[\#D4A047\]:hover ✓
  - lines 2865/2869: VoC modal success text overrides ✓

Token values (from :root in custom.css):
  --nxgen-gold-accessible: #996B1F       (WCAG AA on white)
  --nxgen-gold-accessible-hover: #7A5518 (darker for hover)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run systematic contrast sweep and add any missing CSS overrides</name>
  <files>classic/src/css/custom.css</files>
  <action>
    Perform the systematic light-mode contrast sweep. The analysis above shows that all known
    #E8B058 instances on the home page path are covered. This task verifies and fills any gaps.

    Step 1 — Verify existing overrides are intact:
    Search custom.css for the known overrides and confirm they exist:
      grep -n "text-\\\[#E8B058\\\]\|D4A047\|voc-success" classic/src/css/custom.css | head -20

    Step 2 — Check index.tsx gradient on Support CTA (line 316):
    The "from-[#E8B058]/5 via-transparent to-[#E8B058]/5" is a 5% opacity decorative tint.
    In light mode this is near-invisible (#E8B058 at 5% on cream = subtle warm tint). No contrast
    issue — decorative backgrounds do not need WCAG contrast treatment. No fix needed.

    Step 3 — Check NXGENSphereHero "By NXGEN" byline (line 241):
    Uses text-[#E8B058]. In light mode this is overridden to #996B1F by custom.css line 1753.
    No additional fix needed.

    Step 4 — If during the sweep you find ANY uncovered text-color #E8B058 instance that renders
    on the home page in light mode WITHOUT a CSS override, add the appropriate override to
    custom.css in the "LIGHT MODE OVERRIDES" section (around line 1752). Use the pattern:
      [data-theme='light'] .selector { color: var(--nxgen-gold-accessible) !important; }

    Step 5 — If NO new gaps are found (expected outcome based on analysis), add a comment to
    custom.css confirming the sweep was done:
    Find the "Gold Accent Overrides for WCAG" comment block (around line 1752) and add after the
    last existing override:
      /* Phase 5 gap-closure sweep complete: all #E8B058 text instances on home page path
         confirmed covered by overrides above. Date: 2026-03-07 */

    The comment serves as a maintenance marker so future developers know the sweep was done.
  </action>
  <verify>
    <automated>cd /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/classic && node -e "const fs=require('fs'); const c=fs.readFileSync('src/css/custom.css','utf8'); const hasE8B058Override=c.includes('[data-theme=\'light\'] .text-\\\\[\\\\#E8B058\\\\]'); const hasD4A047Override=c.includes('[data-theme=\'light\'] .hover\\\\:text-\\\\[\\\\#D4A047\\\\]'); const hasVocOverride=c.includes('voc-success-message'); console.log('E8B058 text override present:', hasE8B058Override); console.log('D4A047 hover override present:', hasD4A047Override); console.log('VoC override present:', hasVocOverride);"</automated>
  </verify>
  <done>Sweep complete. All text-color #E8B058 instances on the home page path have CSS overrides in custom.css. Sweep comment added. No uncovered contrast violations found (or new overrides added if violations were found).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — Phase 5 final sign-off</name>
  <what-built>
    Three gap-closure plans are now complete:
    - 05-04: Pagefind migration (Algolia removed, postbuild index, UI theme installed, search.spec.ts updated)
    - 05-05: Hero light mode redesign (background.jpg at 40% opacity, white overlay reduced)
    - 05-06 Task 1: Light mode contrast sweep (all #E8B058 text instances confirmed covered)

    The Playwright suite (8 passed, 1 skipped) was green in Plan 03 and the code changes
    in these gap-closure plans are additive — no existing passing tests should regress.
  </what-built>
  <how-to-verify>
    Please verify the following on https://docs.nxgen.cloud (or a local build/serve if available).
    Switch your browser to LIGHT MODE before testing.

    --- CHECK 1: Hero Banner ---
    Open the home page (/).
    Expected: Background.jpg is visibly present behind the hero text — not washed out.
    The cream gradient background shows through, background.jpg adds visual texture at ~40% opacity.
    "Reimagined." subtitle uses dark amber/gold (#996B1F range), readable against the background.
    "Intelligent Monitoring" headline is dark (#1A1A1A) and sharp.
    No "bleached" or "inverted" appearance.

    --- CHECK 2: Home Page Section Headings ---
    Scroll down to the "Quick Start" and "Releases" section headings.
    Expected: Both h2 headings render in dark amber/brown (#996B1F range), not light gold.
    The "View All Releases →" link uses the same accessible dark amber color.
    On hover, the link darkens (to #7A5518 range) — it does NOT disappear.

    --- CHECK 3: VoC Feedback Modal ---
    Open the feedback widget (bottom-right or wherever it appears on the page).
    Submit a test entry: Title "Test", Category "Feature Request", Desired Outcome "Test".
    Expected: The success message after submission shows dark readable text (heading in
    dark amber/gold, paragraph in dark gray) — NOT white-on-white/cream.

    --- CHECK 4: Search UI ---
    Verify that a Pagefind search input or button is present in the navbar area.
    (The @getcanary/docusaurus-theme-search-pagefind theme renders a search element there.)
    Expected: A search UI element is present. Clicking/activating it opens a search interface.
    The search UI uses the site's gold color scheme — not the Algolia/DocSearch blue styling.

    Note: Search results may return 0 results if the Pagefind index has not been built yet
    (requires running npm run build to generate build/pagefind/). Presence of the UI is sufficient.
  </how-to-verify>
  <action>Pause execution and present the verification checklist to the user. Wait for approval before proceeding.</action>
  <verify>Human confirms all four visual checks pass by typing "approved".</verify>
  <done>User has typed "approved" — Phase 5 visual sign-off obtained.</done>
  <resume-signal>
    Type "approved" if all four checks pass.
    Type "issues: [description]" if any check fails, describing what you see vs. what was expected.
  </resume-signal>
</task>

</tasks>

<verification>
Phase 5 is complete when the human checkpoint is approved.

Run the full Playwright suite as a final automated gate before the checkpoint:
```bash
cd /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/classic
npx playwright test --project=chromium 2>&1 | tail -20
```
Expected: All non-fixme tests pass. The search.spec.ts "pagefind search UI is present" test
should now pass (Pagefind theme installed). The fixme for search results remains skipped.
</verification>

<success_criteria>
- Light mode contrast sweep confirms: all text-color #E8B058 instances on the home page path have accessible overrides in custom.css
- Sweep completion comment added to custom.css (maintenance marker)
- Human checkpoint approved: hero banner visible (not washed out), section headings accessible, VoC modal readable, Pagefind search UI present
- Phase 5 sign-off obtained — both PLSH-01 and PLSH-02 requirements met
- Full Playwright suite green (non-fixme tests) before checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish/05-06-SUMMARY.md` with standard summary format.

If checkpoint is approved, also update:
- .planning/REQUIREMENTS.md: mark PLSH-01 and PLSH-02 as Complete
- .planning/ROADMAP.md: mark Phase 5 as complete with today's date
- .planning/STATE.md: update status to reflect Phase 5 complete
</output>
