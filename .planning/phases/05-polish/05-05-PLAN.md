---
phase: 05-polish
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - classic/src/components/NXGENSphereHero.tsx
autonomous: true
gap_closure: true
requirements: [PLSH-02]

must_haves:
  truths:
    - "Hero background.jpg is visible in light mode at reasonable opacity (not washed out)"
    - "White gradient overlay no longer dominates the light mode hero"
    - "Gold accent text and UI elements on the hero remain readable in light mode"
    - "Dark mode hero appearance is unchanged"
  artifacts:
    - path: "classic/src/components/NXGENSphereHero.tsx"
      provides: "Redesigned light mode hero with acceptable background opacity"
      contains: "0.40"
  key_links:
    - from: "NXGENSphereHero.tsx background layer"
      to: "visible Background.jpg in light mode"
      via: "opacity: isDark ? 0.8 : 0.40"
      pattern: "isDark.*0\\.8.*0\\.4"
---

<objective>
Fix the washed-out hero banner in light mode.

Purpose: The human checkpoint in Plan 03 rejected Phase 5 sign-off specifically because the hero background.jpg renders at 15% opacity with an additive white gradient overlay in light mode — confirmed visually broken. The fix increases image opacity to ~40% and reduces the white overlay to restore visual quality without changing dark mode behavior.

Output: NXGENSphereHero.tsx renders acceptably in light mode. Background.jpg is visible, "Reimagined." headline is readable in accessible gold (#996B1F — already used in the component), and no white wash effect. Dark mode is unchanged.
</objective>

<execution_context>
@C:/Users/abdel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/abdel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish/05-03-SUMMARY.md

<interfaces>
<!-- Key facts the executor needs — no codebase exploration required. -->

From classic/src/components/NXGENSphereHero.tsx (relevant lines):

Line 140 — outer container (light mode uses warm cream gradient background, leave unchanged):
  background: isDark ? '#000000' : 'linear-gradient(135deg, #FFFBF5 0%, #FFF8EE 25%, #FFFDF9 50%, #FEF9F0 75%, #FFFBF5 100%)'

Lines 149–159 — Background image layer (THE PROBLEM):
  <div
    className="absolute inset-0 z-0 bg-cover bg-center bg-no-repeat transition-opacity duration-500"
    style={{
      backgroundImage: 'url("/img/Background.jpg")',
      opacity: isDark ? 0.8 : 0.15,            // <-- CHANGE 0.15 to 0.40
      filter: isDark ? 'none' : 'sepia(20%) saturate(120%)'
    }}
  >
    {/* Adaptive Overlay for Text Readability */}
    <div className={`absolute inset-0 transition-colors duration-500 ${isDark ? 'bg-black/40 backdrop-blur-sm' : 'bg-gradient-to-b from-white/40 via-transparent to-white/60'}`} />
    //  ^-- CHANGE light mode: from-white/40 via-transparent to-white/60
    //      TO: from-white/10 via-transparent to-white/25
  </div>

Line 253 — "Reimagined." headline (already correct — uses #996B1F in light mode, DO NOT change):
  <span className={`block mt-2 text-3xl md:text-4xl font-light italic ${isDark ? 'text-[#E8B058]' : 'text-[#996B1F]'}`}

Lines 241 — "By NXGEN" byline (uses text-[#E8B058] class — covered by CSS override to #996B1F, leave unchanged)

CONSTRAINT: The comment in custom.css marks dark mode values as IMMUTABLE. Only light mode changes allowed.
CONSTRAINT: User chose "Fix light mode hero design" — not "dark mode only approach." The fix must make
             light mode look good, not just acceptable.

DESIGN INTENT: The hero background.jpg at 40% opacity on the warm cream base provides visual richness.
The reduced overlay (from-white/10 via-transparent to-white/25) fades to the page background at the
bottom edge without dominating the top. Gold accent line at the top (line 184–188, unchanged) frames
the hero. The particle system already has bronze/amber colors in light mode (139, 90, 43) at 80% opacity
which will stand out better once the background washes are reduced.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Increase hero background opacity and reduce white overlay in light mode</name>
  <files>classic/src/components/NXGENSphereHero.tsx</files>
  <action>
    In classic/src/components/NXGENSphereHero.tsx, make exactly two targeted changes:

    CHANGE 1 — Line 153, background image opacity:
      FROM: opacity: isDark ? 0.8 : 0.15,
      TO:   opacity: isDark ? 0.8 : 0.40,

    CHANGE 2 — Line 158, overlay gradient classes for light mode:
      FROM: 'bg-gradient-to-b from-white/40 via-transparent to-white/60'
      TO:   'bg-gradient-to-b from-white/10 via-transparent to-white/25'

    These are the ONLY changes to make. Do not alter:
    - The outer container background (cream gradient, line 144)
    - The dark mode overlay (bg-black/40 backdrop-blur-sm)
    - The gold accent line at the top (lines 184–188)
    - The decorative circles (lines 164–176)
    - The headline text colors (line 253 already uses #996B1F for light mode)
    - Particle system colors (already using amber/bronze for light mode)
    - Any other element

    After making the changes, verify no TypeScript errors:
    cd classic && npx tsc --noEmit 2>&1 | head -10
  </action>
  <verify>
    <automated>cd /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/classic && node -e "const fs=require('fs'); const c=fs.readFileSync('src/components/NXGENSphereHero.tsx','utf8'); const hasNewOpacity=c.includes('isDark ? 0.8 : 0.40'); const hasReducedOverlay=c.includes('from-white/10 via-transparent to-white/25'); const noOldOpacity=!c.includes('isDark ? 0.8 : 0.15'); const noOldOverlay=!c.includes('from-white/40 via-transparent to-white/60'); console.log('opacity updated to 0.40:', hasNewOpacity); console.log('overlay reduced:', hasReducedOverlay); console.log('old opacity gone:', noOldOpacity); console.log('old overlay gone:', noOldOverlay);"</automated>
  </verify>
  <done>NXGENSphereHero.tsx line 153 reads `isDark ? 0.8 : 0.40`. Line 158 overlay class reads `from-white/10 via-transparent to-white/25`. No other changes made. TypeScript check passes.</done>
</task>

</tasks>

<verification>
Static verification after the change:

```bash
cd /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/classic
node -e "
const fs = require('fs');
const c = fs.readFileSync('src/components/NXGENSphereHero.tsx', 'utf8');
console.log('opacity 0.40:', c.includes('isDark ? 0.8 : 0.40'));
console.log('overlay reduced:', c.includes('from-white/10 via-transparent to-white/25'));
console.log('dark mode unchanged (0.8):', c.includes('isDark ? 0.8'));
console.log('no old opacity 0.15:', !c.includes(': 0.15'));
console.log('dark overlay unchanged:', c.includes('bg-black/40 backdrop-blur-sm'));
"
```

Visual verification is deferred to Plan 06 which ends with the human checkpoint.
</verification>

<success_criteria>
- NXGENSphereHero.tsx: background opacity in light mode changed from 0.15 to 0.40
- NXGENSphereHero.tsx: white overlay reduced from from-white/40…to-white/60 to from-white/10…to-white/25
- Dark mode behavior is byte-for-byte identical (opacity 0.8, bg-black/40 backdrop-blur-sm unchanged)
- TypeScript compiles without errors
- PLSH-02 hero visual criterion addressed: background.jpg now renders at 40% opacity — above threshold for "reasonable opacity" identified in VERIFICATION.md
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish/05-05-SUMMARY.md` with standard summary format.
</output>
