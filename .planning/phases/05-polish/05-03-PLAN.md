---
phase: 05-polish
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified: []
autonomous: false
requirements:
  - PLSH-01
  - PLSH-02

must_haves:
  truths:
    - "A user in light mode sees readable text in the VoC modal success state after submitting feedback"
    - "A user navigating from the home page can reach any major content section in at most two clicks"
    - "If Phase 4 migration is complete, Algolia search returns at least 1 result for 'getting started' after the re-crawl; if Phase 4 is not yet done, this criterion is deferred to post-Phase-4 validation and Phase 5 cannot be signed off as fully complete"
    - "Footer has no dead links — all navigation destinations resolve to real pages"
    - "The Playwright full test suite passes green before phase sign-off"
  artifacts:
    - path: "classic/e2e/navigation.spec.ts"
      provides: "Passing navigation tests"
    - path: "classic/e2e/search.spec.ts"
      provides: "Passing search smoke tests"
    - path: "classic/e2e/visual-consistency.spec.ts"
      provides: "Passing visual consistency tests"
  key_links:
    - from: "User — light mode"
      to: "VoC modal success message"
      via: "[data-theme='light'] .voc-success-message p CSS override"
      pattern: "data-theme.*light.*voc-success-message"
    - from: "DocSearch UI"
      to: "Algolia index"
      via: "unified appId + indexName in docusaurus.config.ts"
      pattern: "appId.*ALGOLIA_APP_ID"
---

<objective>
Human verification gate: confirm the visual fixes from Plan 01 are working in a real browser, confirm Algolia search returns results after the Phase 4 re-crawl, and sign off on the full Playwright suite.

Purpose: Automated tests cover the code — this checkpoint covers the human experience. Both PLSH-01 (navigation/search) and PLSH-02 (visual consistency) require a real browser check to confirm the fixes work end-to-end.
Output: Phase 5 sign-off. No file changes expected — this plan is verification only.
</objective>

<execution_context>
@C:/Users/abdel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/abdel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-polish/05-VALIDATION.md
@.planning/phases/05-polish/05-01-SUMMARY.md
@.planning/phases/05-polish/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full Playwright suite and confirm all non-fixme tests pass</name>
  <files>classic/src/css/custom.css, classic/src/components/Footer/Footer.tsx, classic/e2e/navigation.spec.ts, classic/e2e/search.spec.ts, classic/e2e/visual-consistency.spec.ts (conditional — only modified if test failures require fixes)</files>
  <action>
Run the full Playwright test suite:

```bash
cd classic && npm run test:e2e
```

If any test fails (other than tests marked `test.fixme()`):
1. Read the failure output
2. Diagnose whether it is a code issue (CSS not applied, build not fresh) or a test issue (wrong selector)
3. Fix the root cause — in `custom.css`, `Footer.tsx`, or the spec file as appropriate
4. Re-run until the suite is green

Fixme tests are expected to stay skipped until Phase 4 content is indexed. Do not un-skip them here.

If `npm run test:e2e` is not defined, run: `cd classic && npx playwright test --project=chromium`
  </action>
  <verify>
    <automated>cd "C:/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/classic" && npx playwright test e2e/navigation.spec.ts e2e/search.spec.ts e2e/visual-consistency.spec.ts --project=chromium --reporter=line 2>&1 | tail -20</automated>
  </verify>
  <done>All non-fixme tests in navigation.spec.ts, search.spec.ts, and visual-consistency.spec.ts pass. Zero test failures (fixme tests are counted as skipped, not failed).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 01 delivered:
    - CSS fix: VoC modal success message `.voc-success-message p` now has `[data-theme='light']` override — text is no longer invisible white-on-white in light mode
    - CSS fix: `hover:text-[#D4A047]` "View All Releases" link now has WCAG-accessible color in light mode
    - Footer: All placeholder `#` links replaced with real destinations (Getting Started, Devices, Features, Troubleshooting → `/docs/...`; Support Center → `/support/contact`; Release Notes → `/releases`)

    Plan 02 delivered:
    - Algolia config unified — docusaurus.config.ts and algolia-config.json now reference the same App ID and index name
    - Custom indexer marked deprecated
    - Algolia Crawler `renderJavaScript: true` confirmed

    This checkpoint verifies the human-visible results.
  </what-built>
  <how-to-verify>
    **Visual consistency (PLSH-02):**
    1. Open https://docs.nxgen.cloud (or `npm run start` locally at http://localhost:3000)
    2. Switch to LIGHT MODE using the theme toggle in the header
    3. Click the feedback button (bottom-right of screen) to open the VoC modal
    4. Fill in the minimum required fields (Title: "Test", choose Feature Request, add Desired Outcome: "Test")
    5. Submit the form — confirm the success message "Feedback Received / Your request has been routed..." is READABLE (dark text, not invisible)
    6. Back on the home page in light mode: verify the "Quick Start" and "Releases" section headings are visible (dark gold, not washed out)
    7. Hover over the "View All Releases →" link — verify it does not disappear on hover (accessible gold color, not white-on-white)

    **Navigation (PLSH-01):**
    8. Scroll to the footer — verify "Getting Started", "Devices", "Features", "Troubleshooting", "Support Center", and "Release Notes" all have real URLs (hover over them to see the link targets in the status bar)
    9. Click "Release Notes" in the footer — confirm it navigates to /releases (not a 404 or `#`)
    10. Click "Getting Started" — confirm it navigates to a docs page (not a 404)

    **Search (PLSH-01) — REQUIRED if Phase 4 is complete:**
    11. Press Ctrl+K (or click the search icon) — confirm the DocSearch modal opens
    12. Type "getting started" — check whether results appear
    13. Type "alarm" — check whether results appear

    IMPORTANT: Determine Phase 4 status before responding:
    - If Phase 4 content migration IS complete and a re-crawl has run: search MUST return at least 1 result for "getting started". Empty results after Phase 4 completion means Plan 02's Algolia fix did not work and must be investigated before Phase 5 can close.
    - If Phase 4 is NOT yet complete: note "search deferred — Phase 4 not yet migrated". Phase 5 visual and navigation criteria can be signed off, but the phase is marked PARTIALLY COMPLETE pending post-Phase-4 search validation.
  </how-to-verify>
  <resume-signal>
    For FULL sign-off (all criteria met):
    Type "approved" — confirm all visual checks pass AND search returns results.

    For PARTIAL sign-off (visual/nav pass, search deferred):
    Type "approved-partial: search deferred — Phase 4 not yet migrated"
    This closes Phase 5 visual/nav work but leaves PLSH-01 search criterion open until after Phase 4.

    NOT acceptable as sign-off (requires investigation before closing Phase 5):
    - "search empty" when Phase 4 IS already complete — this indicates Plan 02 Algolia fix did not take effect and must be diagnosed.

    Or describe specific issues found (e.g., "VoC text still invisible in light mode", "footer link 404s on /docs/devices").
  </resume-signal>
</task>

</tasks>

<verification>
Phase 5 is complete when:

1. All three Playwright spec files exist and non-fixme tests pass: `cd classic && npx playwright test e2e/navigation.spec.ts e2e/search.spec.ts e2e/visual-consistency.spec.ts --project=chromium`
2. VoC modal success text is readable in light mode — confirmed by human in checkpoint
3. Footer has no dead # links — confirmed by navigation.spec.ts test + human scroll check
4. Algolia config is unified — confirmed in Plan 02 SUMMARY
5. Human sign-off received from checkpoint above (full or partial — see resume-signal for conditions)
</verification>

<success_criteria>
- PLSH-01: Navigation from home page to any major section takes at most two clicks. Footer links all resolve. Algolia config points to the correct index. If Phase 4 is complete: search returns at least 1 result for "getting started". If Phase 4 is not yet done: search validation is deferred and noted as open.
- PLSH-02: VoC modal success state is readable in both light and dark mode. Home page headings are WCAG-accessible in light mode. No invisible-text regressions found in human review.
- Full Playwright suite: zero failures on non-fixme tests.
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish/05-03-SUMMARY.md`
</output>
