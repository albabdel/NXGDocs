---
phase: 38-product-config
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - classic/product.config.ts
  - classic/docusaurus.config.ts
  - classic/src/css/tokens.css
autonomous: true
requirements:
  - DOM-03
user_setup: []

must_haves:
  truths:
    - "Product configuration file exists with values for GCXONE and GCSurge"
    - "Docusaurus config reads from product configuration based on PRODUCT env var"
    - "Build output has correct product title and URL"
    - "Product configuration is type-safe with TypeScript"
  artifacts:
    - path: "classic/product.config.ts"
      provides: "Product-specific configuration values"
      min_lines: 50
    - path: "classic/docusaurus.config.ts"
      provides: "Docusaurus configuration reading product config"
      contains: "PRODUCT_CONFIG"
  key_links:
    - from: "classic/docusaurus.config.ts"
      to: "classic/product.config.ts"
      via: "import PRODUCT_CONFIGS"
      pattern: "import.*product\\.config"
---

<objective>
Create product configuration file and integrate with Docusaurus config to enable per-product branding (title, tagline, URL, favicon) based on PRODUCT environment variable.

Purpose: Enable each product build to have its own identity without code duplication. The PRODUCT env var flows from build pipeline (Phase 37) to configuration, producing distinct output per product.
Output: Product configuration file + modified Docusaurus config reading product-specific values.
</objective>

<execution_context>
@~\.config\opencode/get-shit-done/workflows/execute-plan.md
@~\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/ARCHITECTURE.md
@classic/docusaurus.config.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: Create product.config.ts with product definitions</name>
  <files>classic/product.config.ts</files>
  <action>
Create `classic/product.config.ts` with TypeScript type definitions and product configurations:

1. Define `ProductConfig` interface with:
   - `id`: string (product identifier)
   - `title`: string (site title)
   - `tagline`: string (site tagline)
   - `url`: string (production URL)
   - `baseUrl`: string (default '/')
   - `favicon`: string (path to favicon)
   - `logo`: object with `src`, `alt`, `href` (optional per-product logo)
   - `theme`: object with `primaryColor` (hex), `primaryColorDark` (hex)
   - `socialCard`: string (path to social sharing image)
   - `metadata`: object with `keywords`, `description`

2. Export `PRODUCT_CONFIGS` object with entries for:
   - `gcxone`: GCXONE Documentation, gold theme (#C89446), docs.gcxone.com
   - `gcsurge`: GCSurge Documentation, blue theme (#3B82F6), docs.gcsurge.com

3. Export helper functions:
   - `getProductConfig(productId: string): ProductConfig` - returns config or throws
   - `getCurrentProduct(): string` - returns PRODUCT env var or 'gcxone' default
   - `getCurrentConfig(): ProductConfig` - returns current product's config

4. Add JSDoc comments explaining usage with multi-build pipeline.

Do NOT add product field validation - that's Phase 36 concern.
Do NOT add authentication-related config - that's Phase 35 concern.
  </action>
  <verify>
    <automated>npx tsc --noEmit classic/product.config.ts 2>&1 | head -20</automated>
    <manual>Open product.config.ts and verify both products have complete configurations</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>product.config.ts exports PRODUCT_CONFIGS, getProjectConfig, getCurrentProduct, getCurrentConfig with type safety</done>
</task>

<task type="auto">
  <name>task 2: Integrate product config into docusaurus.config.ts</name>
  <files>classic/docusaurus.config.ts</files>
  <action>
Modify `classic/docusaurus.config.ts` to read from product.config.ts:

1. Add import at top:
   ```typescript
   import { getCurrentConfig, getCurrentProduct } from './product.config';
   ```

2. Add near top (after imports):
   ```typescript
   const productConfig = getCurrentConfig();
   const productId = getCurrentProduct();
   console.log(`[Docusaurus Config] Building for product: ${productId}`);
   ```

3. Replace hardcoded values with product config:
   - `title`: use `productConfig.title`
   - `tagline`: use `productConfig.tagline`
   - `url`: use `productConfig.url`
   - `favicon`: use `productConfig.favicon`
   - `headTags`: update theme-color meta to use `productConfig.theme.primaryColor`
   - `themeConfig.image`: use `productConfig.socialCard`

4. Update navbar logo configuration:
   - `navbar.logo.alt`: use `${productConfig.title} Logo` pattern
   - `navbar.logo.src`: use `productConfig.logo.src` if defined

5. Add productName field to config for plugins that need it:
   ```typescript
   customFields: {
     productId,
     productConfig,
   }
   ```

Keep existing plugin configurations unchanged - they work with filtered content from Phase 36/37.
Preserve all existing CSS customizations in themeConfig.
  </action>
  <verify>
    <automated>npx tsc --noEmit classic/docusaurus.config.ts 2>&1 | head -20</automated>
    <manual>Run `PRODUCT=gcxone npx docusaurus build` and verify title is "GCXONE Documentation"</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>docusaurus.config.ts reads all branding values from product.config.ts, builds succeed for both products</done>
</task>

<task type="auto">
  <name>task 3: Add product-specific CSS custom properties</name>
  <files>classic/src/css/tokens.css</files>
  <action>
Modify `classic/src/css/tokens.css` to support product-specific theming via CSS custom properties:

1. Add new CSS custom properties at the end of `:root`:
   ```css
   /* === PRODUCT-SPECIFIC TOKENS (set via JS) === */
   --product-primary: #C89446;
   --product-primary-dark: #B58237;
   --product-primary-light: #D4A574;
   --product-accent: var(--product-primary);
   ```

2. Update existing gold references to use the new product tokens:
   - Replace `--ifm-color-primary` to use `var(--product-primary)`
   - Update `--nxgen-gold` and related tokens to reference `--product-primary`

3. Add product token injection in dark mode section:
   ```css
   [data-theme='dark'] {
     /* Product tokens maintain same values but can be overridden */
     --product-primary: var(--nxgen-gold);
     --product-primary-dark: #B58237;
     --product-primary-light: #D4A574;
   }
   ```

4. Add comment explaining how these tokens will be dynamically set by Root.tsx based on product (future task).

Do NOT remove existing gold color definitions - they're used throughout components.
Do NOT add JavaScript logic - CSS only in this task.
  </action>
  <verify>
    <automated>grep -c "product-primary" classic/src/css/tokens.css</automated>
    <manual>Verify CSS parses without errors: `npx docusaurus build` succeeds</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>tokens.css has --product-primary tokens that can be overridden per product, existing gold colors still work</done>
</task>

</tasks>

<verification>
- TypeScript compilation succeeds for both new files
- Docusaurus build succeeds with PRODUCT=gcxone and PRODUCT=gcsurge
- Build output shows correct title in HTML
- CSS tokens exist and are syntactically valid
</verification>

<success_criteria>
- product.config.ts exists with type-safe ProductConfig interface
- PRODUCT_CONFIGS contains gcxone and gcsurge entries
- docusaurus.config.ts imports and uses product config
- Both products build with correct branding
- CSS has product-specific token foundation
</success_criteria>

<output>
After completion, create `.planning/phases/38-product-config/38-01-SUMMARY.md`
</output>
