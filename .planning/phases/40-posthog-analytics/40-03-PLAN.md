---
phase: 40-posthog-analytics
plan: 03
type: execute
wave: 3
depends_on:
  - 40-01
  - 40-02
files_modified:
  - .planning/phases/40-posthog-analytics/POSTHOG-DASHBOARDS.md
autonomous: false
requirements:
  - ANLT-04
  - MPROD-05
user_setup:
  - service: PostHog
    why: "Creating dashboards and insights in PostHog UI"
    env_vars: []
    dashboard_config:
      - task: "Create product analytics dashboard"
        location: "https://us.posthog.com/project/365239/dashboards"
      - task: "Create per-product insights"
        location: "https://us.posthog.com/project/365239/insights"
must_haves:
  truths:
    - "Per-product analytics dashboards available in PostHog"
    - "User can view metrics filtered by product"
    - "New product dashboards can be created from documented process"
  artifacts:
    - path: ".planning/phases/40-posthog-analytics/POSTHOG-DASHBOARDS.md"
      provides: "Dashboard setup documentation"
      contains: "dashboard creation steps"
  key_links:
    - from: "PostHog Dashboard"
      to: "Product Events"
      via: "Insights filtering by product property"
      pattern: "property.*product"
---

<objective>
Create per-product analytics dashboards in PostHog and document the setup process.

Purpose: Enable product-specific analytics views so stakeholders can see metrics for their product only. Document the process so new products can be added quickly.
Output: Per-product dashboards in PostHog and setup documentation.
</objective>

<execution_context>
@~\.config\opencode/get-shit-done/workflows/execute-plan.md
@~\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@keys.md

# PostHog credentials (from keys.md)
# Project Token: phc_tkcgBrQb37g5F7aiSTcuKWoUaSitBNd6JdcULN6xqrwS
# Project ID: 365239
# Host: https://us.i.posthog.com
# Dashboard URL: https://us.posthog.com/project/365239

# From 40-01 and 40-02:
# - All events tagged with product via group() call
# - Doc views tracked as 'doc_view' events
# - Search queries tracked as 'search_query' events
# - Events include 'product' property
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>task 1: Verify events are appearing in PostHog</name>
  <files>N/A</files>
  <action>
Before creating dashboards, verify that events are flowing to PostHog with product context:

1. Deploy the changes from Plans 40-01 and 40-02
2. Visit the deployed site and navigate through several docs pages
3. Perform a few searches in the search modal
4. Check PostHog Live Events: https://us.posthog.com/project/365239/events

**Verification steps:**
1. Go to PostHog Events page
2. Filter by event name: `doc_view` or `search_query`
3. Click on an event to see its properties
4. Verify the event has:
   - `product` property (e.g., "gcxone")
   - `doc_title`, `doc_path` (for doc_view)
   - `query`, `result_count` (for search_query)
   - `$group_product` property from the group() call

**Expected outcome:**
- Events appear in PostHog within 1-2 minutes
- Each event shows product context in properties
- Group information visible in event details
  </action>
  <verify>
    <automated>MISSING - Manual verification in PostHog UI</automated>
    <manual>
      1. Visit https://us.posthog.com/project/365239/events
      2. Filter by event name 'doc_view' or 'search_query'
      3. Click event and verify 'product' property exists
      4. Confirm group 'product' appears in event details
    </manual>
    <sampling_rate>run after deployment and user verification</sampling_rate>
  </verify>
  <done>Events confirmed in PostHog with product context</done>
</task>

<task type="auto">
  <name>task 2: Document dashboard setup process</name>
  <files>.planning/phases/40-posthog-analytics/POSTHOG-DASHBOARDS.md</files>
  <action>
Create comprehensive documentation for setting up per-product analytics dashboards in PostHog.

File: `.planning/phases/40-posthog-analytics/POSTHOG-DASHBOARDS.md`

```markdown
# PostHog Analytics Dashboards

## Overview

This document describes the PostHog analytics setup for the multi-product documentation platform. Each product (GCXONE, GC Surge) has its own dashboard with product-specific metrics.

## PostHog Project

- **Project ID:** 365239
- **Dashboard URL:** https://us.posthog.com/project/365239
- **Host:** https://us.i.posthog.com

## Event Structure

All events include the following properties:

| Property | Description | Example |
|----------|-------------|---------|
| `product` | Product identifier | `gcxone`, `gcsurge` |
| `$group_product` | PostHog group property | `gcxone` |

### Events Tracked

| Event | Properties | Triggered When |
|-------|------------|----------------|
| `doc_view` | `doc_id`, `doc_title`, `doc_path`, `category`, `product` | User views a documentation page |
| `search_query` | `query`, `result_count`, `search_mode`, `product` | User performs a search |
| `search_result_click` | `query`, `result_id`, `result_title`, `result_url`, `position`, `product` | User clicks a search result |
| `$pageview` | `product` | User views any page |

## Dashboard Setup

### 1. Create Per-Product Dashboard

For each product (GCXONE, GC Surge):

1. Go to **Dashboards** → **New dashboard**
2. Name: `{Product Name} Analytics` (e.g., "GCXONE Analytics")
3. Add the following insights:

### 2. Required Insights

#### Page Views Trend
- **Type:** Line chart
- **Event:** `$pageview`
- **Filter:** `product = {product_id}`
- **Aggregation:** Count per day
- **Time range:** Last 30 days

#### Top Viewed Documents
- **Type:** Table
- **Event:** `doc_view`
- **Filter:** `product = {product_id}`
- **Breakdown:** `doc_title`
- **Aggregation:** Count
- **Limit:** Top 20

#### Search Queries
- **Type:** Table
- **Event:** `search_query`
- **Filter:** `product = {product_id}`
- **Breakdown:** `query`
- **Aggregation:** Count
- **Limit:** Top 20

#### Zero-Result Searches
- **Type:** Table
- **Event:** `search_query`
- **Filter:** `product = {product_id}` AND `result_count = 0`
- **Breakdown:** `query`
- **Aggregation:** Count

#### Search Click-Through Rate
- **Type:** Funnel
- **Steps:**
  1. `search_query` (filter: `product = {product_id}`)
  2. `search_result_click` (filter: `product = {product_id}`)
- **Time range:** Last 7 days

#### Average Search Results
- **Type:** Number
- **Event:** `search_query`
- **Filter:** `product = {product_id}`
- **Aggregation:** Average of `result_count`

### 3. Create Product Group

In PostHog, create a Group type for products:

1. Go to **Settings** → **Groups**
2. Create group type: `product`
3. Group keys: `gcxone`, `gcsurge`

This enables group-level analytics and dashboards.

## Adding a New Product

To add analytics for a new product (fulfills MPROD-05):

### Step 1: Update Product Configuration

Edit `classic/src/utils/productConfig.ts`:

```typescript
export const PRODUCTS: Record<string, ProductConfig> = {
  gcxone: { /* existing */ },
  gcsurge: { /* existing */ },
  newproduct: {
    id: 'newproduct',
    name: 'New Product',
    groupKey: 'newproduct',
    tagline: 'New Product Documentation',
    domain: 'docs.newproduct.com',
  },
};
```

### Step 2: Set Environment Variable

For the new product's Cloudflare Pages project:

```
PRODUCT=newproduct
```

### Step 3: Create Dashboard in PostHog

1. Duplicate an existing product dashboard
2. Rename to "New Product Analytics"
3. Update all filters to `product = newproduct`
4. Save

**Time estimate:** 30 minutes - 1 hour (no code changes required)

## Metrics Glossary

| Metric | Description | Calculation |
|--------|-------------|-------------|
| Daily Active Users | Unique users visiting docs per day | Count distinct `$device_id` |
| Page Views | Total page loads | Count `$pageview` events |
| Doc Views | Documentation page views | Count `doc_view` events |
| Search Volume | Number of searches performed | Count `search_query` events |
| Zero-Result Rate | Searches with no results | `search_query` where `result_count = 0` / total searches |
| Search CTR | Click-through from search results | `search_result_click` / `search_query` |
| Avg Search Position | Average position of clicked results | Mean of `position` in `search_result_click` |

## Troubleshooting

### Events not appearing

1. Check network tab for POST requests to `us.i.posthog.com`
2. Verify `POSTHOG_TOKEN` matches project token
3. Check browser console for PostHog errors

### Product property missing

1. Verify `PRODUCT` environment variable is set
2. Check `productConfig.ts` has the product registered
3. Verify `group()` call is executing (check useEffect in Root.tsx)

### Search events not tracked

1. Verify `useTrackSearch` is being called in SearchModal
2. Check search modal is loading properly
3. Verify PostHog provider is wrapping the app
```

This documentation enables MPROD-05: new products can be added in under 1 day.
  </action>
  <verify>
    <automated>test -f ".planning/phases/40-posthog-analytics/POSTHOG-DASHBOARDS.md" && grep -q "Adding a New Product" ".planning/phases/40-posthog-analytics/POSTHOG-DASHBOARDS.md" && echo "PASS"</automated>
  </verify>
  <done>Dashboard setup documentation created with step-by-step process</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>task 3: Create per-product dashboards in PostHog</name>
  <files>N/A</files>
  <action>
Create analytics dashboards in PostHog UI for each product.

**Prerequisites:**
- Plans 40-01 and 40-02 deployed
- Events confirmed in PostHog (Task 1 passed)

**Steps:**

1. **Create GCXONE Dashboard:**
   - Go to: https://us.posthog.com/project/365239/dashboards
   - Click "New dashboard"
   - Name: "GCXONE Analytics"
   - Add insights with filter: `product = gcxone`

2. **Create GC Surge Dashboard:**
   - Duplicate GCXONE dashboard or create new
   - Name: "GC Surge Analytics"
   - Update filters to: `product = gcsurge`

3. **Required Insights per Dashboard:**
   - Page Views Trend (line chart, 30 days)
   - Top 20 Viewed Documents (table)
   - Search Queries (table)
   - Zero-Result Searches (table)
   - Search Click-Through Rate (funnel)

4. **Create Product Group:**
   - Go to Settings → Groups
   - Create group type: `product`
   - Keys: `gcxone`, `gcsurge`

**Dashboard URLs to verify:**
- https://us.posthog.com/project/365239/dashboards
</action>
  <verify>
    <automated>MISSING - Manual dashboard creation in PostHog UI</automated>
    <manual>
      1. Visit https://us.posthog.com/project/365239/dashboards
      2. Verify "GCXONE Analytics" dashboard exists
      3. Verify "GC Surge Analytics" dashboard exists (or placeholder)
      4. Verify each dashboard has required insights
      5. Test filtering by product property
    </manual>
    <sampling_rate>run after user completes dashboard setup</sampling_rate>
  </verify>
  <done>Per-product dashboards created and accessible in PostHog</done>
</task>

</tasks>

<verification>
1. Events confirmed in PostHog with product context
2. Dashboard documentation created
3. GCXONE Analytics dashboard exists in PostHog
4. Dashboard shows metrics filtered by product
</verification>

<success_criteria>
- [ ] doc_view and search_query events visible in PostHog Events
- [ ] Events include product property
- [ ] POSTHOG-DASHBOARDS.md documentation complete
- [ ] GCXONE Analytics dashboard created
- [ ] Dashboard shows product-filtered metrics
</success_criteria>

<output>
After completion, create `.planning/phases/40-posthog-analytics/40-03-SUMMARY.md`
</output>
