---
phase: 06-schema-and-data-pipeline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - studio/schemaTypes/release.ts
  - studio/schemaTypes/roadmapItem.ts
  - studio/schemaTypes/releaseNote.ts
  - studio/schemaTypes/index.ts
  - studio/sanity.config.ts
  - studio/src/structure.ts
autonomous: false
requirements:
  - SCHEMA-01
  - SCHEMA-02
  - SCHEMA-03
  - SCHEMA-04
user_setup: []

must_haves:
  truths:
    - "Editor can open Sanity Studio and see Sprint Releases in the sidebar — the old Release Notes entry is gone"
    - "Editor can click + New and see Sprint Release and Roadmap Item document types"
    - "Editor can create a sprint release with sprintId, publishedAt, summary, and an items[] array"
    - "Each release item accepts title, description, changeType, affectedAreas, screenshot (image), and videoUrl (url)"
    - "Editor can create a roadmap item with title, description, status (radio), businessValue, changeType, uiChange (boolean), entitiesImpacted, projectedRelease"
    - "A Shipped roadmap item's releaseRef field shows a reference picker scoped to release documents only"
    - "Zero references to releaseNote remain in schemaTypes/index.ts, sanity.config.ts, or src/structure.ts"
  artifacts:
    - path: "studio/schemaTypes/release.ts"
      provides: "release document type with items[] inline array"
      exports: ["releaseType"]
    - path: "studio/schemaTypes/roadmapItem.ts"
      provides: "roadmapItem document type with reference to release"
      exports: ["roadmapItemType"]
    - path: "studio/schemaTypes/index.ts"
      provides: "Updated schema registry — releaseNoteType removed, releaseType + roadmapItemType added"
    - path: "studio/sanity.config.ts"
      provides: "All 4 releaseNote references replaced with release; roadmapItem added where appropriate"
    - path: "studio/src/structure.ts"
      provides: "All 3 releaseNote references replaced; roadmapItem sidebar entry added; exclusion filter updated"
  key_links:
    - from: "studio/schemaTypes/release.ts"
      to: "studio/schemaTypes/index.ts"
      via: "named import releaseType"
      pattern: "import.*releaseType.*from.*release"
    - from: "studio/schemaTypes/roadmapItem.ts"
      to: "studio/schemaTypes/index.ts"
      via: "named import roadmapItemType"
      pattern: "import.*roadmapItemType.*from.*roadmapItem"
    - from: "studio/schemaTypes/roadmapItem.ts"
      to: "studio/schemaTypes/release.ts"
      via: "reference field to: [{type: 'release'}]"
      pattern: "to.*type.*release"
---

<objective>
Atomically migrate Sanity Studio from the old flat `releaseNote` schema to the new `release` schema (with `items[]` inline array) and add the new `roadmapItem` schema — updating all seven reference sites across three files in a single pass.

Purpose: This is the highest-risk step in Phase 6. Partial migration leaves Studio in a broken state with TypeScript errors and ghost sidebar entries. The operation must be atomic.

Output: Two new schema files, the old schema deleted, and all Studio registration sites updated — Studio compiles and shows the correct sidebar, document types, and field layouts.
</objective>

<execution_context>
@C:/Users/abdel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/abdel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-schema-and-data-pipeline/06-RESEARCH.md

<interfaces>
<!-- Existing releaseNote references to replace — verified from live codebase -->

From studio/schemaTypes/index.ts (current):
  Line 2:  import {releaseNoteType} from './releaseNote'
  Line 47: releaseNoteType,   (in schemaTypes array)

From studio/sanity.config.ts (current):
  Line 59:  initialValueTemplates → schemaType: 'releaseNote'
  Line 130: documentListWidget types array → 'releaseNote'
  Line 145: documentListWidget (Recently Published) types array → 'releaseNote'
  Line 171: typesWithCustomActions array → 'releaseNote'

From studio/src/structure.ts (current):
  Line 233-234: .schemaType('releaseNote').filter('_type == "releaseNote" && status == "published"')
  Lines 267-276: Standalone "Release Notes" item with schemaType('releaseNote') and filter
  Line 327:  documentTypeListItems() exclusion filter array → 'releaseNote'

All seven string references must be replaced or removed in this commit.

Schema authoring pattern (from studio/schemaTypes/portableText-ultimate.ts):
  import {defineType, defineField, defineArrayMember} from 'sanity'
  defineArrayMember({type: 'object', name: 'itemName', fields: [...]})

Reference field pattern (from existing schemas):
  defineField({name: 'releaseRef', type: 'reference', to: [{type: 'release'}]})
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create release.ts and roadmapItem.ts schemas</name>
  <files>studio/schemaTypes/release.ts, studio/schemaTypes/roadmapItem.ts</files>
  <action>
Create studio/schemaTypes/release.ts with the exact schema defined in 06-RESEARCH.md (Pattern 1). Key requirements:
- `displayTitle` field: type 'string', validation: required, description: 'Customer-facing sprint name. E.g. "December 2025 Release B" — not an internal sprint ID.'
- `sprintId` field: type 'string', optional, description: 'Internal sprint identifier for engineering reference only. Not shown to customers.'
- `slug` field: type 'slug', options: {source: 'displayTitle'}, validation: required
- `publishedAt` field: type 'date', validation: required
- `summary` field: type 'text', rows: 3, optional
- `items` field: type 'array', validation: min(1), of: [defineArrayMember({type: 'object', name: 'releaseItem', fields: [
    title (string, required),
    description (text, rows: 4, optional),
    changeType (string, options.list with 5 values: feature/fix/improvement/breaking/security, layout: 'radio'),
    affectedAreas (array of string, options.list with 9 areas listed in RESEARCH.md, layout: 'tags'),
    screenshot (image, options: {hotspot: true}, optional),
    videoUrl (url, description: 'YouTube or Vimeo embed URL', optional)
  ], preview: {select: {title: 'title', changeType: 'changeType'}, prepare: function}})]
- Document preview: select displayTitle + publishedAt, prepare function
- Do NOT add a docRef field to release items in this phase — that is a Phase 7/9 concern

Create studio/schemaTypes/roadmapItem.ts with the exact schema defined in 06-RESEARCH.md (Pattern 2). Key requirements:
- title (string, required)
- description (text, rows: 3, optional)
- status (string, options.list: Planned/In Progress/Shipped, layout: 'radio', initialValue: 'Planned', required)
- businessValue (text, rows: 3, optional)
- changeType (string, options.list: feature/improvement/fix/breaking, layout: 'radio', optional)
- uiChange (boolean, title: 'UI Change?', initialValue: false)
- entitiesImpacted (array of string, options.layout: 'tags', optional)
- projectedRelease (string, description: 'Human-readable e.g. "Q2 2026" or "Sprint 2026-06"', optional)
- releaseRef (reference, to: [{type: 'release'}], description: 'Link to the sprint release this shipped in (Shipped items only)', optional — do NOT add required validation on this field)
- Document preview: select title + status, prepare function with badge emoji per status
  </action>
  <verify>
    <automated>cd /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/studio && npx tsc --noEmit 2>&1 | head -20; echo "exit: $?"</automated>
  </verify>
  <done>Both files exist; studio/schemaTypes/release.ts exports releaseType; studio/schemaTypes/roadmapItem.ts exports roadmapItemType; TypeScript compile passes with no errors on the two new files</done>
</task>

<task type="auto">
  <name>Task 2: Atomic migration — update all 5 registration sites, delete releaseNote.ts</name>
  <files>studio/schemaTypes/index.ts, studio/sanity.config.ts, studio/src/structure.ts, studio/schemaTypes/releaseNote.ts</files>
  <action>
This task updates all five registration sites in one pass and deletes the old schema. Execute these changes together — partial migration breaks Studio.

**studio/schemaTypes/index.ts:**
- Remove: `import {releaseNoteType} from './releaseNote'`
- Add: `import {releaseType} from './release'`
- Add: `import {roadmapItemType} from './roadmapItem'`
- In the schemaTypes export array: remove `releaseNoteType,` and add `releaseType,` and `roadmapItemType,` in the same position (after docType)

**studio/sanity.config.ts — 4 sites:**
- Line 59 (initialValueTemplates): change `schemaType: 'releaseNote'` to `schemaType: 'release'`; update the template title from "Release Note" to "Sprint Release"
- Line 130 (documentListWidget types array): replace `'releaseNote'` with `'release'`; add `'roadmapItem'` to the same array
- Line 145 (Recently Published widget types array): replace `'releaseNote'` with `'release'`; add `'roadmapItem'`
- Line 171 (typesWithCustomActions): replace `'releaseNote'` with `'release'`

**studio/src/structure.ts — 3 sites:**
- Lines 233-234 (Published section): replace `.schemaType('releaseNote').filter('_type == "releaseNote" && status == "published"')` with `.schemaType('release').filter('_type == "release"')` — the release schema has no status field, filter on _type only; update the section label from "Release Notes" to "Sprint Releases"
- Lines 267-276 (Standalone sidebar entry): replace the entire "Release Notes" list item with a "Sprint Releases" item using `.schemaType('release').filter('_type == "release"')`. Then add a second new sidebar entry for "Roadmap Items" using `.schemaType('roadmapItem').filter('_type == "roadmapItem"')` immediately after. Use the same S.documentList() pattern already present in the file.
- Line 327 (exclusion filter): replace `'releaseNote'` with `'release'`; add `'roadmapItem'` to the same exclusion array so neither type appears in the auto-generated fallback section

**Delete:**
- Delete studio/schemaTypes/releaseNote.ts entirely after updating the import in index.ts
  </action>
  <verify>
    <automated>cd /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/studio && npx tsc --noEmit 2>&1; echo "exit: $?"; grep -rn "releaseNote" studio/schemaTypes/ studio/sanity.config.ts studio/src/structure.ts 2>/dev/null | grep -v ".git" | grep -v "generated" && echo "FAIL: releaseNote references remain" || echo "PASS: no releaseNote references"</automated>
  </verify>
  <done>Studio TypeScript compiles with no errors; zero occurrences of 'releaseNote' in schemaTypes/index.ts, sanity.config.ts, or src/structure.ts; releaseNote.ts is deleted</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Two new Sanity schemas (release and roadmapItem) are live in Studio; the old releaseNote schema is fully removed. Studio sidebar shows Sprint Releases and Roadmap Items. TypeScript compiles cleanly.</what-built>
  <how-to-verify>
    1. Run Studio locally: `cd studio && npx sanity dev`
    2. Open Studio in browser (default http://localhost:3333)
    3. Verify the sidebar shows "Sprint Releases" — "Release Notes" must NOT appear
    4. Verify the sidebar shows "Roadmap Items"
    5. Click "+ New document" and verify "Sprint Release" and "Roadmap Item" appear; "Release Note" must NOT appear
    6. Click "Sprint Release" → verify all fields render: Display Title (required), Sprint ID (optional), Slug (auto-fill from title), Published Date, Summary, Release Items (array with + Add Item button)
    7. Click "+ Add Item" → verify item fields render: Title, Description, Change Type (radio), Affected Areas (tags), Screenshot (image upload), Video URL
    8. Click "Roadmap Item" → verify all fields render: Title, Description, Status (radio with Planned/In Progress/Shipped), Business Value, Change Type (radio), UI Change (toggle), Entities Impacted (tags), Projected Release, Sprint Release (reference picker — only shows release documents)
  </how-to-verify>
  <resume-signal>Type "approved" when all fields verified, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Zero occurrences of 'releaseNote' (string) in studio/schemaTypes/index.ts, studio/sanity.config.ts, studio/src/structure.ts.

Studio TypeScript compiles without errors: `cd studio && npx tsc --noEmit` exits 0.

New schema files exist and export the correct identifiers:
- grep "export const releaseType" studio/schemaTypes/release.ts
- grep "export const roadmapItemType" studio/schemaTypes/roadmapItem.ts

Old file deleted: studio/schemaTypes/releaseNote.ts does NOT exist.

Human checkpoint approved: sidebar shows Sprint Releases + Roadmap Items, all fields render correctly in Studio.
</verification>

<success_criteria>
- Editor can create a sprint release document with all fields (displayTitle, sprintId, slug, publishedAt, summary, items[]) in Sanity Studio
- Each release item correctly shows title, description, changeType (radio), affectedAreas (tags), screenshot (image), videoUrl fields
- Editor can create a roadmap item with all fields; a Shipped item's releaseRef picker correctly filters to release documents only
- Studio sidebar has "Sprint Releases" and "Roadmap Items" entries; "Release Notes" is gone
- Zero TypeScript errors in Studio after migration
- Human checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/06-schema-and-data-pipeline/06-01-SUMMARY.md`
</output>
