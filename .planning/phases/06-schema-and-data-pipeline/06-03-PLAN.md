---
phase: 06-schema-and-data-pipeline
plan: "03"
type: execute
wave: 2
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - classic/src/data/sanity-releases.generated.json
  - classic/src/data/sanity-roadmap.generated.json
autonomous: false
requirements:
  - MOCK-01
  - MOCK-02

must_haves:
  truths:
    - "2-3 sprint release documents are visible in Sanity Studio and contain 3-4 items each"
    - "At least one release item has a screenshot URL and at least one has a video embed URL"
    - "5-8 roadmap items are visible in Sanity Studio covering all three statuses (Planned, In Progress, Shipped)"
    - "All roadmap item fields are populated — no empty businessValue, changeType, or entitiesImpacted fields"
    - "At least one Shipped roadmap item has releaseRef linked to one of the mock sprint releases"
    - "Running the fetch script locally with SANITY_FETCH_STRICT=true shows non-zero document counts for both types"
    - "sanity-releases.generated.json contains 2-3 documents with populated items arrays"
    - "sanity-roadmap.generated.json contains 5-8 documents; at least one has a non-null releaseSlug string (not a _ref object)"
  artifacts:
    - path: "classic/src/data/sanity-releases.generated.json"
      provides: "Non-empty array of sprint release objects with items[] arrays"
      min_lines: 10
    - path: "classic/src/data/sanity-roadmap.generated.json"
      provides: "Non-empty array of roadmap items; at least one with non-null releaseSlug"
      min_lines: 10
  key_links:
    - from: "Sanity Studio (releaseRef on Shipped roadmap item)"
      to: "sanity-roadmap.generated.json releaseSlug field"
      via: "GROQ: releaseRef->slug.current projection in fetchRoadmapItems()"
      pattern: "releaseSlug.*sprint-"
---

<objective>
Enter sample content into Sanity Studio (2-3 sprint releases with items, 5-8 roadmap items covering all statuses) and verify the fetch pipeline produces correct, non-empty JSON files — the data contract Phase 7 and Phase 8 depend on.

Purpose: Mock data serves two purposes. First, it validates that the schemas work correctly in the editorial flow. Second, it produces real JSON that downstream phases can build against immediately, avoiding "empty page" issues during development.

Output: Populated Sanity Studio, non-empty generated JSON files confirmed by automated count checks.
</objective>

<execution_context>
@C:/Users/abdel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/abdel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-schema-and-data-pipeline/06-RESEARCH.md
@.planning/phases/06-schema-and-data-pipeline/06-01-SUMMARY.md
@.planning/phases/06-schema-and-data-pipeline/06-02-SUMMARY.md

<interfaces>
<!-- Expected JSON shapes (from 06-RESEARCH.md) -->

sanity-releases.generated.json shape:
[
  {
    "_id": "...",
    "displayTitle": "March 2026 Release A",
    "sprintId": "Sprint 2026.03-A",
    "slug": {"current": "sprint-2026-03-a"},
    "publishedAt": "2026-03-07",
    "summary": "Short summary for the index card...",
    "items": [
      {
        "_key": "key1",
        "title": "New alarm grouping view",
        "description": "Operators can now group alarms by zone...",
        "changeType": "feature",
        "affectedAreas": ["alarm-management"],
        "screenshotUrl": "https://cdn.sanity.io/images/.../...",
        "videoUrl": null
      }
    ]
  }
]

sanity-roadmap.generated.json shape (critical: releaseSlug must be a string, not a _ref object):
[
  {
    "_id": "...",
    "title": "Bulk device configuration",
    "status": "Shipped",
    "businessValue": "Reduces configuration time by 80%...",
    "changeType": "feature",
    "uiChange": true,
    "entitiesImpacted": ["Devices", "Admin"],
    "projectedRelease": "Q1 2026",
    "releaseSlug": "sprint-2026-03-a"   // string, not {_ref: "..."}
  },
  {
    "_id": "...",
    "title": "Advanced reporting filters",
    "status": "Planned",
    "releaseSlug": null                 // null is correct for non-Shipped items
  }
]

Smoke test commands (from 06-VALIDATION.md):
  node -e "const r=require('./classic/src/data/sanity-releases.generated.json');process.exit(r.length>=2?0:1)"
  node -e "const r=require('./classic/src/data/sanity-roadmap.generated.json');process.exit(r.length>=5?0:1)"
  node -e "const r=require('./classic/src/data/sanity-roadmap.generated.json');const s=r.find(i=>i.releaseSlug);process.exit(s?0:1)"
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>Sanity Studio has the sprint release and roadmap item schemas live (from Plan 01). The fetch pipeline is ready to write JSON (from Plan 02). Now sample content must be created in Studio so the pipeline has data to export.</what-built>
  <how-to-verify>
    Create the following content in Sanity Studio (http://localhost:3333 or the deployed studio URL):

    **Step 1 — Create 3 sprint releases (publish each after creating):**

    Release 1:
    - Display Title: "March 2026 Release A"
    - Sprint ID: "Sprint 2026.03-A"
    - Slug: (auto-fill from Display Title) → sprint-2026-03-a
    - Published Date: 2026-03-07
    - Summary: "Alarm management improvements and new reporting filters for faster incident response."
    - Add 3 items:
      - Item 1: Title "New alarm grouping view", Description "Operators can now group active alarms by zone or device type for faster triage.", Change Type "New Feature", Affected Areas "Alarm Management", upload any placeholder screenshot image
      - Item 2: Title "Bulk acknowledge alarms", Description "Select multiple alarms and acknowledge them in a single action.", Change Type "Improvement", Affected Areas "Alarm Management"
      - Item 3: Title "Report filter persistence", Description "Date range and filter selections now persist across browser sessions.", Change Type "Bug Fix", Affected Areas "Reporting", Video URL "https://www.youtube.com/embed/dQw4w9WgXcQ"
    - Click Publish

    Release 2:
    - Display Title: "February 2026 Release B"
    - Sprint ID: "Sprint 2026.02-B"
    - Slug: sprint-2026-02-b
    - Published Date: 2026-02-21
    - Summary: "Device configuration and operator guide updates."
    - Add 3 items:
      - Item 1: Title "Bulk device configuration", Description "Configure multiple devices simultaneously from a single panel.", Change Type "New Feature", Affected Areas "Devices"
      - Item 2: Title "Installer guide restructure", Description "Reorganised installer guide with clearer step-by-step sections.", Change Type "Improvement", Affected Areas "Installer Guide"
      - Item 3: Title "Platform SSO login fix", Description "Fixed edge case where SSO login failed after session timeout.", Change Type "Bug Fix", Affected Areas "Platform Fundamentals"
    - Click Publish

    Release 3:
    - Display Title: "February 2026 Release A"
    - Sprint ID: "Sprint 2026.02-A"
    - Slug: sprint-2026-02-a
    - Published Date: 2026-02-07
    - Summary: "API integrations and knowledge base improvements."
    - Add 2 items:
      - Item 1: Title "Webhook retry logic", Description "Webhooks now retry up to 3 times on failure with exponential backoff.", Change Type "Improvement", Affected Areas "API / Integrations"
      - Item 2: Title "Knowledge base search index", Description "Knowledge base articles are now fully indexed for search.", Change Type "Improvement", Affected Areas "Knowledge Base"
    - Click Publish

    **Step 2 — Create 7 roadmap items (publish each after creating):**

    Item 1 (Shipped):
    - Title: "Bulk device configuration"
    - Description: "Allow operators to configure multiple devices simultaneously from a single panel — reduces setup time for large deployments."
    - Status: Shipped
    - Business Value: "Reduces device setup time by 80% for deployments with 50+ devices."
    - Change Type: New Feature
    - UI Change: Yes (toggle on)
    - Entities Impacted: Devices, Admin
    - Projected Release: Q1 2026
    - Sprint Release: (reference picker) → select "February 2026 Release B"
    - Publish

    Item 2 (Shipped):
    - Title: "New alarm grouping view"
    - Description: "Group active alarms by zone or device type for faster operator triage."
    - Status: Shipped
    - Business Value: "Reduces mean time to acknowledge alarms by 40% in high-volume environments."
    - Change Type: New Feature
    - UI Change: Yes
    - Entities Impacted: Alarm Management, Operators
    - Projected Release: Q1 2026
    - Sprint Release: → select "March 2026 Release A"
    - Publish

    Item 3 (In Progress):
    - Title: "Advanced reporting filters"
    - Description: "Date range, custom field filters, and filter preset saving for reports."
    - Status: In Progress
    - Business Value: "Reduces manual data export effort by 60% for weekly reporting workflows."
    - Change Type: Improvement
    - UI Change: Yes
    - Entities Impacted: Reporting
    - Projected Release: Q2 2026
    - Publish

    Item 4 (In Progress):
    - Title: "API rate limit visibility"
    - Description: "Show current API usage and rate limit status in the developer dashboard."
    - Status: In Progress
    - Business Value: "Prevents unexpected API throttling by giving integrators real-time usage visibility."
    - Change Type: New Feature
    - UI Change: No (leave toggle off)
    - Entities Impacted: API / Integrations
    - Projected Release: Q2 2026
    - Publish

    Item 5 (Planned):
    - Title: "Multi-language support (Phase 1)"
    - Description: "UI language switching for English and Spanish — targeted at Latin American market."
    - Status: Planned
    - Business Value: "Opens the platform to Spanish-speaking operators; required for two pending enterprise contracts."
    - Change Type: New Feature
    - UI Change: Yes
    - Entities Impacted: Platform Fundamentals, Operators
    - Projected Release: Q3 2026
    - Publish

    Item 6 (Planned):
    - Title: "Automated compliance report generation"
    - Description: "Schedule and auto-generate compliance reports in PDF format on a weekly or monthly cadence."
    - Status: Planned
    - Business Value: "Eliminates 4+ hours/month of manual report preparation for compliance teams."
    - Change Type: New Feature
    - UI Change: Yes
    - Entities Impacted: Reporting, Admin
    - Projected Release: Q3 2026
    - Publish

    Item 7 (Planned):
    - Title: "Operator shift handover notes"
    - Description: "Structured handover notes field that operators complete at end-of-shift, attached to the active alarm queue state."
    - Status: Planned
    - Business Value: "Reduces missed alarms during shift changeover — top feedback item from Q4 customer survey."
    - Change Type: New Feature
    - UI Change: Yes
    - Entities Impacted: Alarm Management, Operators
    - Projected Release: Q4 2026
    - Publish

    Verify in GROQ Vision (Studio → Vision tab) that references resolved correctly:
    Query: *[_type == "roadmapItem" && defined(releaseRef)]{"title": title, "releaseSlug": releaseRef->slug.current}
    Expected: 2 results with releaseSlug as strings (e.g. "sprint-2026-02-b", "sprint-2026-03-a"), NOT _ref objects.
  </how-to-verify>
  <resume-signal>Type "done" when all releases and roadmap items are published in Studio and the GROQ Vision query shows string releaseSlug values</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Run fetch pipeline and verify non-empty JSON output</name>
  <files>classic/src/data/sanity-releases.generated.json, classic/src/data/sanity-roadmap.generated.json</files>
  <action>
Run the fetch script in strict mode to confirm no warnings are emitted and both generated JSON files contain real data.

```bash
cd classic && SANITY_FETCH_STRICT=true node scripts/fetch-sanity-content.js
```

After the script completes, run the three smoke count checks to confirm non-empty output and correct reference resolution.

If the strict-mode run fails (exits non-zero), inspect the error output. Common causes:
- SANITY_PROJECT_ID or SANITY_API_TOKEN not set in local .env — load them and retry
- GROQ syntax error in a query — check the query functions added in Plan 02
- Documents are still drafts in Studio — verify each document was published (green "Published" indicator in Studio, not "Draft")

Do NOT change build.sh. The || true guard stays as-is for production resilience.
  </action>
  <verify>
    <automated>cd /c/Users/abdel/.gemini/antigravity/scratch/nxgen-docs/classic && node -e "const r=require('./src/data/sanity-releases.generated.json');process.exit(r.length>=2?0:1)" && echo "PASS: releases>=2" || echo "FAIL: releases<2"; node -e "const r=require('./src/data/sanity-roadmap.generated.json');process.exit(r.length>=5?0:1)" && echo "PASS: roadmap>=5" || echo "FAIL: roadmap<5"; node -e "const r=require('./src/data/sanity-roadmap.generated.json');const s=r.find(i=>i.releaseSlug);process.exit(s?0:1)" && echo "PASS: has releaseSlug string" || echo "FAIL: no non-null releaseSlug found"</automated>
  </verify>
  <done>sanity-releases.generated.json contains >= 2 release documents with non-empty items arrays; sanity-roadmap.generated.json contains >= 5 roadmap items; at least one item has releaseSlug as a non-null string (confirming GROQ dereference is working correctly); SANITY_FETCH_STRICT=true exits 0 with no warnings</done>
</task>

</tasks>

<verification>
Three smoke count checks all pass:
- node -e "const r=require('./classic/src/data/sanity-releases.generated.json');process.exit(r.length>=2?0:1)" exits 0
- node -e "const r=require('./classic/src/data/sanity-roadmap.generated.json');process.exit(r.length>=5?0:1)" exits 0
- node -e "const r=require('./classic/src/data/sanity-roadmap.generated.json');const s=r.find(i=>i.releaseSlug);process.exit(s?0:1)" exits 0

SANITY_FETCH_STRICT=true exits 0 (zero warnings emitted).

Inspect sanity-roadmap.generated.json — for Shipped items with releaseRef: the releaseSlug field is a string like "sprint-2026-03-a", NOT an object like {"_ref": "abc123"}.

Inspect sanity-releases.generated.json — at least one item has a non-null screenshotUrl string (confirms screenshot.asset->url GROQ dereference is working).
</verification>

<success_criteria>
- 2-3 sprint releases published in Sanity Studio with 3-4 items each (at least one screenshot, at least one video URL)
- 5-8 roadmap items published covering Planned, In Progress, and Shipped statuses; all fields populated
- At least one Shipped roadmap item has releaseRef linked to a sprint release
- sanity-releases.generated.json: >= 2 documents, valid shape matching the research spec
- sanity-roadmap.generated.json: >= 5 documents, releaseSlug is a string (not _ref object) on Shipped items
- SANITY_FETCH_STRICT=true exits 0
- Phase 6 complete: Phase 7 and Phase 8 can proceed with real data
</success_criteria>

<output>
After completion, create `.planning/phases/06-schema-and-data-pipeline/06-03-SUMMARY.md`
</output>
