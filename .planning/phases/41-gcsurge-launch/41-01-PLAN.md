---
phase: 41-gcsurge-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - classic/product.config.ts
  - classic/static/img/gcsurge-logo.png
  - classic/static/img/favicon-gcsurge.png
autonomous: true
requirements:
  - SURGE-01
user_setup: []

must_haves:
  truths:
    - "GC Surge logo appears in site header"
    - "GC Surge favicon appears in browser tab"
    - "Product config references correct asset paths"
    - "Theme CSS has gcsurge class defined"
  artifacts:
    - path: "classic/static/img/gcsurge-logo.png"
      provides: "GC Surge logo image"
    - path: "classic/static/img/favicon-gcsurge.png"
      provides: "GC Surge favicon"
    - path: "classic/product.config.ts"
      provides: "Product configuration"
      contains: "gcsurge"
    - path: "classic/src/css/product-themes.css"
      provides: "Theme styling"
      contains: ".theme-gcsurge"
  key_links:
    - from: "classic/product.config.ts"
      to: "classic/static/img/gcsurge-logo.png"
      via: "logo.src reference"
      pattern: "gcsurge-logo.png"
    - from: "classic/product.config.ts"
      to: "classic/static/img/favicon-gcsurge.png"
      via: "favicon reference"
      pattern: "favicon-gcsurge.png"
---

<objective>
Verify GC Surge branding assets are in place and correctly referenced in product configuration. Confirm logo, favicon, and theme CSS are properly configured before content import and deployment.

Purpose: Ensure visual identity is ready for GC Surge product launch.
Output: Verified branding configuration ready for build.
</objective>

<execution_context>
@~\.config\opencode/get-shit-done/workflows/execute-plan.md
@~\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@classic/product.config.ts
@classic/src/css/product-themes.css
</context>

<tasks>

<task type="auto">
  <name>Verify branding asset files exist</name>
  <files>
    - classic/static/img/gcsurge-logo.png
    - classic/static/img/favicon-gcsurge.png
  </files>
  <action>
    Verify the GC Surge branding assets exist in the static directory:
    1. Check that `classic/static/img/gcsurge-logo.png` exists and is a valid image file
    2. Check that `classic/static/img/favicon-gcsurge.png` exists and is a valid image file
    3. Report file sizes to confirm they're not placeholder/empty files
    
    If files are missing or invalid, this task should fail with clear error message.
  </action>
  <verify>
    <automated>ls -la classic/static/img/gcsurge-logo.png classic/static/img/favicon-gcsurge.png 2>&1 | grep -E "gcsurge-"</automated>
  </verify>
  <done>
    Both gcsurge-logo.png and favicon-gcsurge.png exist with non-zero file sizes.
  </done>
</task>

<task type="auto">
  <name>Verify product.config.ts references</name>
  <files>
    - classic/product.config.ts
  </files>
  <action>
    Verify that product.config.ts has correct gcsurge configuration:
    1. Check `logo.src` is `'img/gcsurge-logo.png'` (relative to static/)
    2. Check `favicon` is `'img/favicon-gcsurge.png'` (relative to static/)
    3. Check `theme.primaryColor` is `'#3B82F6'` (blue)
    4. Check `title` is `'GC Surge Documentation'`
    5. Check `url` is `'https://docs.gcsurge.com'`
    
    If any values are incorrect, update them to match the expected configuration.
  </action>
  <verify>
    <automated>grep -E "(gcsurge-logo.png|favicon-gcsurge.png|#3B82F6)" classic/product.config.ts | head -5</automated>
  </verify>
  <done>
    Product config has correct logo, favicon, theme color, title, and URL for GC Surge.
  </done>
</task>

<task type="auto">
  <name>Verify theme CSS has gcsurge class</name>
  <files>
    - classic/src/css/product-themes.css
  </files>
  <action>
    Verify that `classic/src/css/product-themes.css` contains the `.theme-gcsurge` class with:
    1. Primary color CSS custom properties (--product-primary, --product-primary-dark, etc.)
    2. Glow effects (--product-glow, --product-glow-sm)
    3. Dark mode variant (`.theme-gcsurge[data-theme='dark']`)
    4. Proper application of tokens to Docusaurus variables
    
    The class should already exist from Phase 38. Verify it's complete and correct.
  </action>
  <verify>
    <automated>grep -c "theme-gcsurge" classic/src/css/product-themes.css</automated>
  </verify>
  <done>
    Theme CSS contains complete .theme-gcsurge class with primary color #3B82F6 and dark mode variant.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run `ls classic/static/img/gcsurge*` to list all gcsurge assets
2. Verify product.config.ts gcsurge entry matches expected configuration
3. Confirm theme-gcsurge class exists in CSS
</verification>

<success_criteria>
- [ ] gcsurge-logo.png exists in static/img/
- [ ] favicon-gcsurge.png exists in static/img/
- [ ] product.config.ts has correct gcsurge configuration
- [ ] product-themes.css has .theme-gcsurge class
- [ ] All file references are valid paths
</success_criteria>

<output>
After completion, create `.planning/phases/41-gcsurge-launch/41-01-SUMMARY.md`
</output>