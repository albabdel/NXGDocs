---
phase: 41-gcsurge-launch
plan: 03
type: execute
wave: 2
depends_on:
  - 41-01
  - 41-02
files_modified:
  - classic/build/gcsurge/
autonomous: false
requirements:
  - SURGE-04
  - SURGE-05
user_setup:
  - service: cloudflare
    why: "Deploy gcsurge build to Cloudflare Pages"
    env_vars: []
    dashboard_config:
      - task: "Verify gcsurge.pages.dev project exists"
        location: "Cloudflare Dashboard > Pages > gcsurge"
      - task: "Set PRODUCT=gcsurge environment variable in Cloudflare"
        location: "Cloudflare Pages > gcsurge > Settings > Environment variables"

must_haves:
  truths:
    - "User can visit gcsurge.pages.dev and see GC Surge documentation"
    - "GC Surge logo appears in header"
    - "GC Surge favicon appears in browser tab"
    - "Theme is blue (#3B82F6) not gold"
    - "Content pages from Confluence are visible"
    - "Navigation is independent from GCXONE"
  artifacts:
    - path: "classic/build/gcsurge/"
      provides: "Built GC Surge site"
      min_lines: 100
    - path: "gcsurge.pages.dev"
      provides: "Deployed GC Surge documentation"
  key_links:
    - from: "build-multi-product.js"
      to: "classic/build/gcsurge/"
      via: "npm run build:multi"
      pattern: "PRODUCT=gcsurge"
    - from: "classic/build/gcsurge/"
      to: "gcsurge.pages.dev"
      via: "Cloudflare Pages deployment"
      pattern: "git push|wrangler"

---

<objective>
Build the GC Surge product and deploy to Cloudflare Pages. Verify the complete end-to-end GC Surge experience works independently from GCXONE.

Purpose: Deploy GC Surge documentation site for public access.
Output: Deployed gcsurge.pages.dev with complete documentation.
</objective>

<execution_context>
@~\.config\opencode/get-shit-done/workflows/execute-plan.md
@~\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@classic/scripts/build-multi-product.js
@classic/product.config.ts
</context>

<tasks>

<task type="auto">
  <name>Build gcsurge product locally</name>
  <files>
    - classic/build/gcsurge/
  </files>
  <action>
    Build the GC Surge product using the multi-build pipeline:
    
    1. Set environment:
       ```
       PRODUCT=gcsurge
       ```
    
    2. Run build for gcsurge:
       ```bash
       cd classic
       npm run docusaurus build -- --out-dir build/gcsurge
       ```
    
    Alternatively, use the multi-build script for gcsurge only:
       ```bash
       node classic/scripts/build-multi-product.js gcsurge
       ```
    
    3. Verify build output:
       - Check `classic/build/gcsurge/` directory exists
       - Check `classic/build/gcsurge/index.html` exists
       - Check build completed without errors
    
    The build will:
    - Fetch content from Sanity with product=gcsurge filter
    - Apply gcsurge theme (blue)
    - Use gcsurge logo and favicon
    - Generate static files in `classic/build/gcsurge/`
  </action>
  <verify>
    <automated>test -d classic/build/gcsurge && test -f classic/build/gcsurge/index.html && echo "Build output exists"</automated>
  </verify>
  <done>
    Build directory classic/build/gcsurge/ exists with index.html and static assets.
  </done>
</task>

<task type="auto">
  <name>Verify build output has gcsurge branding</name>
  <files>
    - classic/build/gcsurge/index.html
    - classic/build/gcsurge/assets/
  </files>
  <action>
    Verify the built output contains GC Surge branding:
    
    1. Check index.html for gcsurge logo reference:
       ```bash
       grep "gcsurge-logo" classic/build/gcsurge/index.html
       ```
    
    2. Check for blue theme color (#3B82F6):
       ```bash
       grep -E "(#3B82F6|theme-gcsurge)" classic/build/gcsurge/index.html
       ```
    
    3. Check for GC Surge title:
       ```bash
       grep "GC Surge Documentation" classic/build/gcsurge/index.html
       ```
    
    4. Verify favicon reference:
       ```bash
       grep "favicon-gcsurge" classic/build/gcsurge/index.html
       ```
    
    If any branding is missing, the build configuration may need adjustment.
  </action>
  <verify>
    <automated>grep -c "gcsurge" classic/build/gcsurge/index.html</automated>
  </verify>
  <done>
    Build output contains gcsurge logo, favicon, title, and theme references.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Deploy to Cloudflare Pages</name>
  <files>
    - gcsurge.pages.dev
  </files>
  <action>
    Deploy the gcsurge build to Cloudflare Pages. Two options:
    
    **Option A: Git Integration (Recommended)**
    The Cloudflare project should auto-deploy on git push if configured:
    1. Ensure `PRODUCT=gcsurge` is set in Cloudflare environment variables
    2. Push changes to trigger deployment
    3. Cloudflare will build and deploy to gcsurge.pages.dev
    
    **Option B: Manual Deployment via Wrangler**
    If wrangler is configured:
    ```bash
    cd classic
    npx wrangler pages deploy build/gcsurge --project-name=gcsurge
    ```
    
    **Verification:**
    After deployment, verify:
    1. https://gcsurge.pages.dev loads
    2. Logo shows GC Surge (not GCXONE)
    3. Theme is blue
    4. Content pages are accessible
  </action>
  <resume-signal>Type "deployed" when deployment complete, or describe any issues</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify end-to-end GC Surge experience</name>
  <files>
    - https://gcsurge.pages.dev
  </files>
  <what-built>
    GC Surge documentation site deployed to Cloudflare Pages with:
    - GC Surge branding (logo, favicon)
    - Blue theme (#3B82F6)
    - Content imported from Confluence
    - Independent from GCXONE
  </what-built>
  <how-to-verify>
    1. Visit https://gcsurge.pages.dev
    2. Verify logo shows "GC Surge" (not GCXONE)
    3. Verify favicon shows GC Surge icon
    4. Verify theme is blue (check header color)
    5. Navigate to a content page
    6. Verify content loads correctly
    7. Verify URL is gcsurge.pages.dev (not docs.gcxone.com)
    8. Check browser console for errors
    9. Verify no GCXONE branding appears
  </how-to-verify>
  <resume-signal>Type "approved" if verification passed, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:
1. Build output exists in classic/build/gcsurge/
2. Build contains gcsurge branding
3. Deployment to Cloudflare Pages successful
4. Site accessible at gcsurge.pages.dev
5. End-to-end verification passed (human verified)
</verification>

<success_criteria>
- [ ] Build output exists in classic/build/gcsurge/
- [ ] Build contains gcsurge-logo reference
- [ ] Build contains blue theme (#3B82F6)
- [ ] Build contains GC Surge title
- [ ] Deployment to Cloudflare Pages complete
- [ ] Site accessible at gcsurge.pages.dev
- [ ] Logo shows GC Surge branding
- [ ] Theme is blue (not gold)
- [ ] Content pages accessible
- [ ] No GCXONE branding present
</success_criteria>

<output>
After completion, create `.planning/phases/41-gcsurge-launch/41-03-SUMMARY.md`
</output>