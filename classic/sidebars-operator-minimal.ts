import type { SidebarsConfig } from '@docusaurus/plugin-content-docs';

const sidebars: SidebarsConfig = {
  operatorMinimalSidebar: [
    {
      type: 'doc',
      id: 'index',
      label: 'Operator Minimal Home',
    },
    // Additional pages can be added here as content is created
  ],
};

export default sidebars;
