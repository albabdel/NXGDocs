name = "gcxone"
compatibility_date = "2024-01-01"

# Multi-project configuration
# Each product deploys to its own Cloudflare Pages project:
#   - gcxone.pages.dev (gcxone product)
#   - gcsurge.pages.dev (gcsurge product)
#
# Deploy commands:
#   npm run deploy:gcxone  -> deploys to gcxone.pages.dev
#   npm run deploy:gcsurge -> deploys to gcsurge.pages.dev
#   npm run deploy:all     -> deploys both products
#
# Environment variables set in Cloudflare Pages dashboard:
#   PRODUCT=gcxone or PRODUCT=gcsurge
#   SANITY_PROJECT_ID, SANITY_DATASET, SANITY_API_TOKEN

# pages_build_output_dir is intentionally NOT set here.
# Each CF Pages project has its own destination_dir configured in the dashboard:
#   gcxone project  -> classic/build/gcxone
#   gcsurge project -> classic/build/gcsurge
# Setting it here would override the dashboard setting for BOTH projects.

[vars]
SANITY_PROJECT_ID = "fjjuacab"
SANITY_DATASET = "production"
AUTH0_DOMAIN = "nxgen.eu.auth0.com"
ZOHO_ORG_ID = "20067436506"
# Secrets — set in Cloudflare Pages dashboard (Settings → Environment variables), never committed:
#   SANITY_API_TOKEN       — Sanity read token
#   ZOHO_REFRESH_TOKEN     — Zoho service account refresh token (from api-console.zoho.eu, authorization_code grant)
#   ZOHO_CLIENT_ID         — Self Client app ID used to obtain the refresh token: 1000.QW27E1UCMBJEBHEJYD7US8Q83FVRHW
#                            (NOT the same as the frontend agent OAuth app 1000.F5X0EPUNG5MJ7NGV5T4CSVO8AU1TZN)
#   ZOHO_CLIENT_SECRET     — Zoho OAuth app client secret (from api-console.zoho.eu)
# To generate ZOHO_REFRESH_TOKEN:
#   1. Go to https://api-console.zoho.eu → open your OAuth app → "Self Client" tab
#   2. Scope: Desk.tickets.READ Desk.tickets.UPDATE Desk.tickets.CREATE Desk.contacts.READ Desk.basic.READ
#   3. Generate code (valid 3 min), then POST to https://accounts.zoho.eu/oauth/v2/token with grant_type=authorization_code
#   4. Save the refresh_token — it is long-lived
